---
phase: quick-260827-lwq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _pages/software.md
autonomous: true
requirements: [SW-DEL-01]   # SW-DEL-01 = this quick task's directive: delete the MambaForSequenceClassification software card (the complete section-card block, lines 72-83 of _pages/software.md, plus its preceding blank line) from the Software page; it is the only site-wide Mamba reference; no other card or style rule changes

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low   # estimate-calibration: sample_count 0, factor 1 (not applied)

must_haves:
  truths:
    - "`_pages/software.md` contains zero occurrences of the string `MambaForSequenceClassification`; the 12-line section-card block (planner-verified: lines 72-83) plus its preceding blank line (line 71) are gone; file goes 150 → 137 lines"
    - "Exactly one blank line separates the dnallmmark section-card's closing `</div>` (post-edit line 70) from the `<h2 class=\"section-heading\">… Genome Editing</h2>` heading (post-edit line 72, the fa-scissors heading)"
    - "All 7 surviving cards are untouched — DNALLM-Suite, PDLLMs, dnallmmark, CrisprStitch, Chorus2, rustkmer, SIF: source `<div class=\"section-card\"` count = 7 and `<div class=\"software-card\"` count = 7 (both were 8; substring match — the DNALLM-Suite divs carry extra style attributes)"
    - "Live serve page http://127.0.0.1:4000/software/ (jekyll serve --livereload running at plan time, edit-to-render ≤10s per Phase 1 probes) renders: deleted title 0 times, `class=\"software-card\"` exactly 7 times, `dnallmmark` ≥1, `CrisprStitch` ≥1; regenerated `_site/software/index.html` and `_site/assets/search.json` are likewise free of the deleted title"
    - "`bash scripts/validate.sh` exits 0 and `bash scripts/ci-smoke.sh _site` prints its PASS line (sitemap count unaffected — the /software/ page itself persists)"
    - "The change is a pure deletion — `git diff --numstat` shows 0 added / 13 deleted — landed as ONE local commit touching exactly `_pages/software.md`, NOT pushed (DEPLOY_ENABLED is live: a push to main auto-deploys production zhangtaolab.org)"
  artifacts:
    - "_pages/software.md: exactly 13 lines removed (1 blank + 12 card lines), zero lines added or modified elsewhere, including the <style> block"
  key_links:
    - "_pages/software.md edit → jekyll serve --livereload regen → http://127.0.0.1:4000/software/ render + `_site/software/index.html` + `_site/assets/search.json` (search index carries page content — planner-verified it currently contains the deleted title and must drop it on regen) → curl occurrence-count assertions"
    - "No other consumer exists: planner-verified scoped grep (`_pages _includes _layouts _data assets images feed.xml _config.yml`) finds the deleted title ONLY in _pages/software.md — there is no _data entry, include, layout, or feed linkage to update"
    - "_site/sitemap.xml → scripts/ci-smoke.sh assertion ① stays green because /software/ remains a live page (only a card inside it is removed)"
---

<objective>
Remove the MambaForSequenceClassification software card from the Software page. It is one of four cards under the "DNA Large Language Models" heading and is the only site-wide reference to that tool (planner-verified: `_includes/_layouts/assets/_data/images/feed.xml` are all clean; rendered copies live only in `_site/software/index.html` and `_site/assets/search.json`, both regenerated by the running serve).

The deletion target is planner-verified against the current file (150 lines):
- line 70: `</div>` closing the dnallmmark section-card — KEEP
- line 71: blank — DELETE (with the block, so exactly one blank line remains)
- lines 72-83: the complete `section-card` block — DELETE: opening `<div class="section-card" markdown="0">`, inner `software-card`/`software-body` divs, the h4 title, the `pub-actions` div with the GitHub button link, the description paragraph, the `software-thumb-placeholder` div, and the three closing `</div>`s
- line 84: blank — KEEP
- line 85: `<h2 class="section-heading">… Genome Editing</h2>` — KEEP

Net effect: 13 lines removed, file becomes 137 lines, and one blank line separates the dnallmmark card from the Genome Editing heading.

Explicitly OUT of scope: every other card, the `<style>` block (lines 11-24, shared by all cards), and both blank-line separations elsewhere. Nothing is added anywhere.

Purpose: retire a software entry the lab no longer lists, keeping the page structure and shared CSS intact.
Output: single-file pure-deletion edit, live-page + validator + smoke proof battery, one local commit (no push).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The only file this task touches (read it once before editing — Edit requires prior Read)
@_pages/software.md

# Proof tooling (validate.sh is a 4-line exec wrapper around validate.rb; ci-smoke.sh takes the site dir as $1)
@scripts/validate.sh
@scripts/ci-smoke.sh

# Planner-verified current state (2026-08-27, clean tree at 1eb4d6f9):
#
# - _pages/software.md is 150 lines; the deletion target is lines 71-83 (blank + 12-line card block)
# - Source counts BEFORE: `<div class="section-card"` = 8 (line 28's carries a style attr — substring match is correct);
#   `<div class="software-card"` = 8; AFTER: both = 7
# - Line 70 = `</div>` (dnallmmark section-card close); line 84 = blank; line 85 = Genome Editing fa-scissors heading
# - Live serve RUNNING: curl http://127.0.0.1:4000/software/ → 200 with `class="software-card"` substring count = 8
#   (planner-measured; DNALLM's styled div also contains the substring, so rendered count 8 ↔ source cards 8)
# - Scoped grep for the deleted title across _pages _includes _layouts _data assets images feed.xml _config.yml:
#   hits ONLY _pages/software.md. In _site: _site/software/index.html + _site/assets/search.json (both regenerate)
# - validate.sh validates _data YAML + papers/ref.bib — software.md is not in its file set, but it is the
#   standard pre-commit battery; ci-smoke.sh _site asserts sitemap ≥10 / publications DOI anchor / feed.xml / vendor —
#   none reference the deleted card, and /software/ stays in the sitemap (page persists)
# - DEPLOY_ENABLED is live (STATE: push to main auto-deploys zhangtaolab.org); prior quick commits (e.g. 772d23c5)
#   are local-only — this task follows the same no-push discipline
</context>

<tasks>

<task type="tracer">
  <name>End-to-end: delete the card block from software.md and prove it gone from the live serve page</name>
  <files>_pages/software.md</files>
  <precondition>`curl -sf http://127.0.0.1:4000/software/` returns 200 (serve running at plan time). If it does not, run `bundle exec jekyll build` and perform the same occurrence-count assertions against `_site/software/index.html` instead of the live URL.</precondition>
  <action>
  <!-- planner-discipline-allow: MambaForSequenceClassification -->
  Read `_pages/software.md` first (Edit requires prior Read). Make exactly ONE Edit whose old_string spans 16 lines — from the `</div>` that closes the dnallmmark section-card (line 70), through the blank line (71), the entire 12-line MambaForSequenceClassification section-card block (lines 72-83), and the blank line (84), up to and including the Genome Editing fa-scissors `<h2 class="section-heading">` line (85) — and whose new_string is the 3-line run: the dnallmmark closing `</div>`, one blank line, then that same `<h2>` heading line verbatim. This removes the block plus exactly one of the two blank lines, leaving one blank line between the dnallmmark card and the heading. The 16-line old_string is unique because it contains the deleted card's content.
  Delete the block EXACTLY as it exists — do not reflow, re-indent, or "clean up" any surviving line. Do NOT touch the `<style>` block (lines 11-24): its `.software-card` / `.software-thumb-placeholder` rules are shared by the 7 surviving cards. Do NOT touch any other card, heading, or the trailing Open Source callout. Nothing is added anywhere — the worktree diff must be pure deletion.
  Then let livereload regenerate (edit-to-render observed ≤10s; the verify below polls up to 30s) and prove the removal on the live page BEFORE moving on.
  </action>
  <verify>
    <automated>for i in $(seq 1 15); do curl -sf http://127.0.0.1:4000/software/ | grep -q 'MambaForSequenceClassification' || break; sleep 2; done; H=$(curl -sf http://127.0.0.1:4000/software/) && printf '%s' "$H" | grep -q 'dnallmmark' && printf '%s' "$H" | grep -q 'CrisprStitch' && [ "$(printf '%s' "$H" | grep -o 'MambaForSequenceClassification' | wc -l | tr -d ' ')" = "0" ] && [ "$(printf '%s' "$H" | grep -o 'class="software-card"' | wc -l | tr -d ' ')" = "7" ] && [ "$(grep -c 'MambaForSequenceClassification' _pages/software.md)" = "0" ] && [ "$(grep -c '<div class="section-card"' _pages/software.md)" = "7" ] && [ "$(grep -c '<div class="software-card"' _pages/software.md)" = "7" ] && [ "$(wc -l < _pages/software.md | tr -d ' ')" = "137" ] && [ "$(sed -n '70p' _pages/software.md)" = "</div>" ] && [ -z "$(sed -n '71p' _pages/software.md)" ] && sed -n '72p' _pages/software.md | grep -q 'fa-scissors' && [ "$(git diff --numstat -- _pages/software.md | cut -f1)" = "0" ] && [ "$(git diff --numstat -- _pages/software.md | cut -f2)" = "13" ] && echo LIVE-DELETE-OK</automated>
  </verify>
  <done>
  Live page battery green: deleted title occurs 0 times (positive guards dnallmmark + CrisprStitch prove the page rendered, not an empty fetch); rendered `class="software-card"` count dropped 8 → exactly 7. Source: 0 title occurrences, both card-div substring counts = 7, file is exactly 137 lines, post-edit line 70 is `</div>`, line 71 is blank, line 72 is the fa-scissors heading. `git diff --numstat` on the file shows 0 added / 13 deleted — pure deletion, nothing else touched.
  </done>
</task>

<task type="auto">
  <name>Validator + smoke battery, regenerated-artifact check, single local commit — no push</name>
  <files>_pages/software.md</files>
  <action>
  The edit landed in Task 1; this task proves the full battery and commits. If you fell back to `jekyll build` in Task 1 (serve down), the build output is already current; otherwise the serve has regenerated `_site` after the Task 1 edit — for `_site/assets/search.json` allow the same regen window (poll loop below, up to 30s) since it embeds page content.
  (1) `bash scripts/validate.sh` must exit 0 — YAML/BibTeX layers; software.md is not in the validator's file set, so this proves no collateral damage to the validated surfaces.
  (2) `bash scripts/ci-smoke.sh _site` must print its PASS line — sitemap ≥10 stays green because /software/ persists (only a card inside it was removed); publications DOI anchor, feed.xml validity, and the vendor tripwire are unaffected.
  (3) Confirm both regenerated artifacts dropped the deleted title: `_site/software/index.html` still contains `dnallmmark` but not the deleted title, and `_site/assets/search.json` no longer matches it case-insensitively.
  (4) Commit exactly one file with the established message convention: `fix(quick/software): remove MambaForSequenceClassification card`. Do NOT push in any form — DEPLOY_ENABLED is live, so a push to main auto-deploys production zhangtaolab.org; push timing belongs to the user/orchestrator.
  </action>
  <verify>
    <automated>for i in $(seq 1 15); do grep -qi 'mamba' _site/assets/search.json || break; sleep 2; done; bash scripts/validate.sh >/dev/null && bash scripts/ci-smoke.sh _site | grep -q '^PASS' && grep -q 'dnallmmark' _site/software/index.html && ! grep -q 'MambaForSequenceClassification' _site/software/index.html && ! grep -qi 'mamba' _site/assets/search.json && [ "$(git show --numstat --format= HEAD | awk '{print $1}')" = "0" ] && [ "$(git show --numstat --format= HEAD | awk '{print $2}')" = "13" ] && [ "$(git diff --name-only HEAD~1 HEAD | tr '\n' ' ')" = "_pages/software.md " ] && git log origin/main..HEAD --oneline | grep -qi mamba && echo SOFTWARE-CARD-DELETE-OK</automated>
  </verify>
  <done>
  validate.sh exit 0; ci-smoke.sh _site prints PASS; regenerated `_site/software/index.html` and `_site/assets/search.json` both free of the deleted title while sibling cards render; the HEAD commit is a pure deletion (0 added / 13 deleted), touches exactly `_pages/software.md`, carries the `fix(quick/software)` message, and sits local-only in the origin/main..HEAD ahead-set — nothing pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Data-only deletion of one static HTML card block in one markdown page; no user input, no runtime code, no dependency changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | over-deletion into the shared `<style>` block or a sibling card (dnallmmark / CrisprStitch), or unbalanced div nesting breaking page layout | low | mitigate | Single-Edit anchor spans dnallmmark close → Genome Editing heading (unique via the deleted card's content); verify gates: exact 137-line count, both card-div substring counts = 7, blank-line shape at lines 70-72, siblings present on live page, numstat exactly 0/13 |
| T-quick-02 | Denial of Service | production deploy via unintended push | medium | mitigate | Executor never invokes git push; DEPLOY_ENABLED gate is live so push to main auto-deploys zhangtaolab.org. Commit stays local; Task 2 verify asserts the commit sits in the origin/main..HEAD ahead-set |
</threat_model>

<verification>
1. Live serve page battery (Task 1): deleted title 0 occurrences, `class="software-card"` exactly 7, dnallmmark + CrisprStitch present (positive guards against vacuous empty-fetch passes).
2. Source shape: 0 title occurrences, `<div class="section-card"` = 7, `<div class="software-card"` = 7, file exactly 137 lines, one blank line between the dnallmmark card close and the Genome Editing heading.
3. `bash scripts/validate.sh` exits 0 and `bash scripts/ci-smoke.sh _site` prints PASS — the same validator runs in CI (deploy.yml Validate content step), so a local pass guarantees the later push will not be blocked.
4. Regenerated artifacts clean: `_site/software/index.html` and `_site/assets/search.json` both drop the deleted title.
5. Exactly one commit, pure deletion (0 added / 13 deleted), touching exactly `_pages/software.md`, local only, nothing pushed.
</verification>

<success_criteria>
- The MambaForSequenceClassification card is gone from the Software page source and every rendered surface; file is 137 lines with exactly one blank line between the dnallmmark card and the Genome Editing heading
- All 7 sibling cards and the shared `<style>` block are byte-identical to before
- Live-page battery + validate.sh + ci-smoke.sh all green; one local `fix(quick/software)` commit, 0 additions / 13 deletions, not pushed
</success_criteria>

<output>
Create `.planning/quick/260827-lwq-delete-mambaforsequenceclassification-so/260827-lwq-SUMMARY.md` when done
</output>
