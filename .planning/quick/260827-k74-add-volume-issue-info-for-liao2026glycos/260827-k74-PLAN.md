---
phase: quick-260827-k74
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - papers/ref.bib
  - _pages/publications.md
autonomous: true
requirements: [BIB-03]   # BIB-03 = this quick task's directive: land volume/issue info for liao2026glycosylase (Science Bulletin 71(12):3032–3045, 30 June 2026 — user-verified from ScienceDirect pii S2095927326005487) and, per the complete-entry convention, drop its DOI placeholder from both surfaces; zheng2025larch stays untouched (no volume info yet)

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low   # estimate-calibration: sample_count 0, factor 1 (not applied)

must_haves:
  truths:
    - "In `papers/ref.bib` the liao2026glycosylase entry reads journal → volume={71} → number={12} → pages={3032--3045} → year={2026} (BibTeX page range uses a literal double hyphen `--`), with no doi field and no trailing comma on the year line — matching the complete-entry convention of he2026trna / liu2025pdllms / yang2025rice"
    - "`grep -c 'doi={' papers/ref.bib` returns exactly 1 (only zheng2025larch's) and `grep -c 'scib' papers/ref.bib` returns 0; `grep -c '^@' papers/ref.bib` stays 12"
    - "In `_pages/publications.md` entry #2 (line 22) ends at `*Science Bulletin* 2026, 71(12):3032&ndash;3045.` — trailing `DOI: https://doi.org/10.1016/j.scib.2026.05.046.` suffix deleted, following the page's own citation convention (cf. entry #6 `*Molecular Plant* 2025, 18(2):175&ndash;178.`)"
    - "The title hyperlink `[...](https://doi.org/10.1016/j.scib.2026.05.046)` at the head of publications.md line 22 is RETAINED (readers click through; good practice). Note: unlike bao2026oryza's s41467 DOI, the scib substring is NOT asserted by scripts/ci-smoke.sh — keep the href regardless"
    - "zheng2025larch is untouched on both surfaces: its ref.bib entry (doi={10.1016/j.hpj.2025.02.020}) and publications.md line 30 (entry #5, `*Horticultural Plant Journal* 2025. DOI: ...`) are byte-identical to HEAD"
    - "The live serve page http://127.0.0.1:4000/publications/ (jekyll serve --livereload running at plan time; edit-to-render observed ≤10s in Phase 1 probes) shows: `2026, 71(12):3032–3045` with a real U+2013 en dash exactly once; zero occurrences of `DOI: https://doi.org/10.1016/j.scib`; exactly one occurrence of `https://doi.org/10.1016/j.scib.2026.05.046` (the title href); `DOI: https` overall count exactly 1 (the HPJ entry #5 placeholder)"
    - "`bash scripts/validate.sh` exits 0 (BibTeX layer: 12 entries parsed, keys unique, required fields present — DOI is not required) and `bash scripts/ci-smoke.sh _site` prints its PASS line"
    - "One `fix(quick/bib-03)` commit lands both files locally and is NOT pushed — DEPLOY_ENABLED is live, so a push to main auto-deploys production zhangtaolab.org; push timing belongs to the user/orchestrator"
  artifacts:
    - "papers/ref.bib: only the liao2026glycosylase entry changed (3-line region: journal/year/doi → journal/volume/number/pages/year); other 11 entries byte-identical"
    - "_pages/publications.md: exactly 1 line changed (line 22, suffix replacement only); other 88 entry lines untouched"
  key_links:
    - "_pages/publications.md edit → jekyll serve --livereload regen → http://127.0.0.1:4000/publications/ plain-text render → curl occurrence-count assertions (`&ndash;` entity renders as U+2013 in the HTML text node; the markdown source itself must keep the `&ndash;` entity spelling)"
    - "papers/ref.bib → jekyll-scholar (`scholar.source: /papers/`, bibliography ref.bib) → sole consumer is _pages/talks.md `@incollection` queries (permanently empty: 0 @incollection entries among the 12) — the field changes alter no rendered surface"
    - "papers/ref.bib → scripts/validate.rb BibTeX layer (parse + required fields + key uniqueness) + D-08 HEAD-vs-worktree entry-count guard (12 = 12, stays silent)"
    - "_site output of serve → scripts/ci-smoke.sh assertions (sitemap count, retained hrefs) — unaffected by this edit; scib DOI not in the assertion set"
---

<objective>
Land volume/issue info for the third 2026 paper still carrying a DOI placeholder: liao2026glycosylase (Science Bulletin). User verified from the ScienceDirect page (pii S2095927326005487): Volume 71, Issue 12, pages 3032–3045, dated 30 June 2026. Following the pattern of quick tasks 260827-f7z (volume info for bao2026oryza/he2026trna) and 260827-jvt (DOI placeholder removal), this task completes BOTH halves for liao2026glycosylase at once: add volume/issue/pages to `papers/ref.bib` + replace the trailing `DOI:` suffix in `_pages/publications.md` with the proper citation, dropping the now-redundant DOI from both surfaces. zheng2025larch (Horticultural Plant Journal 2025, volume still pending) remains the only placeholder left — untouched.

Purpose: publications page entry #2 and the BibTeX source converge on the complete citation `*Science Bulletin* 2026, 71(12):3032–3045`, retiring the last Science Bulletin DOI placeholder.
Output: 2-line-region edit in papers/ref.bib, 1-line edit in _pages/publications.md, one local commit (not pushed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260827-jvt-remove-doi-placeholders-from-two-entries/260827-jvt-SUMMARY.md

@papers/ref.bib
@_pages/publications.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ref.bib — add volume/number/pages to liao2026glycosylase, drop its doi field</name>
  <files>papers/ref.bib</files>
  <action>Edit ONLY the liao2026glycosylase entry (currently lines 12–18). Replace the three-line region `journal={Science Bulletin},` / `year={2026},` / `doi={10.1016/j.scib.2026.05.046}` with `journal={Science Bulletin},` / `volume={71},` / `number={12},` / `pages={3032--3045},` / `year={2026}` — i.e. insert volume/number/pages between journal and year per the file's field-order convention (see he2026trna, which also has number), delete the doi line entirely, and remove the trailing comma after year={2026} since doi no longer follows it (complete entries end `  year={2026}` with no comma before the closing brace). Page range is the literal two-character hyphen sequence `--` (BibTeX page-range convention, as in `2446--2471`). Touch nothing else in the file — zheng2025larch's doi={10.1016/j.hpj.2025.02.020} line stays.</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && grep -A7 '@article{liao2026glycosylase' papers/ref.bib | grep -c -e 'volume={71}' -e 'number={12}' -e 'pages={3032--3045}' && [ "$(grep -c 'doi={' papers/ref.bib)" -eq 1 ] && [ "$(grep -c 'scib' papers/ref.bib)" -eq 0 ] && [ "$(grep -c '^@' papers/ref.bib)" -eq 12 ]</automated>
  </verify>
  <done>liao2026glycosylase entry carries volume 71, number 12, pages 3032--3045 between journal and year; no doi field remains in the entry; ref.bib as a whole has exactly 1 doi field (zheng2025larch's) and still 12 top-level entries.</done>
</task>

<task type="auto">
  <name>Task 2: publications.md line 22 suffix swap + rendered-page and gate verification</name>
  <files>_pages/publications.md</files>
  <action>Edit ONLY line 22 (entry #2). Replace the trailing `*Science Bulletin* 2026. DOI: https://doi.org/10.1016/j.scib.2026.05.046.` with `*Science Bulletin* 2026, 71(12):3032&ndash;3045.` — keeping the leading author list and the title hyperlink `[Boosting genome editing ... editors](https://doi.org/10.1016/j.scib.2026.05.046)` byte-for-byte. Use the HTML entity `&ndash;` in the markdown source (page convention; kramdown renders it as U+2013). Do NOT touch line 30 (entry #5, zheng2025larch HPJ placeholder) or any other line. Then verify against the running dev server: wait up to ~10s for livereload regen, curl http://127.0.0.1:4000/publications/ and assert (a) `2026, 71(12):3032–3045` with a literal U+2013 appears exactly once, (b) `DOI: https://doi.org/10.1016/j.scib` appears zero times, (c) `https://doi.org/10.1016/j.scib.2026.05.046` (the title href) appears exactly once, (d) `DOI: https` appears exactly once (the HPJ entry). Then run `bash scripts/validate.sh` (exit 0) and `bash scripts/ci-smoke.sh _site` (PASS line). Commit both files as one `fix(quick/bib-03)` commit; do NOT push (DEPLOY_ENABLED auto-deploys on push to main).</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && sleep 8 && PAGE=$(curl -s --max-time 15 http://127.0.0.1:4000/publications/) && echo "$PAGE" | grep -c '2026, 71(12):3032–3045' && [ "$(echo "$PAGE" | grep -c 'DOI: https://doi.org/10.1016/j.scib')" -eq 0 ] && [ "$(echo "$PAGE" | grep -c 'https://doi.org/10.1016/j.scib.2026.05.046')" -eq 1 ] && [ "$(echo "$PAGE" | grep -c 'DOI: https')" -eq 1 ] && bash scripts/validate.sh && bash scripts/ci-smoke.sh _site</automated>
  </verify>
  <done>Entry #2 on the rendered publications page reads `*Science Bulletin* 2026, 71(12):3032–3045.` with the title DOI hyperlink intact; the Science Bulletin trailing DOI suffix is gone from both the markdown source and the rendered page; the HPJ entry #5 placeholder is untouched; validate.sh and ci-smoke.sh both green; one local un-pushed fix(quick/bib-03) commit contains both files.</done>
</task>

</tasks>

<verification>
- `grep -A7 '@article{liao2026glycosylase' papers/ref.bib` shows volume={71}, number={12}, pages={3032--3045}, year={2026}, no doi.
- `grep -c 'doi={' papers/ref.bib` == 1 (zheng2025larch only); `grep -c '^@' papers/ref.bib` == 12.
- `git diff --stat` shows exactly 2 files; `git diff papers/ref.bib` shows a single-entry hunk.
- Rendered page assertions (Task 2 automated block) all pass, including the U+2013 en-dash check and the retained title href.
- `bash scripts/validate.sh` exit 0; `bash scripts/ci-smoke.sh _site` PASS.
- zheng2025larch zero-diff on both surfaces: `git diff papers/ref.bib | grep -c 'hpj'` == 0 and `git diff _pages/publications.md | grep -c 'Horticultural'` == 0.
</verification>

<success_criteria>
- liao2026glycosylase cites `*Science Bulletin* 2026, 71(12):3032–3045` on the publications page and carries volume/number/pages in ref.bib with no doi field.
- The title DOI hyperlink on entry #2 still resolves to https://doi.org/10.1016/j.scib.2026.05.046.
- zheng2025larch (bib + page line #5) byte-identical to HEAD — it remains the only DOI placeholder pending volume info.
- validate.sh (12 bib entries, unique keys) and ci-smoke.sh _site both green; single local commit, not pushed.
</success_criteria>

<output>
Create `.planning/quick/260827-k74-add-volume-issue-info-for-liao2026glycos/260827-k74-SUMMARY.md` when done
</output>
