---
phase: quick-260827-f7z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - papers/ref.bib
autonomous: true
requirements: [BIB-01]   # BIB-01 = this quick task's directive: add published volume/issue info to two 2026 entries in papers/ref.bib (bao2026oryza: volume 17, article 6877; he2026trna: vol 44(8) pp 2446-2471)

estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: low   # second quick task in this repo; estimate-only, no calibration run

must_haves:
  truths:
    - "ref.bib entry `bao2026oryza` carries `volume={17},` and `pages={6877},` inserted between `journal={Nature Communications},` and `year={2026},` (article number 6877 in the pages field per file convention, cf. yang2024rice Rice entry `volume={17}, pages={67}`)"
    - "ref.bib entry `he2026trna` carries `volume={44},`, `number={8},`, `pages={2446--2471},` (double-hyphen page range) between `journal={Trends in Biotechnology},` and `year={2026},`"
    - "Field order in both entries follows the file convention: journal, volume, number, pages, year, doi"
    - "`bash scripts/validate.sh` exits 0 (BibTeX parse + required fields + key uniqueness); top-level entry count stays 12 so the D-08 workspace-count guard stays silent"
    - "`JEKYLL_ENV=production bundle exec jekyll build` exits 0 and `_site/publications/index.html` renders the new fields: `17:6877` (shape proven by existing Rice render `2024, 17:67.`) and `44(8):2446.2471` with the en-dash rendered from BibTeX `--` (shape proven by existing `197(1):kiae557` / `18(2):175–178` renders); both strings are verifiably absent from the current build (planner grep: 0 occurrences)"
    - "`bash scripts/ci-smoke.sh _site` prints its PASS line — its anchor assertion greps exactly this task's DOI `s41467-026-73769-8` on the publications page"
    - "The change is committed locally and NOT pushed — push to main auto-deploys to production zhangtaolab.org; the user/orchestrator owns push timing"
  artifacts:
    - "papers/ref.bib: exactly the two entries updated; no other entry, field, or ordering touched; `grep -c '^@' papers/ref.bib` still 12"
  key_links:
    - "papers/ref.bib volume/number/pages fields → jekyll-scholar APA CSL render `_site/publications/index.html` (pattern `VOLUME(ISSUE):PAGES`, `--` → en-dash U+2013) → scripts/ci-smoke.sh anchor assertion (`s41467-026-73769-8`) → CI deploy.yml Validate content step runs the same scripts/validate.rb on push"
---

<objective>
Two 2026 papers have finalized their volume/issue info; the bibliography still shows journal+year+doi only. Update `papers/ref.bib`:

1. `bao2026oryza` (Nature Communications, DOI 10.1038/s41467-026-73769-8, bib lines 3–9): add `volume={17}` and article number `6877` in the `pages` field — per the file's own convention for article-number journals (cf. `yang2024rice` Rice entry: `volume={17}, pages={67}`, which renders on the live publications page as `2024, 17:67.`). No `number` field.
2. `he2026trna` (Trends in Biotechnology, DOI 10.1016/j.tibtech.2026.02.016, bib lines 19–25): add `volume={44}, number={8}, pages={2446--2471}` (published August 2026, Resource Article, Volume 44 Issue 8 pp 2446–2471).

Field order convention in this file: journal, volume, number, pages, year, doi.

Purpose: citations on zhangtaolab.org/publications/ show complete volume/issue/page info like the rest of the bibliography.
Output: two-entry edit to papers/ref.bib, validator + production-build + smoke proof battery, local commit (no push).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The only file this task touches (read the two target entries once; planner quotes them below)
@papers/ref.bib

# Proof tooling (read once each — validate.sh is a 4-line exec wrapper; ci-smoke.sh anchor ② targets this task's DOI)
@scripts/validate.sh
@scripts/ci-smoke.sh

# Planner-verified current state (commit 1eb4d6f9, clean tree):
# - bao2026oryza fields today: title, author, journal={Nature Communications}, year={2026}, doi={10.1038/s41467-026-73769-8}
# - he2026trna fields today: title, author, journal={Trends in Biotechnology}, year={2026}, doi={10.1016/j.tibtech.2026.02.016}
# - Top-level entries: 12 (`grep -c '^@' papers/ref.bib` = 12; matches Phase 3 validate baseline "12/12")
# - Render shapes proven from the CURRENT built page: no-issue entry → `2024, 17:67.`; with-issue entry → `2025, 197(1):kiae557.` and `18(2):175–178.` (en-dash from `--`)
# - `6877` and `2446` appear 0 times in the current _site/publications/index.html — clean before/after differential
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Insert volume/issue/pages into both bib entries, exact values, pinned field order, parse gate green</name>
  <files>papers/ref.bib</files>
  <precondition>Working tree clean at 1eb4d6f9 with `grep -c '^@' papers/ref.bib` = 12, and `bundle exec` functional (Gemfile.lock present; repo builds locally per Phase 1).</precondition>
  <action>
  Two scoped edits, one per entry. Use 2-space indentation and trailing commas exactly matching neighboring lines.
  (1) In `bao2026oryza` (bib lines 3–9), insert two lines between `journal={Nature Communications},` and `year={2026},`: `volume={17},` then `pages={6877},`. The article number 6877 goes in the pages field because this file's convention for article-number journals puts it there (yang2024rice: `volume={17}, pages={67}`). Do NOT add a `number` field — Nature Communications articles have volume + article number, no issue. Leave title/author/journal/year/doi untouched.
  (2) In `he2026trna` (bib lines 19–25), insert three lines between `journal={Trends in Biotechnology},` and `year={2026},`: `volume={44},` then `number={8},` then `pages={2446--2471},`. Keep the DOUBLE hyphen `--` in the page range — BibTeX renders it as the en-dash in citations, and every ranged entry in this file uses it (e.g. liu2025pdllms `pages={175--178}`). Do NOT write a single hyphen or an en-dash character in the bib source.
  Do NOT touch any other entry, reorder existing fields, or change entry keys — the file must keep exactly 12 top-level entries. After editing, run `bash scripts/validate.sh` (BibTeX parse + required fields + key uniqueness; ~0.2s). It must exit 0 with no output. Note: the D-08 workspace-count guard compares entry counts between HEAD and the working tree — counts stay 12 = 12, so it stays silent by design; do not add or remove entries.
  </action>
  <verify>
    <automated>B=$(sed -n '/@article{bao2026oryza,/,/^}/p' papers/ref.bib) && H=$(sed -n '/@article{he2026trna,/,/^}/p' papers/ref.bib) && printf '%s\n' "$B" | grep -A1 '^  journal={Nature Communications},$' | grep -q '^  volume={17},$' && printf '%s\n' "$B" | grep -A1 '^  volume={17},$' | grep -q '^  pages={6877},$' && printf '%s\n' "$B" | grep -A1 '^  pages={6877},$' | grep -q '^  year={2026},$' && printf '%s\n' "$H" | grep -A1 '^  journal={Trends in Biotechnology},$' | grep -q '^  volume={44},$' && printf '%s\n' "$H" | grep -A1 '^  volume={44},$' | grep -q '^  number={8},$' && printf '%s\n' "$H" | grep -A1 '^  number={8},$' | grep -q '^  pages={2446--2471},$' && printf '%s\n' "$H" | grep -A1 '^  pages={2446--2471},$' | grep -q '^  year={2026},$' && [ "$(grep -c '^@' papers/ref.bib)" = "12" ] && bash scripts/validate.sh && echo BIB-EDIT-OK</automated>
  </verify>
  <done>
  Both entries carry the exact new fields with adjacent-line ordering proven (journal→volume→[number]→pages→year) per file convention; `--` page range intact in he2026trna; entry count still 12; `validate.sh` exits 0; `git diff papers/ref.bib` touches only these two entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render proof battery, smoke assertions, local commit — no push</name>
  <files>_site/ (regenerated build output only — gitignored; no source files modified in this task)</files>
  <action>
  With the Task 1 edit in place, prove the change end-to-end. Every assertion must hold before committing:
  (1) `JEKYLL_ENV=production bundle exec jekyll build` exits 0 — jekyll-scholar re-parses ref.bib, so a malformed entry would break the build here;
  (2) `_site/publications/index.html` contains the rendered article number `17:6877` (expected shape `2026, 17:6877.` following the proven Rice pattern `2024, 17:67.`) and the volume/issue/pages render `44(8):2446–2471` — in the grep below the `.` between 2446 and 2471 is an any-char wildcard standing in for the en-dash U+2013 that the CSL style emits from BibTeX `--`; both strings have 0 occurrences in the pre-edit build, so any hit is this change;
  (3) `bash scripts/ci-smoke.sh _site` prints its PASS line — note assertion ② greps exactly `s41467-026-73769-8` (bao2026oryza's DOI), so this proves the edited entry still renders on the publications page, plus sitemap/feed/vendor tripwires stay green;
  (4) `git diff --stat` shows exactly one changed file: `papers/ref.bib`.
  Then commit ONLY `papers/ref.bib` with the established message convention, e.g. `fix(quick/bib-01): add volume/issue info for bao2026oryza (17:6877) and he2026trna (44(8):2446-2471)`. Do NOT push in any form: DEPLOY_ENABLED is live, so a push to main auto-deploys to production zhangtaolab.org — push timing belongs to the user/orchestrator. Leave the commit local.
  </action>
  <verify>
    <automated>bash scripts/validate.sh && JEKYLL_ENV=production bundle exec jekyll build >/dev/null 2>&1 && grep -q '17:6877' _site/publications/index.html && grep -q '44(8):2446.2471' _site/publications/index.html && bash scripts/ci-smoke.sh _site && [ "$(git diff --name-only HEAD~1 HEAD)" = "papers/ref.bib" ] && git log origin/main..HEAD --oneline | grep -c 'bib-01' </automated>
  </verify>
  <done>
  Battery green: production build exits 0; publications page renders `17:6877` and `44(8):2446–2471` (en-dash); ci-smoke PASS line includes the bao2026oryza DOI anchor; the sole committed change is papers/ref.bib; the `fix(quick/bib-01)` commit exists locally and nothing was pushed to origin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Data-only edit adding volume/number/pages fields to two committed bibliography entries; no user input, no runtime code, no dependency changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | bib field values / file structure | low | accept | Values are committed constants pinned to exact literals by Task 1's adjacent-line grep battery; scripts/validate.rb (same gate CI runs pre-build) enforces BibTeX parse + required fields + key uniqueness; entry-count invariance (12) guards against accidental entry drops |
| T-quick-02 | Denial of Service | production deploy via unintended push | medium | mitigate | Hard constraint: executor never invokes git push; DEPLOY_ENABLED gate is live so push to main auto-deploys to zhangtaolab.org. Commit stays local; Task 2 asserts the commit is un-pushed (origin/main..HEAD) |
</threat_model>

<verification>
1. `bash scripts/validate.sh` exits 0 — the identical validator runs in CI (`deploy.yml` Validate content step, pre-build), so a local pass guarantees the later push will not be blocked.
2. `JEKYLL_ENV=production bundle exec jekyll build` exits 0; `_site/publications/index.html` gains `17:6877` and `44(8):2446–2471` (both verifiably absent pre-edit).
3. `bash scripts/ci-smoke.sh _site` → PASS (sitemap>=10, publications anchor = this task's DOI, feed.xml valid, no vendor leak).
4. Exactly one source file changed (`papers/ref.bib`), exactly two entries touched, 12 top-level entries intact, committed locally, not pushed.
</verification>

<success_criteria>
- `bao2026oryza`: `volume={17}, pages={6877}` in convention order (article number in pages per file convention)
- `he2026trna`: `volume={44}, number={8}, pages={2446--2471}` in convention order (double-hyphen range preserved)
- Validator + production build + smoke battery all green; rendered citations show the new volume/issue info
- Local commit only; push timing left to the user
</success_criteria>

<output>
Create `.planning/quick/260827-f7z-update-volume-issue-info-for-two-publish/260827-f7z-SUMMARY.md` when done
</output>
