---
phase: quick-260827-u7s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _pages/software.md
autonomous: true
requirements: [SW-FIX-01]   # SW-FIX-01 = this quick task's directive: replace the wrong Paper href on the PDLLMs card (line 50 of _pages/software.md, currently the pubmed PMID 39733335 URL — verified via NCBI E-utilities to point at an unrelated paper) with the Molecular Plant fulltext URL used verbatim by publications.md entry #6 (line 32); the CrisprStitch (line 80) and Chorus2 (line 97) Paper links are verified correct and must not change

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low   # estimate-calibration: no calibrated samples; sized from sibling quick task 260827-lwq (same file, same battery)

must_haves:
  truths:
    - "`_pages/software.md` line 50's PDLLMs Paper button href is `https://www.cell.com/molecular-plant/fulltext/S1674-2052(24)00390-3` — byte-identical to the link in `_pages/publications.md` entry #6 (line 32); the old PMID 39733335 URL has 0 occurrences in the file"
    - "The two verified-correct sibling Paper links are untouched: `38146164` (CrisprStitch, line 80) and `33960617` (Chorus2, line 97) each occur exactly once in source and exactly once in the live page HTML"
    - "Live serve page http://127.0.0.1:4000/software/ (jekyll serve --livereload running at plan time, edit-to-render ≤10s per Phase 1 probes): old PMID 0 occurrences, `cell.com/molecular-plant/fulltext` ≥1, both sibling PMIDs exactly 1 each — page assertions scoped to the /software/ HTML only"
    - "Regenerated artifacts: `_site/software/index.html` old PMID 0 + full new URL present (fixed-string match, parentheses literal); `_site/assets/search.json` old PMID 0 (planner-verified it is ALREADY 0 — hrefs are not indexed, only card text like `PDLLMs` ×5 — it must stay 0; do not assert the new URL appears there)"
    - "`bash scripts/validate.sh` exits 0 and `bash scripts/ci-smoke.sh _site` prints its PASS line (software.md is not in the validator file set; the battery proves no collateral damage and pre-clears the CI gate)"
    - "`git diff` is exactly 1 line changed in 1 file (numstat 1 added / 1 deleted on `_pages/software.md`, 137 lines before and after), landed as ONE local `fix(quick/software)` commit, NOT pushed (DEPLOY_ENABLED is live: a push to main auto-deploys production zhangtaolab.org)"
  artifacts:
    - "_pages/software.md: exactly one line modified (line 50, the PDLLMs Paper anchor) — href swapped, anchor markup/classes/icon untouched, no other line differs"
  key_links:
    - "_pages/software.md edit → jekyll serve --livereload regen → http://127.0.0.1:4000/software/ render + `_site/software/index.html` (both must flip old-PMID 1→0 and gain the new URL) + `_site/assets/search.json` (text-only index; already clean, stays clean)"
    - "_pages/publications.md entry #6 (line 32) is the URL authority — the new href matches it verbatim including the unencoded parentheses `S1674-2052(24)00390-3`, which are safe inside a double-quoted HTML attribute and match the publications.md usage already live in production"
    - "No other consumer: planner-verified scoped grep across the repo (excluding _site/.git/.planning) finds the wrong PMID ONLY at _pages/software.md:50 — no _data entry, include, layout, or feed linkage to update"
    - "DEPLOY_ENABLED gate is live → commit stays local-only; push timing belongs to the user/orchestrator (same discipline as quick tasks 8df20a2f / 772d23c5 / 335eb74c)"
---

<objective>
Fix the wrong Paper link on the PDLLMs card in `_pages/software.md`. The card's Paper button (line 50) currently points to `https://pubmed.ncbi.nlm.nih.gov/39733335/` — verified via NCBI E-utilities to resolve to an unrelated paper ("The Role of the Eyelids of the Black-Winged Kite, Elanus caeruleus...", Microscopy Research and Technique), not the PDLLMs article.

Replace the href — and only the href — with `https://www.cell.com/molecular-plant/fulltext/S1674-2052(24)00390-3`, the exact link already used by the PDLLMs entry (#6, line 32) in `_pages/publications.md`, verified as the correct paper (PMID 39659015, DOI 10.1016/j.molp.2024.12.006, "PDLLMs: A group of tailored DNA large language models for analyzing plant genomes", Molecular Plant 18(2):175-178 — the same citation the card displays). The URL contains parentheses `S1674-2052(24)00390-3`; they are safe UNENCODED inside the double-quoted href attribute (plain ASCII, no HTML-special chars) and match the publications.md usage verbatim — do NOT percent-encode them.

Explicitly OUT of scope — do not touch the other two verified-correct Paper links: `https://pubmed.ncbi.nlm.nih.gov/38146164/` (CrisprStitch, line 80) and `https://pubmed.ncbi.nlm.nih.gov/33960617/` (Chorus2, line 97). No other card, heading, style rule, or file changes. Net diff: exactly 1 line changed in 1 file.

Purpose: the Software page's PDLLMs Paper button currently sends visitors to an unrelated eyelid-anatomy paper; it must open the actual Molecular Plant PDLLMs article, same destination as the publications page.
Output: single-line href fix in `_pages/software.md`, live-page + regenerated-artifact + validator + smoke proof battery, one local commit (no push).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The only file this task touches (read it once before editing — Edit requires prior Read)
@_pages/software.md

# Proof tooling (validate.sh is a 4-line exec wrapper around validate.rb; ci-smoke.sh takes the site dir as $1)
@scripts/validate.sh
@scripts/ci-smoke.sh

# Planner-verified current state (2026-08-27, clean tree at 1eb4d6f9):
#
# - _pages/software.md is 137 lines; the three Paper anchors sit at:
#     line 50 — PDLLMs, the WRONG link (pubmed 39733335) — REPLACE ITS HREF ONLY
#     line 80 — CrisprStitch, PMID 38146164 — verified correct, KEEP
#     line 97 — Chorus2, PMID 33960617 — verified correct, KEEP
# - The wrong PMID string occurs EXACTLY ONCE in the whole repo source (software.md:50) and once in
#   rendered _site/software/index.html; the anchor line itself is unique, so a minimal href-only
#   old_string/new_string Edit is unambiguous
# - New URL authority: _pages/publications.md line 32 (entry #6) carries
#   https://www.cell.com/molecular-plant/fulltext/S1674-2052(24)00390-3 with literal parentheses —
#   copy it byte-for-byte; grep -c in BRE treats "(" as literal, but use grep -F for full-URL checks
# - _site/assets/search.json: 39733335 count is ALREADY 0 (index carries card TEXT — PDLLMs ×5 —
#   not hrefs; cell.com count 0), so its assertion is "stays 0", never "gains the new URL"
# - Live serve RUNNING: curl http://127.0.0.1:4000/software/ → 200; planner-measured live counts:
#   39733335 = 1, 38146164 = 1, 33960617 = 1, cell.com/molecular-plant/fulltext = 0 (all flip per plan)
# - validate.sh validates _data YAML + papers/ref.bib — software.md is not in its file set, but it is
#   the standard pre-commit battery; ci-smoke.sh _site asserts sitemap ≥10 / publications DOI anchor /
#   feed.xml validity / vendor tripwire — none reference this link
# - DEPLOY_ENABLED is live (STATE: push to main auto-deploys zhangtaolab.org); prior quick commits
#   (8df20a2f, 772d23c5, 335eb74c) are local-only — this task follows the same no-push discipline
</context>

<tasks>

<task type="tracer">
  <name>End-to-end: swap the PDLLMs Paper href in software.md and prove it live on the serve page</name>
  <files>_pages/software.md</files>
  <precondition>`curl -sf http://127.0.0.1:4000/software/` returns 200 (serve running at plan time). If it does not, run `bundle exec jekyll build` and perform the same occurrence-count assertions against `_site/software/index.html` instead of the live URL.</precondition>
  <action>
  <!-- planner-discipline-allow: 39733335 -->
  Read `_pages/software.md` first (Edit requires prior Read). Make exactly ONE Edit with the minimal unique replacement: old_string `https://pubmed.ncbi.nlm.nih.gov/39733335/` → new_string `https://www.cell.com/molecular-plant/fulltext/S1674-2052(24)00390-3`. The old string occurs exactly once (line 50, planner-verified) so the Edit is unambiguous; this touches ONLY the href inside the Paper anchor — leave the surrounding `<a ... class="btn-pill btn-paper" ...>`, the `<i class="fa-solid fa-file-lines"></i>` icon, the button label, and every other line byte-identical.
  Transcribe the new URL exactly as written above, parentheses included — do NOT percent-encode `(` `)` into %28/%29 (they are plain ASCII, safe unencoded inside the double-quoted href attribute, and the byte-identical form is already live on /publications/ entry #6). Do NOT "fix" or normalize anything else in the file: the CrisprStitch Paper link (line 80) and the Chorus2 Paper link (line 97) are verified correct and MUST remain untouched, as must the card text, the citation paragraph (which already cites the right paper), the `<style>` block, and the sibling cards.
  Then let livereload regenerate (edit-to-render observed ≤10s; the verify below polls up to 30s) and prove the swap on the live /software/ HTML BEFORE moving on — page-scope the counts to that HTML response only.
  </action>
  <verify>
    <automated>for i in $(seq 1 15); do curl -sf http://127.0.0.1:4000/software/ | grep -q '39733335' || break; sleep 2; done; H=$(curl -sf http://127.0.0.1:4000/software/) && printf '%s' "$H" | grep -q 'PDLLMs' && [ "$(printf '%s' "$H" | grep -o '39733335' | wc -l | tr -d ' ')" = "0" ] && [ "$(printf '%s' "$H" | grep -o 'cell.com/molecular-plant/fulltext' | wc -l | tr -d ' ')" -ge 1 ] && printf '%s' "$H" | grep -qF 'S1674-2052(24)00390-3' && [ "$(printf '%s' "$H" | grep -o '38146164' | wc -l | tr -d ' ')" = "1" ] && [ "$(printf '%s' "$H" | grep -o '33960617' | wc -l | tr -d ' ')" = "1" ] && [ "$(grep -c '39733335' _pages/software.md)" = "0" ] && [ "$(grep -c 'cell.com/molecular-plant/fulltext' _pages/software.md)" = "1" ] && [ "$(grep -c '38146164' _pages/software.md)" = "1" ] && [ "$(grep -c '33960617' _pages/software.md)" = "1" ] && [ "$(wc -l < _pages/software.md | tr -d ' ')" = "137" ] && [ "$(git diff --numstat -- _pages/software.md | cut -f1)" = "1" ] && [ "$(git diff --numstat -- _pages/software.md | cut -f2)" = "1" ] && [ "$(git diff --name-only | tr '\n' ' ')" = "_pages/software.md " ] && echo LIVE-LINK-OK</automated>
  </verify>
  <done>
  Live page battery green (positive guard PDLLMs proves the page rendered, not an empty fetch): old PMID 0 occurrences, `cell.com/molecular-plant/fulltext` ≥1 with the full fixed-string URL `S1674-2052(24)00390-3` present, and both sibling PMIDs exactly 1 each. Source: old PMID 0, new URL exactly 1, both sibling PMIDs exactly 1, file still 137 lines. `git diff --numstat` shows exactly 1 added / 1 deleted and `git diff --name-only` lists only `_pages/software.md` — a one-line, one-file change.
  </done>
</task>

<task type="auto">
  <name>Validator + smoke battery, regenerated-artifact check, single local commit — no push</name>
  <files>_pages/software.md</files>
  <action>
  The edit landed in Task 1; this task proves the full battery and commits. If you fell back to `jekyll build` in Task 1 (serve down), the build output is already current; otherwise the serve has regenerated `_site` after the edit — for `_site/software/index.html` allow the regen window (poll loop below, up to 30s; planner-measured pre-edit count is 1 and must flip to 0).
  (1) `bash scripts/validate.sh` must exit 0 — YAML/BibTeX layers; software.md is not in the validator's file set, so this proves no collateral damage to the validated surfaces and pre-clears the CI Validate content step that runs the same validator.
  (2) `bash scripts/ci-smoke.sh _site` must print its PASS line — sitemap ≥10, publications DOI anchor, feed.xml validity, vendor tripwire; none reference the software page's link.
  (3) Regenerated artifacts: `_site/software/index.html` no longer contains the removed pubmed PMID and does contain the new Molecular Plant URL (fixed-string, parentheses literal). `_site/assets/search.json` must contain the removed PMID 0 times — planner-verified it is ALREADY 0 because the index carries card text (PDLLMs ×5), not hrefs, so assert absence only and expect no new-URL match there.
  (4) Commit exactly one file with the established message convention: `fix(quick/software): correct PDLLMs paper link (wrong pubmed PMID → Molecular Plant fulltext)`. Do NOT push in any form — DEPLOY_ENABLED is live, so a push to main auto-deploys production zhangtaolab.org; push timing belongs to the user/orchestrator.
  </action>
  <verify>
    <automated>for i in $(seq 1 15); do grep -q '39733335' _site/software/index.html || break; sleep 2; done; bash scripts/validate.sh >/dev/null && bash scripts/ci-smoke.sh _site | grep -q '^PASS' && [ "$(grep -c '39733335' _site/software/index.html)" = "0" ] && grep -qF 'S1674-2052(24)00390-3' _site/software/index.html && [ "$(grep -c '39733335' _site/assets/search.json)" = "0" ] && grep -q 'PDLLMs' _site/assets/search.json && [ "$(git show --numstat --format= HEAD | awk '{print $1}')" = "1" ] && [ "$(git show --numstat --format= HEAD | awk '{print $2}')" = "1" ] && [ "$(git diff --name-only HEAD~1 HEAD | tr '\n' ' ')" = "_pages/software.md " ] && git log origin/main..HEAD --oneline | grep -qi 'pdllm' && echo SOFTWARE-LINK-FIX-OK</automated>
  </verify>
  <done>
  validate.sh exit 0; ci-smoke.sh _site prints PASS; regenerated `_site/software/index.html` is free of the removed PMID and carries the full new URL; `_site/assets/search.json` stays free of it (with PDLLMs card text still indexed as the positive guard). The HEAD commit is a one-line change (1 added / 1 deleted), touches exactly `_pages/software.md`, carries the `fix(quick/software)` message, and sits local-only in the origin/main..HEAD ahead-set — nothing pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Data-only single-href swap in one static markdown page; no user input, no runtime code, no dependency changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | collateral edits to the sibling Paper anchors (CrisprStitch line 80 / Chorus2 line 97), the anchor markup, or encoding drift in the new URL | low | mitigate | Minimal unique href-only Edit (old string occurs exactly once, planner-verified); URL transcribed verbatim with literal parentheses, never percent-encoded; verify gates: old PMID 0 / new URL exactly 1 in source, both sibling PMIDs exactly 1 in source AND live HTML, file still 137 lines, numstat exactly 1/1 on the single file |
| T-quick-02 | Tampering | wrong link persists in a rendered surface (live page, _site/software/index.html, search.json) after the source fix | low | mitigate | Poll-loop verify on the live /software/ HTML and on _site/software/index.html (each flips 1→0, ≤30s window per Phase 1 probes); search.json asserted free of the old PMID (already 0 — hrefs unindexed — stays 0) |
| T-quick-03 | Denial of Service | production deploy via unintended push | medium | mitigate | Executor never invokes git push; DEPLOY_ENABLED gate is live so push to main auto-deploys zhangtaolab.org. Commit stays local; Task 2 verify asserts the commit sits in the origin/main..HEAD ahead-set |
</threat_model>

<verification>
1. Live serve page battery (Task 1): old PMID 0 occurrences, `cell.com/molecular-plant/fulltext` ≥1 plus full fixed-string URL present, both sibling PMIDs exactly 1 each, PDLLMs present as the positive render guard — counts scoped to the /software/ HTML response only.
2. Source shape: old PMID 0, new URL exactly 1, both sibling PMIDs exactly 1, file exactly 137 lines, `git diff --numstat` 1/1 on `_pages/software.md` alone.
3. `bash scripts/validate.sh` exits 0 and `bash scripts/ci-smoke.sh _site` prints PASS — the same validator runs in CI (deploy.yml Validate content step), so a local pass guarantees the later push will not be blocked.
4. Regenerated artifacts clean: `_site/software/index.html` drops the old PMID and gains the full new URL; `_site/assets/search.json` stays free of the old PMID while still indexing the PDLLMs card text.
5. Exactly one commit, one line changed in one file, local only, nothing pushed.
</verification>

<success_criteria>
- The PDLLMs Paper button on /software/ opens the Molecular Plant PDLLMs article (URL byte-identical to publications.md entry #6, parentheses unencoded) instead of the unrelated PMID 39733335 paper
- The CrisprStitch and Chorus2 Paper links are byte-identical to before, and no other line of `_pages/software.md` changed (numstat 1/1, 137 lines)
- Live-page battery + validate.sh + ci-smoke.sh all green; one local `fix(quick/software)` commit, not pushed
</success_criteria>

<output>
Create `.planning/quick/260827-u7s-fix-wrong-paper-link-on-pdllms-card-in-p/260827-u7s-SUMMARY.md` when done
</output>
