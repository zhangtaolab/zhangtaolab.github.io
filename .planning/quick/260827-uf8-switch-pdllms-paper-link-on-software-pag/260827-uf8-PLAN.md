---
phase: quick-260827-uf8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _pages/software.md
autonomous: true
requirements: [SW-FIX-02]   # SW-FIX-02 = this quick task's directive: switch the PDLLMs card Paper href in _pages/software.md (line 50) from the cell.com fulltext URL (landed by quick task 260827-u7s, commit a6d140fb) to the DOI link https://doi.org/10.1016/j.molp.2024.12.006 (verified: NCBI E-utilities PMID 39659015 + Crossref, "PDLLMs: A group of tailored DNA large language models for analyzing plant genomes", Molecular Plant 18(2):175-178 — the same citation the card displays); CrisprStitch (line 80, PMID 38146164) and Chorus2 (line 97, PMID 33960617) Paper links and _pages/publications.md entry #6 (line 32, keeps its cell.com title link — user scoped this task to the software page only) must NOT change

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low   # estimate-calibration: no calibrated samples; sized from sibling quick task 260827-u7s (same file, same battery, one-line href swap)

must_haves:
  truths:
    - "`_pages/software.md` line 50's PDLLMs Paper button href is `https://doi.org/10.1016/j.molp.2024.12.006` — transcribed verbatim, no trailing slash added or removed; the old cell.com fulltext URL has 0 occurrences in the file (planner-verified pre-edit count: exactly 1, line 50)"
    - "The two verified-correct sibling Paper links are untouched: `38146164` (CrisprStitch, line 80) and `33960617` (Chorus2, line 97) each occur exactly once in source and exactly once in the live /software/ page HTML"
    - "`_pages/publications.md` is byte-identical to before: it still contains `cell.com/molecular-plant` exactly 2 times (entry #6 line 32 `S1674-2052(24)00390-3` + entry #51 line 134 `S1674-2052(19)30124-8`) — the user scoped this task to the software page only; the publications entry keeps its cell.com title link"
    - "Live serve page http://127.0.0.1:4000/software/ (jekyll serve --livereload running at plan time, 200 verified; edit-to-render ≤10s per Phase 1 probes, verify polls ≤30s): `cell.com/molecular-plant` count 0 and the full DOI URL count exactly 1, both scoped to the /software/ HTML response only — a repo-wide grep is WRONG here because publications.md and the news page legitimately keep cell.com links"
    - "Regenerated artifact `_site/software/index.html` flips the same way (planner-verified pre-edit cell.com count: 1, line 221 → 0; DOI 0 → 1); `_site/assets/search.json` stays free of `cell.com` (planner-verified already 0 — the index carries card text like `PDLLMs` ×5, not hrefs; do not assert the DOI appears there)"
    - "`bash scripts/validate.sh` exits 0 and `bash scripts/ci-smoke.sh _site` prints its PASS line (software.md is not in the validator file set; the battery proves no collateral damage and pre-clears the CI Validate content step that runs the same validator)"
    - "`git diff` is exactly 1 line changed in 1 file (numstat 1 added / 1 deleted on `_pages/software.md`, 137 lines before and after), landed as ONE local `fix(quick/software)` commit, NOT pushed (DEPLOY_ENABLED is live: a push to main auto-deploys production zhangtaolab.org)"
  artifacts:
    - "_pages/software.md: exactly one line modified (line 50, the PDLLMs Paper anchor) — href swapped, anchor markup/classes/icon untouched, no other line differs"
  key_links:
    - "_pages/software.md edit → jekyll serve --livereload regen → http://127.0.0.1:4000/software/ render + `_site/software/index.html` (both must flip cell.com 1→0 and DOI 0→1) + `_site/assets/search.json` (text-only index; already clean, stays clean)"
    - "DOI authority: `https://doi.org/10.1016/j.molp.2024.12.006` — planner-verified against the task directive (NCBI PMID 39659015 + Crossref record for the exact PDLLMs citation the card displays: Mol Plant 2025;18(2):175-178); no parentheses in this URL, so the u7s percent-encoding concern does not apply"
    - "No other consumer: planner-verified scoped grep — within `_pages/software.md` the cell.com fulltext URL occurs ONLY at line 50; the other in-repo occurrences (`_pages/publications.md` lines 32/134, news page via `_data/news.yml`) are OUT OF SCOPE and must remain"
    - "DEPLOY_ENABLED gate is live → commit stays local-only; push timing belongs to the user/orchestrator (same discipline as quick tasks 8df20a2f / 772d23c5 / 335eb74c / a6d140fb)"
---

<objective>
Switch the PDLLMs card Paper link on the Software page from the publisher fulltext URL to the canonical DOI link. Quick task 260827-u7s (commit a6d140fb) fixed the card's wrong PMID by pointing line 50 of `_pages/software.md` at the cell.com fulltext URL; the user now wants the DOI form `https://doi.org/10.1016/j.molp.2024.12.006` instead.

The DOI is verified (NCBI E-utilities PMID 39659015 record + Crossref: "PDLLMs: A group of tailored DNA large language models for analyzing plant genomes", Molecular Plant 18(2):175-178 — the same citation the card already displays). The cell.com fulltext URL is currently unique in software.md source (line 50, planner-verified).

Explicitly OUT of scope — do not touch: the CrisprStitch Paper link (`https://pubmed.ncbi.nlm.nih.gov/38146164/`, line 80), the Chorus2 Paper link (`https://pubmed.ncbi.nlm.nih.gov/33960617/`, line 97), and `_pages/publications.md` entry #6 (line 32 — its cell.com title link stays; the user scoped this task to the software page only). The news page's cell.com link is likewise not in scope. No other card, heading, style rule, or file changes. Net diff: exactly 1 line changed in 1 file.

Purpose: the Software page's PDLLMs Paper button should open the canonical, resolver-stable DOI destination rather than a publisher-specific fulltext URL.
Output: single-line href swap in `_pages/software.md`, live-page + regenerated-artifact + validator + smoke proof battery, one local commit (no push).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The only file this task touches (read it once before editing — Edit requires prior Read)
@_pages/software.md

# Proof tooling (validate.sh is a 4-line exec wrapper around validate.rb; ci-smoke.sh takes the site dir as $1)
@scripts/validate.sh
@scripts/ci-smoke.sh
</context>

<tasks>

<task type="tracer">
  <name>End-to-end: swap the PDLLMs Paper href to the DOI in software.md and prove it live on the serve page</name>
  <files>_pages/software.md</files>
  <precondition>`curl -sf http://127.0.0.1:4000/software/` returns 200 (serve running at plan time, planner-verified). If it does not, run `bundle exec jekyll build` and perform the same occurrence-count assertions against `_site/software/index.html` instead of the live URL.</precondition>
  <action>
  <!-- planner-discipline-allow: cell.com/molecular-plant -->
  Read `_pages/software.md` first (Edit requires prior Read). Make exactly ONE Edit with the minimal unique replacement: old_string `https://www.cell.com/molecular-plant/fulltext/S1674-2052(24)00390-3` → new_string `https://doi.org/10.1016/j.molp.2024.12.006`. The old string occurs exactly once (line 50, planner-verified) so the Edit is unambiguous; this touches ONLY the href inside the Paper anchor — leave the surrounding `<a ... class="btn-pill btn-paper" ...>`, the `<i class="fa-solid fa-file-lines"></i>` icon, the button label, and every other line byte-identical.
  Transcribe the new DOI URL exactly as written above — no trailing slash, no `http://`, no percent-encoding needed (plain ASCII, no parentheses). Do NOT "fix" or normalize anything else in the file: the CrisprStitch Paper link (line 80, PubMed 38146164) and the Chorus2 Paper link (line 97, PubMed 33960617) are verified correct and MUST remain untouched, as must the card text, the citation paragraph (which already cites the right paper), the `<style>` block, and the sibling cards. Equally out of scope: `_pages/publications.md` entry #6 keeps its cell.com title link (user scoped this task to the software page only) — do not open that file for editing.
  Then let livereload regenerate (edit-to-render observed ≤10s; the verify below polls up to 30s) and prove the swap on the live /software/ HTML BEFORE moving on — page-scope every count to that HTML response only, never a repo-wide grep (publications.md and the news page legitimately keep cell.com links).
  </action>
  <verify>
    <automated>for i in $(seq 1 15); do curl -sf http://127.0.0.1:4000/software/ | grep -qF 'https://doi.org/10.1016/j.molp.2024.12.006' && break; sleep 2; done; H=$(curl -sf http://127.0.0.1:4000/software/) && printf '%s' "$H" | grep -q 'PDLLMs' && [ "$(printf '%s' "$H" | grep -oF 'https://doi.org/10.1016/j.molp.2024.12.006' | wc -l | tr -d ' ')" = "1" ] && [ "$(printf '%s' "$H" | grep -o 'cell\.com/molecular-plant' | wc -l | tr -d ' ')" = "0" ] && [ "$(printf '%s' "$H" | grep -o '38146164' | wc -l | tr -d ' ')" = "1" ] && [ "$(printf '%s' "$H" | grep -o '33960617' | wc -l | tr -d ' ')" = "1" ] && [ "$(grep -c 'cell\.com/molecular-plant' _pages/software.md)" = "0" ] && [ "$(grep -cF 'https://doi.org/10.1016/j.molp.2024.12.006' _pages/software.md)" = "1" ] && [ "$(grep -c '38146164' _pages/software.md)" = "1" ] && [ "$(grep -c '33960617' _pages/software.md)" = "1" ] && [ "$(grep -c 'cell\.com/molecular-plant' _pages/publications.md)" = "2" ] && [ "$(wc -l < _pages/software.md | tr -d ' ')" = "137" ] && [ "$(git diff --numstat -- _pages/software.md | cut -f1)" = "1" ] && [ "$(git diff --numstat -- _pages/software.md | cut -f2)" = "1" ] && [ "$(git diff --name-only | tr '\n' ' ')" = "_pages/software.md " ] && echo LIVE-DOI-OK</automated>
  </verify>
  <done>
  Live page battery green (positive guard PDLLMs proves the page rendered, not an empty fetch): the full DOI URL exactly 1, the old cell.com fulltext URL 0 occurrences, and both sibling PMIDs exactly 1 each — all counts scoped to the /software/ HTML response. Source: old URL 0, DOI exactly 1, both sibling PMIDs exactly 1, publications.md still carries its 2 cell.com molecular-plant links, file still 137 lines. `git diff --numstat` shows exactly 1 added / 1 deleted and `git diff --name-only` lists only `_pages/software.md` — a one-line, one-file change.
  </done>
</task>

<task type="auto">
  <name>Validator + smoke battery, regenerated-artifact check, single local commit — no push</name>
  <files>_pages/software.md</files>
  <action>
  The edit landed in Task 1; this task proves the full battery and commits. If you fell back to `jekyll build` in Task 1 (serve down), the build output is already current; otherwise the serve has regenerated `_site` after the edit — for `_site/software/index.html` allow the regen window (poll loop below, up to 30s; planner-measured pre-edit cell.com count is 1 at line 221 and must flip to 0).
  (1) `bash scripts/validate.sh` must exit 0 — YAML/BibTeX layers; software.md is not in the validator's file set, so this proves no collateral damage to the validated surfaces and pre-clears the CI Validate content step that runs the same validator.
  (2) `bash scripts/ci-smoke.sh _site` must print its PASS line — sitemap ≥10, publications DOI anchor, feed.xml validity, vendor tripwire; none reference the software page's link.
  (3) Regenerated artifacts: `_site/software/index.html` no longer contains the removed cell.com fulltext URL (count 0) and does contain the DOI URL exactly once. `_site/assets/search.json` must contain `cell.com` 0 times — planner-verified it is ALREADY 0 because the index carries card text (PDLLMs ×5), not hrefs, so assert absence only and expect no DOI match there.
  (4) Commit exactly one file with the established message convention: `fix(quick/software): switch PDLLMs card Paper link to DOI (10.1016/j.molp.2024.12.006)`. Do NOT push in any form — DEPLOY_ENABLED is live, so a push to main auto-deploys production zhangtaolab.org; push timing belongs to the user/orchestrator.
  </action>
  <verify>
    <automated>for i in $(seq 1 15); do grep -qF 'https://doi.org/10.1016/j.molp.2024.12.006' _site/software/index.html && break; sleep 2; done; bash scripts/validate.sh >/dev/null && bash scripts/ci-smoke.sh _site | grep -q '^PASS' && [ "$(grep -c 'cell\.com/molecular-plant' _site/software/index.html)" = "0" ] && [ "$(grep -cF 'https://doi.org/10.1016/j.molp.2024.12.006' _site/software/index.html)" = "1" ] && [ "$(grep -c 'cell\.com' _site/assets/search.json)" = "0" ] && grep -q 'PDLLMs' _site/assets/search.json && [ "$(git show --numstat --format= HEAD | awk '{print $1}')" = "1" ] && [ "$(git show --numstat --format= HEAD | awk '{print $2}')" = "1" ] && [ "$(git diff --name-only HEAD~1 HEAD | tr '\n' ' ')" = "_pages/software.md " ] && git log origin/main..HEAD --oneline | grep -qi 'pdllm' && echo SOFTWARE-DOI-SWAP-OK</automated>
  </verify>
  <done>
  validate.sh exit 0; ci-smoke.sh _site prints PASS; regenerated `_site/software/index.html` is free of the cell.com fulltext URL and carries the DOI exactly once; `_site/assets/search.json` stays free of cell.com (with PDLLMs card text still indexed as the positive guard). The HEAD commit is a one-line change (1 added / 1 deleted), touches exactly `_pages/software.md`, carries the `fix(quick/software)` message, and sits local-only in the origin/main..HEAD ahead-set — nothing pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Data-only single-href swap in one static markdown page; no user input, no runtime code, no dependency changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | collateral edits to the sibling Paper anchors (CrisprStitch line 80 / Chorus2 line 97), the publications.md entry #6 cell.com link, the anchor markup, or drift in the new DOI URL (trailing slash / http downgrade) | low | mitigate | Minimal unique href-only Edit (old string occurs exactly once, planner-verified); DOI transcribed verbatim from the verified directive; verify gates: old URL 0 / DOI exactly 1 in source, both sibling PMIDs exactly 1 in source AND live HTML, publications.md cell.com count still exactly 2, file still 137 lines, numstat exactly 1/1 on the single file |
| T-quick-02 | Tampering | old link persists in a rendered surface (live page, _site/software/index.html) after the source fix, or counts taken repo-wide and misread as failures | low | mitigate | Poll-loop verify on the live /software/ HTML and on _site/software/index.html (each flips cell.com 1→0 and DOI 0→1, ≤30s window per Phase 1 probes); every count explicitly scoped to the /software/ HTML or the single built file — never a repo-wide grep, because publications.md and the news page legitimately keep cell.com links |
| T-quick-03 | Denial of Service | production deploy via unintended push | medium | mitigate | Executor never invokes git push; DEPLOY_ENABLED gate is live so push to main auto-deploys zhangtaolab.org. Commit stays local; Task 2 verify asserts the commit sits in the origin/main..HEAD ahead-set |
</threat_model>

<verification>
1. Live serve page battery (Task 1): the full DOI URL exactly 1, the old cell.com fulltext URL 0 occurrences, both sibling PMIDs exactly 1 each, PDLLMs present as the positive render guard — counts scoped to the /software/ HTML response only.
2. Source shape: old URL 0, DOI exactly 1, both sibling PMIDs exactly 1, publications.md still exactly 2 cell.com molecular-plant links, file exactly 137 lines, `git diff --numstat` 1/1 on `_pages/software.md` alone.
3. `bash scripts/validate.sh` exits 0 and `bash scripts/ci-smoke.sh _site` prints PASS — the same validator runs in CI (deploy.yml Validate content step), so a local pass guarantees the later push will not be blocked.
4. Regenerated artifacts clean: `_site/software/index.html` drops the cell.com fulltext URL and gains the DOI exactly once; `_site/assets/search.json` stays free of cell.com while still indexing the PDLLMs card text.
5. Exactly one commit, one line changed in one file, local only, nothing pushed.
</verification>

<success_criteria>
- The PDLLMs Paper button on /software/ opens `https://doi.org/10.1016/j.molp.2024.12.006` (canonical DOI, verified against PMID 39659015 / Crossref for the exact citation the card displays) instead of the cell.com fulltext URL
- The CrisprStitch and Chorus2 Paper links are byte-identical to before; `_pages/publications.md` entry #6 keeps its cell.com title link (still 2 cell.com molecular-plant links in that file); no other line of `_pages/software.md` changed (numstat 1/1, 137 lines)
- Live-page battery + validate.sh + ci-smoke.sh all green; one local `fix(quick/software)` commit, not pushed
</success_criteria>

<output>
Create `.planning/quick/260827-uf8-switch-pdllms-paper-link-on-software-pag/260827-uf8-SUMMARY.md` when done
</output>
