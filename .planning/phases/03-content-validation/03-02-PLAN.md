---
phase: 03-content-validation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/validate.rb
  - .github/workflows/deploy.yml
autonomous: true
requirements: [CONTENT-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An uncommitted change to papers/ref.bib that alters the entry count triggers the Chinese reminder naming publications.md while the script still exits 0 — the reminder never affects the exit code (D-08, CONTENT-01-f)"
    - "deploy.yml build job runs a 'Validate content' step (bundle exec ruby scripts/validate.rb) positioned after Setup Pages and before Build with Jekyll, and a green push to main produces a successful run whose log shows the step executing (D-04, ROADMAP sc1/sc2 CI 半边 + sc4)"
    - "A deliberately broken content commit pushed to main turns the run red at the Validate content step (pre-build interception), creates no new Pages deployment, and the live site keeps serving the previous version (HTTP 200 throughout); the subsequent revert push returns the pipeline to green with a new deployment (CONTENT-01-g, ROADMAP sc3/sc4)"
    - "In CI the D-08 reminder is naturally inert (worktree equals HEAD at checkout fetch-depth 1, counts equal) with zero CI-specific branching in the script — no HEAD^/origin/main usage (RESEARCH Pitfall 5)"
  artifacts:
    - path: ".github/workflows/deploy.yml（修改）"
      provides: "build job 新增 'Validate content' 步骤（Setup Pages 之后、Build with Jekyll 之前；字面量 run 命令，无内联模板表达式）；既有 6 步原样不动"
    - path: "scripts/validate.rb（追加重醒层）"
      provides: "D-08 提醒块：git show HEAD:papers/ref.bib 与工作区条目计数对比，不等输出中文提醒（含 publications.md 字样），退出码零影响；guard 失败静默跳过"
  key_links:
    - from: "deploy.yml 'Validate content' 步骤"
      to: "bundle exec ruby scripts/validate.rb"
      via: "字面量命令（Setup Ruby 的 bundler-cache 已装 bibtex-ruby，无 apt 安装、零新依赖 D-06；与 Smoke assertions 步骤同风格——run 块无 ${{ }} 内联表达式，T-03-02）"
      pattern: "run: bundle exec ruby scripts/validate.rb"
    - from: "Validate content 步骤 exit 1"
      to: "无 upload-pages-artifact → deploy job（needs: build）不跑 → 线上保持旧版"
      via: "Phase 2 既有拦截语义（断言先于 Upload）：步骤红 → job 红 → 无工件 → 不部署；Task 3 以真实红提交 + deployment id 不变端到端证明"
      pattern: "needs: build"
  prohibitions:
    - statement: "MUST NOT replace, regenerate, or auto-sync the handwritten publications.md list from papers/ref.bib (bib holds 12 entries vs the handwritten 89 — replacement silently drops ~77 entries; Phase 1 prohibition 固化于 PROJECT.md Key Decisions). D-08 is advisory text output only, direction ref.bib→reminder; the reverse direction and any hard bib↔markdown correspondence check are deferred (CONTEXT Deferred Ideas)"
      status: resolved
      verification: judgment
---

<objective>
## Phase Goal（user story，源自 ROADMAP Goal）

**As a** 实验室维护者, **I want to** 在发布前通过本地命令与 CI 构建前独立步骤（同一脚本两处复用）校验内容语法，且 ref.bib 条目变化时收到 publications.md 同步提醒, **so that** 错误内容永远到不了线上，页面不再悄悄缺内容。

## Plan 02 — 双入口收口：D-08 提醒层 + CI 构建前验证步骤 + 红路径端到端证明

Purpose: 补齐 CONTENT-01 的另一半。per D-04，CI 在 deploy.yml build job 的 Build with Jekyll 之前加独立验证步骤（错误不被构建日志淹没；验证红 → 无工件 → 不部署，线上保持旧版——Phase 2 既有语义）；per D-08，ref.bib 条目数变化时输出中文提醒但绝不阻断。最后用一次真实的「故意红提交」在 CI 上端到端证明拦截语义成立（CONTENT-01-g）。

Output: validate.rb 提醒层、deploy.yml 验证步骤、红/绿双路径 CI 实证记录（run id + deployment id 入 SUMMARY）。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-validation/03-CONTEXT.md
@.planning/phases/03-content-validation/03-RESEARCH.md
@.planning/phases/03-content-validation/03-PATTERNS.md
@.planning/phases/03-content-validation/03-01-SUMMARY.md
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-08 提醒层 — ref.bib 条目数变化提醒（警告不阻断）</name>
  <read_first>03-RESEARCH.md（Pattern 4 git 语义与已验证示例第 230-235 行、Pitfall 5 fetch-depth 1 陷阱、Assumptions Log A1 runner git 可用性）、03-CONTEXT.md（D-08 决策原文与提醒文案方向）、03-01-SUMMARY.md（validate.rb 现有结构与输出契约）、papers/ref.bib</read_first>
  <files>scripts/validate.rb</files>
  <precondition>Plan 01 完成：scripts/validate.sh 基线全绿（errors 数组与汇总输出已就位）；工作区干净（本任务 probe 需未提交改动语义 + 恢复证洁）</precondition>
  <action>在 validate.rb 的最终 PASS/FAIL 汇总输出之前加入 D-08 提醒层（per D-08：警告不阻断）。① guard：git rev-parse --git-dir 可用且 git cat-file -e HEAD:papers/ref.bib 成功，任一失败静默跳过提醒（非 git 目录 / 无 HEAD 历史时的降级，A1 兜底——提醒属增强，guard 失败不算错误）。② 对比：git show HEAD:papers/ref.bib 与工作区 papers/ref.bib 两侧各做条目计数（grep -cE '^@[a-zA-Z]+\{'——RESEARCH Pattern 4 同款原始文本计数；不依赖 BibTeX 解析结果，bib 语法坏时计数仍可执行）。③ 不等则输出一行中文提醒：「提醒：ref.bib 条目数 <旧值> → <新值> 已变化；publications.md 为手写列表，请确认已同步新增/删除条目」（D-08 锁定文案方向，含 publications.md 字样）。④ 本层对退出码零影响：不写入 errors 数组，条目数变化时 exit 仍为 0（若无其他错误）——D-08「不误伤正常提交」。⑤ 实现内嵌 validate.rb（Ruby 反引号执行 git show，取 stdout 计数），不放在 validate.sh 侧——CI 步骤直调 validate.rb，包装层逻辑会造成两入口行为分叉。⑥ CI 天然无操作：checkout@v4 fetch-depth 1 下工作区 = HEAD、两计数相等、提醒自动不触发——不写任何 CI 环境变量特判；禁止依赖 HEAD^ / origin/main 的对比写法（Pitfall 5：depth 1 下不存在）。提醒是纯文本输出，不触碰 publications.md 本身（prohibition P-03-3：不替换/再生成/自动同步手写列表；反向提醒属 Deferred，不做）。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && scripts/validate.sh > /tmp/v3-p2-base.txt 2>&1; RC=$?; [ "$RC" = "0" ] && [ "$(wc -l < /tmp/v3-p2-base.txt | tr -d ' ')" = "1" ] && cp papers/ref.bib /tmp/v3-bibp2.bak && printf '@article{probenew, title={New entry}, author={N}, year={2026}}\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3-p2-rem.txt 2>&1; R1=$?; cp /tmp/v3-bibp2.bak papers/ref.bib; [ "$R1" = "0" ] && grep -q '提醒' /tmp/v3-p2-rem.txt && grep -q 'publications.md' /tmp/v3-p2-rem.txt && grep -Eq '条目数 12 → 13' /tmp/v3-p2-rem.txt && scripts/validate.sh >/dev/null 2>&1 && [ -z "$(git status --porcelain _data papers)" ] && echo TASK1-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Clean-tree baseline run: exit 0 and output is exactly one line (the PASS summary — no reminder when the working tree matches HEAD)
    - Uncommitted valid entry appended to ref.bib (12 → 13): run output contains the reminder (提醒 + publications.md + 「条目数 12 → 13」) AND exit code is still 0 — warning never blocks (D-08, CONTENT-01-f)
    - Probe restored; git status --porcelain over _data/ and papers/ empty; post-restore run is single-line exit 0 again
    - Script contains no HEAD^ or origin/main comparison and no CI environment-variable branching (grep -able absence in review; behavior guaranteed by the HEAD-vs-worktree design)
  </acceptance_criteria>
  <done>D-08 落地：ref.bib 条目数变化触发中文提醒且 exit 0；干净树下零提醒；CI 端因工作区=HEAD 天然静默</done>
</task>

<task type="auto">
  <name>Task 2: CI 集成 — deploy.yml 插入 Validate content 步骤（Setup Pages 后、Build with Jekyll 前）并推送实证绿跑</name>
  <read_first>03-RESEARCH.md（Pattern 5 CI 插入模式与 deploy.yml:26-45 步骤序列原文、Open Question 4 推荐位置）、03-PATTERNS.md（deploy.yml 插入模式与既有风格约束）、.github/workflows/deploy.yml（现状全文——插入点与既有 6 步）、03-CONTEXT.md（D-04 决策原文）、.planning/phases/02-auto-deploy/02-CONTEXT.md（D-08 冒烟断言、D-09 验证模式、D-16 部署闸门的既有语义链）</read_first>
  <files>.github/workflows/deploy.yml</files>
  <precondition>① gh CLI 已认证（gh auth status 通过——Phase 2 全程使用）；② Plan 01 与本计划 Task 1 的提交已在本地（本次 push 一并上线验证脚本）；③ 工作区干净；④ 稳态部署语义在位：DEPLOY_ENABLED=true（Phase 2 Plan 05 设闸后 push main 即构建+部署——本次推送内容无破坏，部署为等价重发）</precondition>
  <reversibility rating="reversible">单步插入，git revert 即可整体回退；既有 6 步零改动</reversibility>
  <action>修改 .github/workflows/deploy.yml：在 build job 的 Setup Pages 步骤（actions/configure-pages@v5）之后、Build with Jekyll 步骤之前插入一个新步骤（per D-04「Build with Jekyll 之前」；取 RESEARCH Open Question 4 推荐位置——紧跟 Setup Pages 之后，对 D-04 原文最直读）。步骤内容：name: Validate content；run: bundle exec ruby scripts/validate.rb（字面量命令——与既有 Smoke assertions 步骤同风格，run 块内无内联模板表达式，不复述仓库内容进 shell，T-03-02）。既有 6 步（Checkout / Setup Ruby / Setup Pages / Build with Jekyll / Smoke assertions / Upload artifact）一个字符都不改。依赖已就绪：Setup Ruby 的 bundler-cache: true 已安装 bibtex-ruby（Gemfile.lock），无需 apt 安装、无新依赖（D-06）。拦截语义复用 Phase 2 既有链条：验证步骤红 → build job 红 → 无 upload-pages-artifact → deploy job（needs: build）不跑 → 线上保持旧版（D-04 引用的决策链；Task 3 做真实红路径证明）。步骤名 Validate content 与 validate.rb 头注释的双入口说明保持一致。完成后 git add + commit + push origin main（稳态自动触发构建与部署），用 gh run watch 等待该 commit 的 run 至终态。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && ruby -ryaml -e 'YAML.load_file(".github/workflows/deploy.yml"); puts "YAML-OK"' | grep -qx 'YAML-OK' && grep -q 'name: Checkout' .github/workflows/deploy.yml && grep -q 'name: Setup Ruby' .github/workflows/deploy.yml && grep -q 'name: Smoke assertions' .github/workflows/deploy.yml && grep -q 'name: Upload artifact' .github/workflows/deploy.yml && L1=$(grep -n 'name: Setup Pages' .github/workflows/deploy.yml | cut -d: -f1) && L2=$(grep -n 'name: Validate content' .github/workflows/deploy.yml | cut -d: -f1) && L3=$(grep -n 'name: Build with Jekyll' .github/workflows/deploy.yml | cut -d: -f1) && [ "$L1" -lt "$L2" ] && [ "$L2" -lt "$L3" ] && grep -A1 'name: Validate content' .github/workflows/deploy.yml | grep -Fxq '        run: bundle exec ruby scripts/validate.rb' && git add .github/workflows/deploy.yml && git commit -m "ci(03): add Validate content step before Build with Jekyll (D-04)" && git push origin main && sleep 30 && SHA=$(git rev-parse HEAD) && RUN_ID=$(gh run list --branch main --limit 5 --json databaseId,headSha --jq ".[] | select(.headSha==\"$SHA\") | .databaseId" | head -1) && gh run watch "$RUN_ID" --exit-status >/dev/null 2>&1 && gh run view "$RUN_ID" --log | grep -q 'Validate content' && echo TASK2-PASS</automated>
  </verify>
  <acceptance_criteria>
    - deploy.yml parses as valid YAML; all six pre-existing step names still present (Checkout, Setup Ruby, Setup Pages, Build with Jekyll, Smoke assertions, Upload artifact — no existing step modified)
    - New step name Validate content has a line number strictly between Setup Pages and Build with Jekyll (D-04 pre-build position)
    - The step's run line is exactly "        run: bundle exec ruby scripts/validate.rb" (8-space indent, literal command, no inline template expressions in the new step)
    - Push to main succeeded; the run for the pushed SHA concluded success (gh run watch --exit-status exit 0) and its log contains the Validate content step — the script runs green in the CI bundler environment (bibtex-ruby resolves, Chinese output renders)
  </acceptance_criteria>
  <done>CI 双入口收口：同一脚本在 deploy.yml 构建前独立步骤运行且绿跑实证；既有步骤零改动</done>
</task>

<task type="auto">
  <name>Task 3: 红路径端到端证明 — 故意坏提交推 main：Validate content 红 → 无部署 → 线上保持旧版 → revert 复绿</name>
  <read_first>03-RESEARCH.md（Validation Architecture CONTENT-01-g 行、Pitfall 1 两类静默失败实证）、.planning/phases/02-auto-deploy/02-CONTEXT.md（D-16 双闸与「断言红则无工件可部署」语义）、03-02 Task 2 产出的绿跑 run id（对照）</read_first>
  <files></files>
  <precondition>Task 2 完成（Validate content 步骤已在 main 上绿跑过一次）；工作区干净；gh CLI 已认证；单人维护窗口（红提交与 revert 之间不与他人 push 交错）</precondition>
  <reversibility rating="reversible">红证提交随后 git revert --no-edit 即刻回滚，内容净变化为零；红提交本身保留在历史中作为 CONTENT-01-g 的证据（有意为之）</reversibility>
  <action>CI 红路径端到端证明（CONTENT-01-g / ROADMAP sc3+sc4：错误被拦截而非静默失败）。前置快照：gh api repos/zhangtaolab/zhangtaolab.github.io/deployments 取最新 deployment id（记 PRE），curl https://zhangtaolab.org/?cb=<随机> 确认 200 基线。① 制造真实内容错误：向 _data/grants.yml 末尾追加一行「- name: [unclosed」（YAML 语法错误——validate 层必拦的形态），git commit（信息注明 temporary red-proof, revert follows）并 push origin main。② 观察断言：gh run watch 该 commit 的 run 至终态，断言 run conclusion=failure 且失败步骤名恰为 Validate content（gh run view --json jobs 取 conclusion=="failure" 的 step 名——是验证步骤红而非 Build with Jekyll 红，证明构建前拦截）；gh api deployments 最新 id 仍等于 PRE（红跑未产生任何新部署）；curl 线上首页带 cache-buster 参数仍 200（旧 artifact 持续服务，Cloudflare 缓存规避同 Phase 2 惯例）。③ 恢复：git revert --no-edit HEAD 并 push；gh run watch 至 success；再取 deployments 最新 id（记 POST2）应大于 PRE（稳态自动部署恢复）；线上仍 200。④ 全程把两个 run id、失败步骤名、PRE/POST2 deployment id 记入 SUMMARY 证据表。若②中任一断言不成立（run 意外绿、失败步骤不是 Validate content、或产生了新部署）——立即停止并回报，绝不带着未证明的拦截语义继续（此时线上可能已暴露坏内容，须先 revert 再调查）。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && PRE=$(gh api repos/zhangtaolab/zhangtaolab.github.io/deployments --jq '.[0].id') && printf -- '- name: [unclosed\n' >> _data/grants.yml && git add _data/grants.yml && git commit -m "test(03): temporary red-proof of CI validate gate (revert follows)" && git push origin main && sleep 30 && SHA=$(git rev-parse HEAD) && RID=$(gh run list --branch main --limit 5 --json databaseId,headSha --jq ".[] | select(.headSha==\"$SHA\") | .databaseId" | head -1) && gh run watch "$RID" --exit-status >/dev/null 2>&1; RC=$?; [ "$RC" -ne 0 ] && gh run view "$RID" --json jobs --jq '.jobs[].steps[] | select(.conclusion=="failure") | .name' | grep -qx 'Validate content' && POST=$(gh api repos/zhangtaolab/zhangtaolab.github.io/deployments --jq '.[0].id') && [ "$POST" = "$PRE" ] && [ "$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 "https://zhangtaolab.org/?cb=$RANDOM")" = "200" ] && git revert --no-edit HEAD && git push origin main && sleep 30 && SHA2=$(git rev-parse HEAD) && RID2=$(gh run list --branch main --limit 5 --json databaseId,headSha --jq ".[] | select(.headSha==\"$SHA2\") | .databaseId" | head -1) && gh run watch "$RID2" --exit-status >/dev/null 2>&1 && POST2=$(gh api repos/zhangtaolab/zhangtaolab.github.io/deployments --jq '.[0].id') && [ "$POST2" -gt "$PRE" ] && [ "$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 "https://zhangtaolab.org/?cb=$RANDOM")" = "200" ] && [ -z "$(git status --porcelain)" ] && echo TASK3-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Red commit run: conclusion failure; the failed step name is exactly Validate content (pre-build interception — not Build with Jekyll)
    - No new deployment created by the red run (latest deployment id unchanged from PRE) and the live site returned 200 throughout the red window (previous artifact keeps serving)
    - Revert push: run conclusion success; a new deployment id exists (POST2 > PRE — steady-state auto-deploy resumed); live site 200
    - Working tree clean at the end (git status --porcelain empty — revert left no residue)
    - SUMMARY records the evidence table: both run ids, failed step name, PRE/POST2 deployment ids
  </acceptance_criteria>
  <done>拦截语义端到端实证：坏内容在 CI 构建前被 Validate content 步骤拦下（run 红、无部署、线上不动），revert 后流水线与稳态部署复绿</done>
</task>

</tasks>

## Artifacts this phase produces（Plan 02 部分）

- `.github/workflows/deploy.yml`（修改）——新符号：步骤名 `Validate content`（build job 内、Setup Pages 与 Build with Jekyll 之间）；既有 6 步与 deploy job 零改动
- `scripts/validate.rb`（追加）——新行为符号：D-08 提醒行（输出前缀「提醒：ref.bib 条目数 N → M 已变化；publications.md …」），退出码契约不变（0/1 仅由 errors 数组决定）
- 证据记录（03-02-SUMMARY.md）：绿跑 run id、红跑 run id + 失败步骤名、PRE/POST2 deployment id、对比 probe 结果（validate exit 1 而 jekyll build exit 0）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 仓库内容 → CI 工作流（deploy.yml 新步骤） | 步骤 run 命令由仓库定义并执行——不得引入 shell 注入面（内联模板表达式会把仓库内容复述进 shell） |
| 红证提交 → main 分支 / 线上站点 | 故意坏内容短暂进入 main：依赖「验证红 → 无工件 → 不部署」语义兜底，线上不受影响；恢复路径 = 立即 revert |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering（注入） | deploy.yml 新步骤 run 命令 | medium | mitigate | run 块为字面量 bundle exec ruby scripts/validate.rb，无内联模板表达式（与既有 Smoke assertions 同风格）；Task 2 automated 以精确整行 grep（-Fxq，含缩进）钉死该行内容 |
| T-03-06 | Tampering | 工作流编辑破坏既有部署语义 | medium | mitigate | 只插入不改既有步骤：Task 2 automated 断言 6 个既有步骤名全部在位 + 新步骤行号严格位于 Setup Pages 与 Build with Jekyll 之间 + 推送后全 run 绿（含 deploy job 稳态）——三重证明语义未损 |
| T-03-04 | DoS（可用性） | 红证提交窗口内 main 短暂含坏内容、线上停在旧版 | low | accept | 设计内行为：线上保持旧版正是要证明的拦截语义；红证→revert 分钟级完成、单人维护窗口执行；revert run 绿 + 新 deployment 恢复稳态；接受理由＝零用户可见影响，换取拦截语义的唯一实证路径（dispatch 验证模式无法制造红步骤） |
| T-03-SC | Tampering | 包安装 | high | mitigate | 同 Plan 01：本计划零包安装（D-06；RESEARCH Package Legitimacy Audit 无新增包）——无 [ASSUMED]/[SUS] 包，无需人工闸门 |

</threat_model>

<verification>
- Task 1/2/3 的 automated 验证全绿（CONTENT-01-f/g + D-04 CI 半边）
- 阶段全套件（RESEARCH Validation Architecture phase gate）：`scripts/validate.sh && bundle exec jekyll build --destination /tmp/_site_v3 && scripts/ci-smoke.sh /tmp/_site_v3` 三连绿
- 对比 probe（本阶段存在理由的机器证明——「验证脚本是唯一防线」）：备份 news.yml 后以 perl -pi -e 's/^- /  /' 映射化，同份坏内容下 `scripts/validate.sh` 退出 1 而 `JEKYLL_ENV=production bundle exec jekyll build --destination /tmp/_site_v3_contrast` 退出 0，恢复后 git status --porcelain 证洁（RESEARCH Pitfall 1 破坏性实验的可复跑版本）
- CONTENT-01 test map a~g 七项全部有机器真值记录（a~e 于 Plan 01，f 于本计划 Task 1，g 于本计划 Task 3）
</verification>

<success_criteria>
- ROADMAP sc4 成立：维护者发布前有本地命令 + CI 构建前独立步骤双保险，同一脚本两处复用（D-04）
- ROADMAP sc1/sc2 的 CI 半边成立：验证步骤位于 Build with Jekyll 之前，绿跑实证脚本在 CI 环境运行
- ROADMAP sc3 成立且有反向证明：坏内容使 run 在 Validate content 步骤红、无部署、线上保持旧版（错误显式报出而非静默失败）
- D-04/D-08 落地；prohibitions 三条（P-03-1/2/3）无违反迹象，待 /gsd-verify-work 人工背书
</success_criteria>

<output>
Create `.planning/phases/03-content-validation/03-02-SUMMARY.md` when done
</output>
