---
phase: 03-content-validation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - scripts/validate.rb
autonomous: true
requirements: [CONTENT-01]
gap_closure: true
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "基线（干净树）：scripts/validate.sh exit 0 且输出恰一行 `PASS: news=6, team=4, pi=1, alumni=2, grants=2, bib=12, 键唯一`——12 条 bib 中 8 条无 doi 条目保持全绿（P-03-2 回归护栏；验证 spot-check 1）"
    - "空 _data yml + 同时坏的 grants.yml：一次运行同时报出两条——空文件走既有结构层路径输出「顶层结构应为列表…当前是 NilClass」（D-01/D-02 结构规则原样复用）+ grants.yml 第 N 行 YAML 语法错误——并输出 FAIL: 2 个问题、exit 1、输出中无英文回溯帧（gap 1 收口 / spot-check 15 翻绿）"
    - "被删除/改名的 _data yml：报出中文「<文件名>：文件不存在——请勿删除或改名数据文件」，其余文件同跑继续校验（其错误同次报出），FAIL 汇总 + exit 1（gap 1 收口 / spot-check 16 翻绿）"
    - "papers/ref.bib 含非 UTF-8 字节（GBK 保存事故）：报出中文「papers/ref.bib 含非 UTF-8 字节（多为编辑器以 GBK 等编码保存）——请以 UTF-8 重新保存」，已收集的 YAML 错误仍全部打印，FAIL + exit 1、无英文回溯（gap 2 收口 / spot-check 17 翻绿）"
    - "papers/ref.bib 缺失：报出中文文件不存在消息，YAML 侧错误仍打印，FAIL + exit 1（gap 2 收口 / spot-check 18 翻绿）"
    - "设计内错误类零回归（spot-checks 2-13）：YAML 语法行列号定位、跨文件双错一次收集、role/date/映射化结构错误、bib 缺 year/重复键/截断/缺逗号（D-03 三层校验原样）、D-08 提醒（条目数 12 → 13、含 publications.md 字样、exit 0 不阻断）全部与已执行阶段逐字同形，且经同一本地一条命令入口跑出（D-05/D-07/D-08）"
    - "WR-01 收口：`cd _data && ../scripts/validate.sh` 与仓库根调用输出逐字相同（同一 PASS 行、exit 0）——全部文件 I/O 经 __dir__ 锚定的 ROOT 常量解析；D-08 git 层保持仓库相对路径 spec"
    - "WR-02 收口：层① 加载入口为 Psych.safe_load(..., permitted_classes: [Date], aliases: true)——旧 AST 反序列化调用链在文件中彻底消失（含注释），数据文件中出现 !ruby/object 标签时得到中文「含不支持的 YAML 类型标签」报错而非崩溃或对象实例化（T-03-01 缓解描述与实现一致）"
    - "IN-01 收口：以空格形态书写的重复引用键（如 @article {key, 与 @article{key, 混合并存）被原始文本 tally 抓到（键提取与条目计数两个正则同源化，基线计数仍为 12）；IN-03 收口：counts 的空哈希死初始化已删除"
  artifacts:
    - path: "scripts/validate.rb"
      provides: "六层引擎结构保留；层① 换 safe_load 入口 + 四级 rescue 阶梯（Psych::SyntaxError / Psych::DisallowedClass / Errno::ENOENT / StandardError）；层③ raw 单次读入 + valid_encoding? 预检 + 三级 rescue 阶梯（BibTeX::ParseError / Errno::ENOENT / StandardError）；新增 ROOT = File.expand_path(\"..\", __dir__) 常量锚定全部 File I/O；层⑤⑥ 正则同源化（IN-01）；死 counts 初始化移除（IN-03）；中文消息文案与 0/1 退出码契约不变（D-06 零新增依赖：psych 为 Ruby default gem）"
    - path: "scripts/validate.sh"
      provides: "零改动（字节不变）——$(dirname \"$0\") 已定位无关；锚定在 validate.rb 内完成后子目录调用即恢复"
    - path: ".github/workflows/deploy.yml"
      provides: "零改动（字节不变）——Validate content 步骤字面量 bundle exec ruby scripts/validate.rb 原样，CI 端经脚本自身修复自动继承，红/绿拦截语义不动（D-04）"
  key_links:
    - from: "validate.rb 全部 File I/O"
      to: "_data/*.yml + papers/ref.bib"
      via: "File.join(ROOT, 仓库相对路径)——__dir__ 锚定使任意 CWD 调用等价（planner 本机实测：从 _data 子目录运行路径解析正确）"
      pattern: "File.expand_path(\"..\", __dir__)"
    - from: "D-08 提醒层 git 命令"
      to: "HEAD:papers/ref.bib"
      via: "仓库相对路径 spec（planner 本机实测：HEAD: 后接绝对路径在任何 CWD 下都失败，纯仓库相对路径从子目录可用——git 层禁止改用绝对路径）"
      pattern: "git show HEAD:papers/ref.bib"
    - from: "deploy.yml Validate content 步骤"
      to: "bundle exec ruby scripts/validate.rb"
      via: "既有字面量 run 命令不变——CI 在工作区根运行，ROOT 锚定对其为无操作，修复零 CI 侧改动即生效（D-04 双入口同一脚本）"
      pattern: "run: bundle exec ruby scripts/validate.rb"
  flagged_assumptions:
    - "CONTENT-01 的 edge-probe 覆盖行为 unclassified/unresolved（承自 03-01-PLAN flagged_assumption 与 03-VERIFICATION.md human_verification 第 3 条）：本计划把验收面扩为 RESEARCH probe 矩阵 a~g 回归 + 三个新发现边界输入类（空文件/缺文件/非 UTF-8）显式 truth；a~g + 边界类对 CONTENT-01 是否充分仍待 /gsd-verify-work 人工确认，不得自动视为已覆盖"
    - "safe_load 接受面参数（permitted_classes: [Date]、aliases: true）为 planner 本机实测选型（psych 5.3.1：5 个数据文件解析结果与旧入口完全相等；空文件返回 nil；unquoted ISO 日期标量解析为 Date——与旧接受面行为对齐；!ruby/object 仍被 Psych::DisallowedClass 拦截）。残余的、有意的行为变化：未来数据文件若出现冷门 YAML 类型标签，将得到中文拒绝消息而非旧入口的静默接受——属收紧而非放宽，若开发者否决需回到本条修订"
    - "P-03-1/2/3 三条 judgment 级禁制仍为 flagged-unverified（见 prohibitions），最终裁定权属开发者（03-VERIFICATION.md human_verification 第 1 条，随本计划收口后一并人工背书）"
  prohibitions:
    - statement: "MUST NOT write to or modify any repository file from the validator — strictly read-only (zero write paths: no File.write, no FileUtils, no redirect-writes); every destructive probe restores the file and proves cleanliness via git status --porcelain (P-03-1)"
      status: flagged-unverified
      verification: judgment
    - statement: "MUST NOT block legitimate content — the 12-entry baseline (8 entries without doi) stays 100% green; the rescue/encoding changes may only convert crashes into Chinese verdicts, never add rejection rules for content the site currently renders correctly (P-03-2)"
      status: flagged-unverified
      verification: judgment
    - statement: "MUST NOT perform any file operation against publications.md — it appears only inside D-08 reminder text; no replacement, regeneration, or auto-sync of the handwritten list (P-03-3)"
      status: flagged-unverified
      verification: judgment
---

<objective>
## Phase Goal（user story，源自 ROADMAP Goal，gap-closure 视角）

**As a** 实验室维护者, **I want to** 即便我犯下空文件/删错文件/编辑器以 GBK 保存这类高频事故错误，验证命令也以中文明确报出全部文件的全部错误并 exit 1（而非英文回溯崩溃、掩盖其余错误）, **so that** 「错误被明确报出而非静默失败」（CONTENT-01）在任何边界输入下都成立。

## Plan 03 — Gap Closure：层①/层③ 抗崩收口（safe_load + rescue 阶梯 + 编码预检 + __dir__ 锚定）+ WR-01/WR-02 + IN-01/IN-03

Purpose: 关闭 03-VERIFICATION.md 的两个 critical gap（空/缺 _data/*.yml 崩溃掩盖他错；非 UTF-8/缺 ref.bib 崩溃于汇总前）与开发者裁定一并修复的 WR-01（CWD 相对路径）、WR-02（YAML 反序列化路径），顺带收口 IN-01（键提取/计数正则不一致漏检空格形态）与 IN-02/IN-03（三次读盘、死初始化）。单一根因：层①与层③ 的 rescue 面窄于「收集一切、中文报出、永不崩溃」的自我契约（03-01 Plan truth 6 / T-03-03）。修复全部落在 scripts/validate.rb 单文件内；validate.sh 与 deploy.yml 字节不变，CI 拦截语义（truth 12/13 已端到端实证）零改动继承（D-04）。

Output: 修复后的 scripts/validate.rb——验证者 spot-checks 1-18 全部可跑出 PASS（15-18 由崩溃翻绿，1-14 零回归）；全收集/中文报出/exit 契约在全部输入类上成立。

规划期已验证事实（planner 本机 probe，2026-08-20，Ruby 4.0.6 / psych 5.3.1 / bibtex-ruby 6.2.0，执行器勿凭记忆改写）：
- Psych.safe_load 对现有 5 个数据文件解析结果与旧入口逐文件完全相等（Array 长度与内容 equal=true）；空文件/纯注释文件返回 nil（走既有「顶层结构应为列表，当前是 NilClass」路径）；Psych::SyntaxError 仍自带 line/column/problem
- safe_load 默认拒绝 unquoted ISO 日期标量（Psych::DisallowedClass: Date）——必须传 permitted_classes: [Date] 才与旧接受面行为对齐；叠加 aliases: true 后锚点/别名可用；两种参数下 !ruby/object 标签仍被拦截（DisallowedClass）
- git show HEAD: 后接绝对路径在任何 CWD 下失败；纯仓库相对路径（papers/ref.bib）从仓库根与子目录都可用——D-08 层的 git spec 必须保持仓库相对
- 同源化正则（键提取 /@\s*[a-zA-Z]\w*\s*\{([^,\s]+)\s*,/ 与条目计数 /@\s*[a-zA-Z]\w*\s*\{/）在现基线 ref.bib 上均得 12（计数语义不变）；空格形态 @article {dupk, ×2 被 tally 为 {"dupk"=>2}，而 bibtex-ruby 解析后静默改名（dupk→dupl）——正是原始文本查重必须看见该形态的实证
- 非 UTF-8 字节串 valid_encoding? 为 false；直接喂 BibTeX.parse 抛 ArgumentError（即 gap 2 崩溃帧）
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-validation/03-CONTEXT.md
@.planning/phases/03-content-validation/03-RESEARCH.md
@.planning/phases/03-content-validation/03-VERIFICATION.md
@.planning/phases/03-content-validation/03-REVIEW.md
@scripts/validate.rb
@scripts/validate.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 抗崩 tracer — 层① safe_load + 四级 rescue、层③ 编码预检 + 三级 rescue + raw 单次读入、__dir__ 路径锚定；四类崩溃输入翻绿</name>
  <read_first>03-VERIFICATION.md（frontmatter gaps 两条的 missing 修法清单、Behavioral Spot-Checks 第 15-18 行）、03-REVIEW.md（CR-01/CR-02/WR-01/WR-02 补丁形状——与本 action 同源）、scripts/validate.rb（现行 203 行全文——层① 第 28-39 行、层③ 第 126-135 行、层⑤⑥ 第 155-191 行）、scripts/validate.sh、03-01-SUMMARY.md（现有结构与输出契约描述）</read_first>
  <files>scripts/validate.rb</files>
  <precondition>bundle 环境可用：bundle exec ruby -e 'require "psych"; require "bibtex"' 退出码 0；工作区干净（git status --porcelain 为空）——本任务含 4 条破坏性 probe，须恢复后证洁</precondition>
  <reversibility rating="reversible">单文件编辑，git revert 即整体回退；safe_load 入口切换是开发者 2026-08-20 gap-planning 闸门裁定的一并修复项（与 gap 1 推荐修法同源）</reversibility>
  <action>只改 scripts/validate.rb（validate.sh 与 .github/workflows/deploy.yml 一字不动，per D-04 CI 字面量继承）。① WR-01 路径锚定：在 YAML_FILES 常量附近新增 ROOT = File.expand_path("..", __dir__)；层① 的读取改为 File.read(File.join(ROOT, path))；层③ 的读取改为 File.read(File.join(ROOT, BIB_PATH))。YAML_FILES 的值与 BIB_PATH 保持仓库相对字符串原样（"_data/news.yml" …、"papers/ref.bib"）——理由有二：全部中文错误消息打印这些字面量（D-07 消息逐字不变，含「papers/ref.bib …」前缀），且 D-08 层把 BIB_PATH 内插进 HEAD:#{BIB_PATH} 的 git spec 必须是仓库相对（planner 实测：git show HEAD: 后接绝对路径失败、纯相对路径从任意 CWD 可用）；消息里的 File.basename(path) 部分自然不变。② 层① 收口（gap 1 + WR-02，per D-06 psych 为 default gem 零新增依赖）：把现行的 parse_file 后接 AST 到 Ruby 对象转换的加载调用（第 35 行）整体替换为 parsed[label] = Psych.safe_load(File.read(File.join(ROOT, path)), permitted_classes: [Date], aliases: true)。参数依据（本机实测）：5 个现有文件解析结果与旧入口完全相等；空文件返回 nil、由既有结构层报「顶层结构应为列表…当前是 NilClass」（D-01/D-02 结构规则原样复用，零新规则——P-03-2）；Date 许可使 unquoted ISO 日期标量与旧接受面行为一致（随后被既有 date 格式规则以「date 格式不合法」拦截——设计的拦截路径保留）；!ruby/object 标签仍被拒绝。rescue 阶梯按_specific→general_ 追加三条：rescue Psych::SyntaxError（既有消息逐字保留）；rescue Psych::DisallowedClass => e，errors 追加「#{File.basename(path)}：含不支持的 YAML 类型标签（#{e.message 前 60 字符}）——数据文件只需普通文本/数字/列表，请去掉类型标签」；rescue Errno::ENOENT，errors 追加「#{File.basename(path)}：文件不存在——请勿删除或改名数据文件」；rescue StandardError => e 兜底，errors 追加「#{File.basename(path)} 读取异常（#{e.class}）：#{e.message[0, 80]}——请人工检查该文件」。任一 rescue 触发时不把 label 写入 parsed（既有 next unless parsed.key?(label) 使结构层跳过该文件、其余文件继续——T-03-03 兜底在层①真正成立）。③ 层③ 收口（gap 2 + IN-02）：先单次读入 raw = File.read(File.join(ROOT, BIB_PATH))；raw.valid_encoding? 为 false 时 errors 追加「#{BIB_PATH} 含非 UTF-8 字节（多为编辑器以 GBK 等编码保存）——请以 UTF-8 重新保存」并保持 bib 为 nil 跳过解析，否则 bib = BibTeX.parse(raw)。rescue 阶梯：BibTeX::ParseError（既有消息逐字保留）；Errno::ENOENT，errors 追加「#{BIB_PATH}：文件不存在——请勿删除或改名数据文件」；StandardError => e 兜底，errors 追加「#{BIB_PATH} 解析异常（#{e.class}）：#{e.message[0, 80]}——请人工检查」。层⑤ 的键扫描与层⑥ 的工作区计数改用同一 raw 变量（消除三次读盘），层⑥ 的对比入口加 raw 存在性 guard（ref.bib 缺失时 raw 为 nil，直接静默跳过工作区侧计数——否则对 nil 计数即崩溃）；层⑤ 维持仅在解析成功（bib 非 nil）时执行的既有门控。④ WR-02 注释修正（T-03-01 缓解描述纠偏）：重写层① 上方注释块（第 29-31 行）与文件头部相关行，如实描述 safe_load 入口（safe 语义、Date 许可的用途）；新注释中不得出现被移除旧 API 的名字（验证门 grep 会扫全文件含注释）。⑤ 其余不动：层② 结构规则、层④ 必填、汇总输出格式、exit 0/1 契约、D-08 提醒文案与退出码零影响语义（D-08）。不运行 jekyll build；严格只读（P-03-1）；probe 后恢复并证洁。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && scripts/validate.sh > /tmp/v3g-t1-base.txt 2>&1; RC=$?; [ "$RC" = "0" ] && [ "$(wc -l < /tmp/v3g-t1-base.txt | tr -d ' ')" = "1" ] && grep -q '^PASS: news=6, team=4, pi=1, alumni=2, grants=2, bib=12, 键唯一' /tmp/v3g-t1-base.txt && cp _data/news.yml /tmp/v3g-t1-news.bak && cp _data/grants.yml /tmp/v3g-t1-grants.bak && : > _data/news.yml && printf -- '- name: [unclosed\n' >> _data/grants.yml && scripts/validate.sh > /tmp/v3g-t1-empty.txt 2>&1; R1=$?; cp /tmp/v3g-t1-news.bak _data/news.yml; cp /tmp/v3g-t1-grants.bak _data/grants.yml; [ "$R1" = "1" ] && grep -q '顶层结构应为列表' /tmp/v3g-t1-empty.txt && grep -q 'NilClass' /tmp/v3g-t1-empty.txt && grep -Eq 'grants\.yml 第 [0-9]+ 行' /tmp/v3g-t1-empty.txt && grep -q '^FAIL: 2 个问题' /tmp/v3g-t1-empty.txt && ! grep -Eq 'validate\.rb:[0-9]+' /tmp/v3g-t1-empty.txt && mv _data/news.yml /tmp/v3g-t1-news-moved && printf -- '- name: [unclosed\n' >> _data/grants.yml && scripts/validate.sh > /tmp/v3g-t1-miss.txt 2>&1; R2=$?; cp /tmp/v3g-t1-grants.bak _data/grants.yml; mv /tmp/v3g-t1-news-moved _data/news.yml; [ "$R2" = "1" ] && grep -q 'news.yml：文件不存在——请勿删除或改名数据文件' /tmp/v3g-t1-miss.txt && grep -Eq 'grants\.yml 第 [0-9]+ 行' /tmp/v3g-t1-miss.txt && ! grep -Eq 'validate\.rb:[0-9]+' /tmp/v3g-t1-miss.txt && cp papers/ref.bib /tmp/v3g-t1-bib.bak && cp _data/news.yml /tmp/v3g-t1-news2.bak && printf -- '- date: [unclosed\n' > _data/news.yml && printf '@article{gbkprobe, title={\xd6\xd0\xce\xc4}, author={a}, year={2026}}\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3g-t1-gbk.txt 2>&1; R3=$?; cp /tmp/v3g-t1-bib.bak papers/ref.bib; cp /tmp/v3g-t1-news2.bak _data/news.yml; [ "$R3" = "1" ] && grep -q '含非 UTF-8 字节' /tmp/v3g-t1-gbk.txt && grep -q '请以 UTF-8 重新保存' /tmp/v3g-t1-gbk.txt && grep -Eq 'news\.yml 第 [0-9]+ 行' /tmp/v3g-t1-gbk.txt && ! grep -Eq 'validate\.rb:[0-9]+' /tmp/v3g-t1-gbk.txt && mv papers/ref.bib /tmp/v3g-t1-bib-moved && printf -- '- date: [unclosed\n' > _data/news.yml && scripts/validate.sh > /tmp/v3g-t1-nobib.txt 2>&1; R4=$?; mv /tmp/v3g-t1-bib-moved papers/ref.bib; cp /tmp/v3g-t1-news2.bak _data/news.yml; [ "$R4" = "1" ] && grep -q 'papers/ref.bib：文件不存在' /tmp/v3g-t1-nobib.txt && grep -Eq 'news\.yml 第 [0-9]+ 行' /tmp/v3g-t1-nobib.txt && (cd _data && ../scripts/validate.sh > /tmp/v3g-t1-sub.txt 2>&1); R5=$?; [ "$R5" = "0" ] && grep -q '^PASS: news=6, team=4, pi=1, alumni=2, grants=2, bib=12, 键唯一' /tmp/v3g-t1-sub.txt && grep -q 'Psych.safe_load' scripts/validate.rb && ! grep -q 'to_ruby' scripts/validate.rb && grep -q 'valid_encoding?' scripts/validate.rb && [ "$(grep -c 'rescue Errno::ENOENT' scripts/validate.rb)" -ge 2 ] && grep -q '__dir__' scripts/validate.rb && scripts/validate.sh >/dev/null 2>&1 && [ -z "$(git status --porcelain _data papers)" ] && echo TASK1-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Baseline (clean tree): scripts/validate.sh exits 0, output is exactly one line "PASS: news=6, team=4, pi=1, alumni=2, grants=2, bib=12, 键唯一" — the 8 no-doi entries stay green (P-03-2 regression guard)
    - Emptied _data/news.yml (: >) + concurrently syntax-broken grants.yml: one run reports BOTH the 顶层结构应为列表 … 当前是 NilClass message for news.yml AND the grants.yml 第 N 行 YAML syntax error, prints "FAIL: 2 个问题", exits 1; output contains no Ruby backtrace frame (spot-check 15 flipped)
    - Moved-away _data/news.yml + broken grants.yml: output contains "news.yml：文件不存在——请勿删除或改名数据文件" AND the grants.yml error (other files still validated), exit 1, no backtrace (spot-check 16 flipped)
    - GBK bytes (\xd6\xd0\xce\xc4) appended into papers/ref.bib + broken news.yml: output contains 含非 UTF-8 字节 … 请以 UTF-8 重新保存 AND the news.yml 第 N 行 YAML error (collected YAML errors still printed), exit 1, no backtrace (spot-check 17 flipped)
    - Moved-away papers/ref.bib + broken news.yml: output contains "papers/ref.bib：文件不存在" AND the news.yml YAML error, exit 1 (spot-check 18 flipped)
    - WR-01: (cd _data && ../scripts/validate.sh) exits 0 with the same PASS line as a root invocation
    - Source assertions: scripts/validate.rb contains Psych.safe_load, valid_encoding?, __dir__; contains at least two rescue Errno::ENOENT clauses; the old AST-conversion call is absent from the entire file including comments (negative grep over the source)
    - All probes restored; git status --porcelain over _data/ and papers/ is empty; a final baseline run exits 0
  </acceptance_criteria>
  <done>四类边界输入（空 yml/缺 yml/GBK bib/缺 bib）从英文回溯崩溃翻绿为中文报出 + 全收集 + FAIL 汇总 + exit 1；基线与子目录调用全绿；WR-01/WR-02 在源码层面可 grep 证明</done>
</task>

<task type="auto">
  <name>Task 2: IN-01 正则同源化 + IN-03 死代码清除 + 设计内错误类回归电池（含 !ruby/object 拒绝与空格形态重复键）</name>
  <read_first>03-REVIEW.md（IN-01 第 135-140 行、IN-03 第 149-154 行修法）、scripts/validate.rb（层⑤ 第 155-162 行键提取正则、层⑥ bib_entry_count 第 174-176 行计数正则、第 26/40 行 counts）、03-VERIFICATION.md（Behavioral Spot-Checks 第 1-14 行——回归基准）、03-RESEARCH.md（Pitfall 2 重复键静默改名——原始正则是唯一真相源的理由）</read_first>
  <files>scripts/validate.rb</files>
  <precondition>Task 1 完成（safe_load 入口、rescue 阶梯、raw 单次读入、ROOT 锚定已在位，基线 PASS）；工作区干净（本任务含 8 条破坏性 probe）</precondition>
  <reversibility rating="reversible">正则替换与死代码删除均为局部编辑，git revert 可整体回退；基线计数语义经 planner 实测不变（12/12）</reversibility>
  <action>仍只改 scripts/validate.rb。① IN-01 正则同源化：层⑤ 键提取正则由现行未锚定形态改为 raw.scan(/@\s*[a-zA-Z]\w*\s*\{([^,\s]+)\s*,/)（捕获组与后续 tally/报错逻辑逐字保留）；层⑥ bib_entry_count 的计数正则由行锚定形态改为 text.scan(/@\s*[a-zA-Z]\w*\s*\{/).length（去行锚、允许 @ 与类型名后有空格）。两个正则自此共享同一 opener 子模式 [a-zA-Z]\w* 加可选空白——planner 本机实测：现基线 ref.bib 上键提取与条目计数均仍为 12（PASS 行与 D-08 提醒算术零变化），空格形态 @article {dupk, 的重复键被 tally 为同一键出现 2 次（而 bibtex-ruby 解析后静默改名——原始文本查重必须看见该形态，Pitfall 2）。② IN-03：删除第 26 行 counts 的空哈希死初始化（该变量随后由 parsed.transform_values 赋值一次、仅在其后读取，删除后行为不变）。③ 回归电池（verify 块全量执行）：覆盖 spot-checks 2-13 代表项——YAML 语法行列号双文件一次收集、role 拼错、date 非法、映射化（当前是 Hash）、!ruby/object 标签拒绝（WR-02 行为面：中文类型标签消息 + exit 1 + 无回溯）、bib 缺 year、空格形态重复键、截断条目、缺逗号（D-03 三层校验全链原样回归）、D-08 提醒（追加合法条目 12 → 13 + publications.md 字样 + exit 0 不阻断）——全部经 D-05 的本地一条命令入口 scripts/validate.sh 跑出。全部 probe 恢复并证洁（P-03-1）。不新增任何校验规则、不加严任何现有规则（P-03-2——正则变化只影响「看得见什么」，不影响「什么合法」）。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && scripts/validate.sh > /tmp/v3g-t2-base.txt 2>&1; RC=$?; [ "$RC" = "0" ] && grep -q '^PASS: news=6, team=4, pi=1, alumni=2, grants=2, bib=12, 键唯一' /tmp/v3g-t2-base.txt && cp _data/news.yml /tmp/v3g-t2-news.bak && cp _data/grants.yml /tmp/v3g-t2-grants.bak && cp _data/team_members.yml /tmp/v3g-t2-tm.bak && cp papers/ref.bib /tmp/v3g-t2-bib.bak && printf -- '- date: [unclosed\n' > _data/news.yml && printf -- '- name: [unclosed\n' >> _data/grants.yml && scripts/validate.sh > /tmp/v3g-t2-dual.txt 2>&1; R1=$?; cp /tmp/v3g-t2-news.bak _data/news.yml; cp /tmp/v3g-t2-grants.bak _data/grants.yml; [ "$R1" = "1" ] && grep -Eq 'news\.yml 第 [0-9]+ 行第 [0-9]+ 列' /tmp/v3g-t2-dual.txt && grep -Eq 'grants\.yml 第 [0-9]+ 行' /tmp/v3g-t2-dual.txt && perl -pi -e 's/role: student/role: studnet/' _data/team_members.yml && scripts/validate.sh > /tmp/v3g-t2-role.txt 2>&1; R2=$?; cp /tmp/v3g-t2-tm.bak _data/team_members.yml; [ "$R2" = "1" ] && grep -q 'role 取值不合法' /tmp/v3g-t2-role.txt && grep -q 'pi | member | student' /tmp/v3g-t2-role.txt && perl -pi -e 's/date: "May 2026"/date: "2026-05"/' _data/news.yml && scripts/validate.sh > /tmp/v3g-t2-date.txt 2>&1; R3=$?; cp /tmp/v3g-t2-news.bak _data/news.yml; [ "$R3" = "1" ] && grep -q 'date 格式不合法' /tmp/v3g-t2-date.txt && perl -pi -e 's/^- /  /' _data/news.yml && scripts/validate.sh > /tmp/v3g-t2-map.txt 2>&1; R3b=$?; cp /tmp/v3g-t2-news.bak _data/news.yml; [ "$R3b" = "1" ] && grep -q '当前是 Hash' /tmp/v3g-t2-map.txt && printf -- '- !ruby/object:Kernel {}\n' >> _data/news.yml && scripts/validate.sh > /tmp/v3g-t2-obj.txt 2>&1; R4=$?; cp /tmp/v3g-t2-news.bak _data/news.yml; [ "$R4" = "1" ] && grep -q 'news.yml：含不支持的 YAML 类型标签' /tmp/v3g-t2-obj.txt && ! grep -Eq 'validate\.rb:[0-9]+' /tmp/v3g-t2-obj.txt && printf '@article{probeyear, title={T}, author={A}, journal={J}}\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3g-t2-year.txt 2>&1; R5=$?; cp /tmp/v3g-t2-bib.bak papers/ref.bib; [ "$R5" = "1" ] && grep -q 'probeyear' /tmp/v3g-t2-year.txt && grep -q 'year 字段缺失' /tmp/v3g-t2-year.txt && printf '@article{spacedup, title={T}, author={A}, year={2026}}\n@article {spacedup, title={T2}, author={A2}, year={2027}}\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3g-t2-sdup.txt 2>&1; R6=$?; cp /tmp/v3g-t2-bib.bak papers/ref.bib; [ "$R6" = "1" ] && grep -q '引用键 spacedup 重复出现 2 次' /tmp/v3g-t2-sdup.txt && printf '@article{probetrunc, title={截断\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3g-t2-trunc.txt 2>&1; R7=$?; cp /tmp/v3g-t2-bib.bak papers/ref.bib; [ "$R7" = "1" ] && grep -q 'papers/ref.bib' /tmp/v3g-t2-trunc.txt && printf '@article{probecomma, title={t} author={a}}\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3g-t2-comma.txt 2>&1; R8=$?; cp /tmp/v3g-t2-bib.bak papers/ref.bib; [ "$R8" = "1" ] && grep -q '解析失败' /tmp/v3g-t2-comma.txt && printf '@article{probenew, title={New entry}, author={N}, year={2026}}\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3g-t2-rem.txt 2>&1; R9=$?; cp /tmp/v3g-t2-bib.bak papers/ref.bib; [ "$R9" = "0" ] && grep -q '提醒' /tmp/v3g-t2-rem.txt && grep -q 'publications.md' /tmp/v3g-t2-rem.txt && grep -Eq '条目数 12 → 13' /tmp/v3g-t2-rem.txt && grep -Fq '\{([^,\s]+)\s*,/' scripts/validate.rb && grep -Fq '/@\s*[a-zA-Z]\w*\s*\{/' scripts/validate.rb && ! grep -q 'counts = {}' scripts/validate.rb && scripts/validate.sh > /tmp/v3g-t2-final.txt 2>&1; RF=$?; [ "$RF" = "0" ] && [ "$(wc -l < /tmp/v3g-t2-final.txt | tr -d ' ')" = "1" ] && [ -z "$(git status --porcelain _data papers)" ] && echo TASK2-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Baseline PASS line unchanged (single line, dynamic counts, 键唯一 marker) — harmonized regexes keep baseline counts at 12/12
    - Dual broken YAML files (news + grants) reported in one run with 行/列 and 行 localization respectively (spot-checks 2/3 regression)
    - role typo probe reports role 取值不合法 with legal values pi | member | student; date probe reports date 格式不合法; mapping-ified news.yml reports 当前是 Hash (spot-checks 5/6/4 regression)
    - !ruby/object tag appended to news.yml produces the Chinese 含不支持的 YAML 类型标签 message, exit 1, no backtrace — WR-02 behavioral face (objects never instantiated)
    - bib probes: probeyear missing year reported by key; spaced-form duplicate pair (@article{spacedup, + @article {spacedup,) reported as 引用键 spacedup 重复出现 2 次 — IN-01 closed (both regex shapes seen by one tally); truncated entry and missing-comma entry each exit 1 naming papers/ref.bib (spot-checks 9-12 regression)
    - D-08 reminder: appended valid entry produces 提醒 + publications.md + 「条目数 12 → 13」 with exit 0 — reminder still never blocks (D-08, spot-check 13 regression)
    - Source: the harmonized opener regex /@\s*[a-zA-Z]\w*\s*\{/ appears in bib_entry_count and the key-tally capture \{([^,\s]+)\s*,/ in layer ⑤; the dead counts initializer is gone (negative grep over source)
    - All probes restored; git status --porcelain over _data/ and papers/ empty; final run is single-line PASS exit 0 (clean-tree silence, spot-check 14 regression)
  </acceptance_criteria>
  <done>设计内错误类（YAML 语法/结构、BibTeX 解析/必填/键唯一、D-08 提醒）在修复后零回归且消息逐字同形；空格形态重复键不再漏检；死代码清除；WR-02 行为面（类型标签拒绝）机器实证</done>
</task>

</tasks>

## Artifacts this phase produces（Plan 03 部分 — gap closure）

- `scripts/validate.rb`（修改）——变更后的契约面：
  - 层① 入口签名：Psych.safe_load(File.read(File.join(ROOT, path)), permitted_classes: [Date], aliases: true)；四级 rescue 阶梯（Psych::SyntaxError / Psych::DisallowedClass / Errno::ENOENT / StandardError），新增两条中文消息（「文件不存在——请勿删除或改名数据文件」「含不支持的 YAML 类型标签…」）
  - 层③ 数据流：raw 单次读入复用于解析/键扫描/D-08 计数（IN-02 顺带收口）；valid_encoding? 预检 + GBK 中文消息 + 三级 rescue 阶梯
  - 新增常量：ROOT = File.expand_path("..", __dir__)；YAML_FILES 值与 BIB_PATH 保持仓库相对字符串（消息与 git spec 双用途）
  - 层⑤⑥ 正则同源化（IN-01）：键提取 /@\s*[a-zA-Z]\w*\s*\{([^,\s]+)\s*,/ 与计数 /@\s*[a-zA-Z]\w*\s*\{/
  - 移除符号：counts 的空哈希死初始化（IN-03）、旧 AST 转换调用链及其误导注释（WR-02）
  - 不变契约：PASS/FAIL 输出格式、exit 0/1 语义、D-08 提醒文案与零阻断、六层引擎结构
- `scripts/validate.sh`（零改动，字节不变）
- `.github/workflows/deploy.yml`（零改动，字节不变——CI 端经脚本变更自动继承修复）
- `.planning/phases/03-content-validation/03-03-PLAN.md`（本计划）与 `COVERAGE.md`（无外部 API 集成声明，api-coverage 检查点产物）
- 无新增导出符号/新文件路径/新依赖（D-06）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 维护者手写内容（_data/*.yml、papers/ref.bib，可能经 PR 引入）→ 验证脚本 | 不可信输入：畸形/恶意输入不得使脚本崩溃、掩盖他错或实例化任意对象 |
| 验证脚本 → 仓库内容文件 | 只读语义（零写路径，P-03-1） |
| 任意调用 CWD → 脚本（新增边界） | 子目录调用（WR-01 修复后成为支持面）：路径解析必须与 CWD 无关，git spec 必须仓库相对 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / Elevation | validate.rb YAML 加载入口 | high | mitigate | Psych.safe_load(permitted_classes: [Date], aliases: true) 替换旧 AST 反序列化调用链（WR-02 收口）；Date 为值类、仅为日期标量行为对齐而许可——!ruby/object 标签仍被 Psych::DisallowedClass 拒绝（planner 本机实测）；Task 1 源码负向 grep + Task 2 !ruby/object 行为 probe 双证；误导注释一并纠正（T-03-01 缓解描述与实现一致） |
| T-03-03 | DoS | 畸形/缺失/非 UTF-8 输入使脚本崩溃并掩盖其余文件错误 | medium | mitigate | 层① 四级、层③ 三级 rescue 阶梯 + 解析前 valid_encoding? 预检；任何单文件异常只进 errors 数组、不中断运行；四类原崩溃输入的翻绿 probe 为验收面（gap 1/2 收口） |
| T-03-05 | Tampering | 验证过程改写被校验内容文件 | medium | mitigate | 严格只读（File.read 读取、零写路径，P-03-1）；每个 probe 的 automated 命令以 git status --porcelain 空 + 恢复后基线重跑绿收尾 |
| T-03-08 | Tampering（注入） | D-08 层 git 命令内插 | low | mitigate | 仅固定仓库相对常量进入 git 命令内插（BIB_PATH 字面量）；被校验内容不流入 shell；路径锚定改造刻意不触碰该内插形态（planner 实测绝对路径 spec 本就不可用） |
| T-03-SC | Tampering | 包安装 | high | mitigate | 本计划零包安装（D-06；safe_load 来自 psych——Ruby default gem，非新增依赖）——无 [ASSUMED]/[SUS] 包，无需人工闸门检查点 |

</threat_model>

<verification>
- Task 1/2 的 automated 验证全绿：验证者 spot-checks 15-18（四类边界输入）翻绿 + spot-checks 1-14 回归项保持绿——03-VERIFICATION.md 的 18 行 probe 矩阵全部可复跑为 PASS
- 变更面证明：本计划的提交不触碰 .github/workflows/deploy.yml 与 scripts/validate.sh——`git diff --name-only 71dcea14..HEAD -- scripts/ .github/` 仅返回 scripts/validate.rb（CI 红路径 truth 12/13 语义零改动继承，D-04）
- 阶段全套件：`scripts/validate.sh && bundle exec jekyll build --destination /tmp/_site_v3g && scripts/ci-smoke.sh /tmp/_site_v3g` 三连绿（修复不破坏正常构建链）
- probe 纪律：每条破坏性 probe 后恢复现场并 git status --porcelain 证洁（P-03-1）
</verification>

<success_criteria>
- 03-VERIFICATION.md 两个 critical gap（CR-01/CR-02）收口：空/缺 _data/*.yml、非 UTF-8/缺 papers/ref.bib 四类输入全部「中文报出 + 全收集 + FAIL 汇总 + exit 1」，无英文回溯、无错误掩盖
- WR-01（子目录调用）与 WR-02（safe_load 入口 + 注释纠正）按开发者 2026-08-20 裁定一并修复；IN-01（空格形态重复键漏检）与 IN-03（死初始化）顺带收口
- 设计内错误类（probe 矩阵 a~f）零回归、消息逐字同形；D-08 提醒行为不变（12→13 触发、干净树静默、退出码零影响）
- CI 双入口语义不变：deploy.yml 字节不动，Validate content 步骤红/绿行为经脚本继承（truth 12/13 已实证无需重证）
- 重跑 /gsd-verify-work 后 Phase 3 目标「维护者更新内容时语法错误被拦截，避免静默失败」在全部已知输入类上成立
</success_criteria>

<output>
Create `.planning/phases/03-content-validation/03-03-SUMMARY.md` when done
</output>
