---
phase: 03-content-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate.rb
  - scripts/validate.sh
autonomous: true
requirements: [CONTENT-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "scripts/validate.sh（bundle exec 下一条本地命令，不跑 jekyll build）在 1 秒内校验全部 5 个 _data/*.yml 与 papers/ref.bib；当前基线 exit 0 且输出单行 PASS 汇总，含动态计算的逐文件条目计数（news=6, team=4, pi=1, alumni=2, grants=2, bib=12）与键唯一标记（D-05/D-06，CONTENT-01-e）"
    - "任一 _data/*.yml 的 YAML 语法错误以中文报出文件名+行号+列号（格式：news.yml 第 N 行第 M 列：YAML 语法错误（problem））且 exit 1——先于任何 Jekyll 构建发生（D-07，ROADMAP sc1，CONTENT-01-a）"
    - "jekyll build 静默忽略的结构错误——必填字段缺失、role 拼错、news date 非法格式、Latest 出现在非首条、顶层非列表——以中文报出文件与条目序号（格式：news.yml 第 3 条：date 字段缺失）且 exit 1（D-01/D-02，ROADMAP sc3，CONTENT-01-d）"
    - "BibTeX 错误以中文报出 papers/ref.bib + 引用键或字段名，exit 1——覆盖缺逗号/截断条目（ParseError 包中文建议）、逐条 title/author/year 缺失、原始文本层面的引用键重复（bibtex-ruby 对重复键静默改名，解析结果不可用于查重）（D-03，ROADMAP sc2，CONTENT-01-b/c）"
    - "无 DOI 的条目（ref.bib 现存 wang2026maize 为实证）通过校验——DOI 非必填（D-03 宽容规则）"
    - "全部文件的全部错误一次运行收集报出（非 fail-fast）；语法坏的单文件只短路自身的结构检查，其余文件继续（RESEARCH Pattern 3）"
  artifacts:
    - path: "scripts/validate.rb"
      provides: "单文件验证引擎：YAML 语法层（psych）+ YAML 结构层（逐文件 schema）+ BibTeX 解析层 + 必填层 + 键唯一层（原始正则 tally）；errors 数组收集；中文 PASS/FAIL 汇总；exit 0/1 契约"
    - path: "scripts/validate.sh"
      provides: "本地维护者入口（exec bundle exec ruby 透传；与 ci-smoke.sh 同目录同范式；不装 pre-commit 钩子，D-05）"
  key_links:
    - from: "本地命令 scripts/validate.sh"
      to: "bundle exec ruby scripts/validate.rb"
      via: "exec 透传包装（PATTERNS §validate.sh 范式；bundle exec 使 bibtex-ruby 6.2.0 自 Gemfile.lock 解析，裸 ruby 可能找不到 gem）"
      pattern: "exec bundle exec ruby"
    - from: "scripts/validate.rb"
      to: "_data/news.yml + _data/team_members.yml + _data/pi.yml + _data/alumni.yml + _data/grants.yml + papers/ref.bib"
      via: "Psych.parse_file(path).to_ruby（SyntaxError 自带 line/column/problem）+ BibTeX.parse(File.read(...))（ParseError 无行号——脚本补文件名与中文建议，不承诺 bib 行号，Pitfall 3）"
      pattern: "Psych.parse_file"
  flagged_assumptions:
    - "edge-probe fallback 对 CONTENT-01 返回 category=unclassified、status=unresolved（probe: 'unclassified — review manually'）：需求的行为边界无法自动分类，无可提升的 probe-derived truth。本计划验收面改以 RESEARCH Validation Architecture（CONTENT-01-a..f probe 矩阵）与 ROADMAP 四条成功标准为根据——/gsd-verify-work 时需人工确认该覆盖充分"
  prohibitions:
    - statement: "MUST NOT modify any content file it validates (_data/*.yml, papers/ref.bib) or publications.md — validation tooling is strictly read-only per CONTEXT「不做任何数据文件或模板的改动」: no auto-fix, no normalization, no reformatting; every destructive probe restores the file and proves it via git status --porcelain"
      status: resolved
      verification: judgment
    - statement: "MUST NOT block legitimate content — validation rules stay no stricter than current data reality: the 5-file baseline stays 100% green (D-01), DOI stays optional so wang2026maize passes (D-03), and no rule may reject a content format the site currently renders correctly"
      status: resolved
      verification: judgment
---

<objective>
## Phase Goal（user story，源自 ROADMAP Goal）

**As a** 实验室维护者, **I want to** 在发布前用一条本地命令（scripts/validate.sh）校验全部 `_data/*.yml` 与 `papers/ref.bib`，语法与结构错误以中文明确到文件与位置并使命令退出非零, **so that** 内容错误在推送与构建之前被拦截，不再静默失败（构建全绿而页面悄悄缺内容）。

## Plan 01 — 验证脚本主体：tracer（一条命令两族文件端到端）+ 结构 schema 层 + BibTeX 深层校验

Purpose: 交付 CONTENT-01 的本地半边与全部校验逻辑本体。tracer 先把「wrapper → Ruby 引擎 → 两族文件 → 中文判定 + exit 码」这条最薄路径端到端跑通（含 bibtex-ruby 在 bundle exec 下可解析的架构性验证），随后两个 expansion 任务在同一 errors 数组上叠加 jekyll build 静默吞掉的结构层（D-01/D-02）与 BibTeX 必填/键唯一层（D-03）。脚本只读数据文件、零新增依赖（D-06）。

Output: scripts/validate.rb + scripts/validate.sh，基线全绿（0 新失败），probe 矩阵 CONTENT-01-a~e 全过（f/g 由 Plan 02 补）。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-validation/03-CONTEXT.md
@.planning/phases/03-content-validation/03-RESEARCH.md
@.planning/phases/03-content-validation/03-PATTERNS.md
@scripts/ci-smoke.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 端到端 tracer — validate.rb（YAML 语法层 + BibTeX 解析层）+ validate.sh 一条命令入口</name>
  <read_first>03-RESEARCH.md（Pattern 1 第 179-201 行 BibTeX 三层原型、Pattern 2 第 207-216 行 YAML 两层原型、Pattern 3 非 fail-fast、Code Examples §2/§4、Anti-Patterns、Pitfall 3 解析错误无行号、Pitfall 4 API 形状）、03-PATTERNS.md（validate.rb/validate.sh 范式、FAIL 风格与 ci-smoke.sh 的差异）、scripts/ci-smoke.sh（输出范式与头注释风格）、_data/news.yml、papers/ref.bib（验证对象现状）、Gemfile.lock（bibtex-ruby 6.2.0 在列证据）</read_first>
  <files>scripts/validate.rb, scripts/validate.sh</files>
  <precondition>bundle 环境可用：bundle exec ruby -e 'require "bibtex"' 退出码 0（bibtex-ruby 自 Gemfile.lock 解析）；工作区干净（git status --porcelain 为空）——本任务含破坏性 probe，须保证恢复后可证洁</precondition>
  <action>创建 scripts/validate.rb 与 scripts/validate.sh，per D-05（一条手动命令、不装 pre-commit 钩子）+ D-06（纯 Ruby 零新增依赖：psych 为 Ruby default gem、bibtex-ruby 6.2.0 已随 jekyll-scholar 锁在 Gemfile.lock、bundle exec 运行）+ D-07（报错中文、文件名/字段名/行号原样）。validate.rb 结构：① 中文头注释写明双入口约定（本地 scripts/validate.sh；CI 由 deploy.yml 直接 bundle exec ruby 调本脚本——CI 步骤由 Plan 02 落地）。② 顶部 errors = [] 收集数组（RESEARCH Pattern 3：全部错误一次报出，非 fail-fast）。③ YAML 语法层：对 5 个数据文件（_data/news.yml、_data/team_members.yml、_data/pi.yml、_data/alumni.yml、_data/grants.yml）逐个 Psych.parse_file(path).to_ruby，rescue Psych::SyntaxError 后 errors 追加「<basename> 第 e.line 行第 e.column 列：YAML 语法错误（e.problem）」（RESEARCH Code Examples §2，属性本机实测存在），该文件跳过后续结构检查、其余文件继续。④ BibTeX 解析层：BibTeX.parse(File.read("papers/ref.bib"))，rescue BibTeX::ParseError 后 errors 追加「papers/ref.bib 解析失败（检查最近编辑：多为缺失逗号或未闭合大括号）—— 」拼接 e.message 前 120 字符（Pitfall 3：解析器不提供文件名与行号，消息由脚本补；不承诺 bib 行号定位）。⑤ 汇总输出：errors 为空输出单行「PASS: news=6, team=4, pi=1, alumni=2, grants=2, bib=12」——逐文件计数由解析结果动态计算（Ruby 数组 length / bib.length），禁止把数字硬编码进字符串常量（维护者加条目后 PASS 行须自动更新）；非空输出「FAIL: N 个问题」+ 逐行错误；exit(errors.empty? ? 0 : 1)。BibTeX API 形状锁定（Pitfall 4，全部本机实测，禁凭训练记忆改写）：条目迭代用 bib.to_a（bib.entries 是 Hash）；字段访问只用 e[:title] 下标形式（条目是 Hash 子类，无点方法，点方法抛 NoMethodError）；键名用 e[:bibtex_key]；判空用 to_s.strip.empty?（字段值带大括号原样）。YAML 解析入口一律走 Psych.parse_file(...).to_ruby（parser API，与 Jekyll 数据读取同路，T-03-01 缓解）；不使用对象反序列化式加载接口。validate.sh 按 03-PATTERNS §validate.sh 原样三要素：#!/usr/bin/env bash + 中文头注释（内容验证入口；CI 走 deploy.yml 直调 validate.rb）+ exec bundle exec ruby "$(dirname "$0")/validate.rb" "$@"；chmod +x scripts/validate.sh（validate.rb 经 ruby 调用无需执行位）。不在脚本内运行 jekyll build（独立快速入口是本阶段存在理由）；不修改任何数据文件（严格只读，prohibition P-03-1）。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && scripts/validate.sh > /tmp/v3-t1-base.txt 2>&1; RC=$?; [ "$RC" = "0" ] && grep -q '^PASS: news=6, team=4, pi=1, alumni=2, grants=2, bib=12' /tmp/v3-t1-base.txt && cp _data/news.yml /tmp/v3-news.bak && printf -- '- date: [unclosed\n' > _data/news.yml && scripts/validate.sh > /tmp/v3-t1-yaml.txt 2>&1; R1=$?; cp /tmp/v3-news.bak _data/news.yml; [ "$R1" = "1" ] && grep -Eq 'news\.yml 第 [0-9]+ 行' /tmp/v3-t1-yaml.txt && grep -q 'YAML 语法错误' /tmp/v3-t1-yaml.txt && cp papers/ref.bib /tmp/v3-bib.bak && printf '@article{probex, title={t} author={a}}\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3-t1-bib.txt 2>&1; R2=$?; cp /tmp/v3-bib.bak papers/ref.bib; [ "$R2" = "1" ] && grep -q 'papers/ref.bib' /tmp/v3-t1-bib.txt && scripts/validate.sh > /dev/null 2>&1 && [ -z "$(git status --porcelain _data papers)" ] && grep -q 'Psych.parse_file' scripts/validate.rb && ! grep -Eq 'YAML\.(unsafe_)?load' scripts/validate.rb && grep -q 'exec bundle exec ruby' scripts/validate.sh && echo TASK1-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Baseline: scripts/validate.sh exits 0 with output line exactly matching "PASS: news=6, team=4, pi=1, alumni=2, grants=2, bib=12" (counts computed from parsed data, matching the session-verified baseline: news 6 entries, team_members 4, pi 1, alumni 2, grants 2, ref.bib 12 @article entries)
    - Injected YAML syntax error (single line "- date: [unclosed" replacing news.yml) produces exit 1 and output matching "news.yml 第 N 行" plus "YAML 语法错误" (Chinese, file + line, D-07)
    - Injected BibTeX missing-comma entry (@article{probex, title={t} author={a}} appended to ref.bib) produces exit 1 and output naming papers/ref.bib
    - Both probes restored; git status --porcelain over _data/ and papers/ is empty; a post-restore run exits 0 again
    - scripts/validate.rb contains Psych.parse_file and no deserialization-style load entry point; scripts/validate.sh contains the exec bundle exec ruby wrapper
  </acceptance_criteria>
  <done>一条命令本地端到端可用：基线 PASS exit 0；YAML 与 BibTeX 两族语法错误均以中文报出文件+位置并 exit 1；probe 现场恢复且证洁</done>
</task>

<task type="auto">
  <name>Task 2: YAML 结构层 — 逐文件 schema + news date 规则（D-01/D-02）</name>
  <read_first>03-RESEARCH.md（Code Examples §1 基线实测 0 错误的 schema 原型与字段消费点核对清单、Code Examples §3 结构检查骨架、Pitfall 6 基线失败风险、Pitfall 7 educationshort 死字段）、_data/news.yml、_data/team_members.yml、_data/pi.yml、_data/alumni.yml、_data/grants.yml（schema 推导源——现有内容即基线）、03-PATTERNS.md（错误信息格式 D-07）</read_first>
  <files>scripts/validate.rb</files>
  <precondition>Task 1 完成（语法层与 errors 数组已就位，基线 PASS）</precondition>
  <reversibility rating="costly">D-02 按 news.yml 现状定 date 规则——CONTEXT 原文评级：若未来改主意转 ISO 日期，需同时改数据文件、feed.xml、news 页模板三处</reversibility>
  <action>在 validate.rb 的 YAML 语法层之上叠加结构层（per D-01：语法解析 + 结构检查双层，schema 字段清单直接采用 RESEARCH Code Examples §1 本机实测基线全绿的原型，勿自行加严——Pitfall 6「现有数据必须全绿」是 D-01 的基线约束）。① 通用规则：每文件 Psych 解析结果必须 is_a?(Array)，否则 errors 追加「<basename>：顶层结构应为列表（每条以 \"- \" 开头），当前是 <class>——新增条目请复制既有条目的 \"- \" 前缀格式」（拦截映射化静默失败：丢掉 \"- \" 后构建全绿、内容从页面消失）；条目级检查用 each_with_index，报错格式「<basename> 第 N 条：<字段> 字段缺失」（D-07 认可的示例格式）。② news.yml：必填 date、headline；date 规则 per D-02——值等于 \"Latest\" 仅允许首条（第 2 条及以后出现时报「news.yml 第 N 条：\"Latest\" 仅允许首条」——CONTEXT discretion 项，采 RESEARCH Open Question 3 推荐：sidebar/feed 按时间序展示，非首条 Latest 属数据摆放错误）；否则必须匹配全拼月份正则（January|February|March|April|May|June|July|August|September|October|November|December 任一 + 空格 + 4 位年份，全串锚定 \A...\z），不匹配时报错文案附合法格式示例（如「合法格式：May 2026 或首条 Latest」——A2 假设的摩擦缓解，报错自解释）。数据文件本身不动、feed.xml 的 site.time 回退不动（D-02 原文）。③ team_members.yml：必填 name、role、position；role 必须 ∈ {pi, member, student}（枚举来自文件首行注释原文「role: pi | member | student」），取值不合法时报错并列出三个合法值（role 是 _pages/team.md 的 where 判别键，拼错则人从页面消失）；photo/email/scholar/github/education 可选、不校验（模板有 if 守卫）。④ pi.yml：必填 education 且为非空数组（_pages/about.md:26 下标访问 site.data.pi[0].education）；educationshort 忽略（模板零消费死字段，Pitfall 7——设为可选或直接不检查）。⑤ alumni.yml：必填 name、period、degree、position（team.md 表格四列全部裸输出、无守卫）。⑥ grants.yml：必填 name（about.md:54 消费）。结构错误与语法错误共用同一 errors 数组、不中断其余文件检查（Pattern 3）。脚本保持只读（prohibition P-03-1）。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && scripts/validate.sh >/dev/null 2>&1 && cp _data/team_members.yml /tmp/v3-tm.bak && perl -pi -e 's/role: student/role: studnet/' _data/team_members.yml && scripts/validate.sh > /tmp/v3-t2-role.txt 2>&1; R1=$?; cp /tmp/v3-tm.bak _data/team_members.yml; [ "$R1" = "1" ] && grep -q 'role' /tmp/v3-t2-role.txt && grep -q 'pi' /tmp/v3-t2-role.txt && cp _data/news.yml /tmp/v3-news2.bak && perl -pi -e 's/date: "May 2026"/date: "2026-05"/' _data/news.yml && scripts/validate.sh > /tmp/v3-t2-date.txt 2>&1; R2=$?; cp /tmp/v3-news2.bak _data/news.yml; [ "$R2" = "1" ] && grep -q 'date' /tmp/v3-t2-date.txt && grep -q '2026' /tmp/v3-t2-date.txt && perl -pi -e 's/date: "May 2026"/date: "Latest"/' _data/news.yml && scripts/validate.sh > /tmp/v3-t2-latest.txt 2>&1; R3=$?; cp /tmp/v3-news2.bak _data/news.yml; [ "$R3" = "1" ] && grep -q 'Latest' /tmp/v3-t2-latest.txt && perl -pi -e 's/^- /  /' _data/news.yml && scripts/validate.sh > /tmp/v3-t2-map.txt 2>&1; R4=$?; cp /tmp/v3-news2.bak _data/news.yml; [ "$R4" = "1" ] && grep -q '顶层结构应为列表' /tmp/v3-t2-map.txt && scripts/validate.sh >/dev/null 2>&1 && [ -z "$(git status --porcelain _data papers)" ] && echo TASK2-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Baseline stays green after adding the schema layer (D-01: no new failures on existing data — Pitfall 6 guard)
    - role value "studnet" (probe rewrite of student) produces exit 1 with an error naming role and listing the legal values pi/member/student
    - date value "2026-05" produces exit 1 with an error naming date and showing the legal format
    - "Latest" placed in entries 2/3 (probe rewrite of May 2026 dates) produces exit 1 with an error mentioning Latest 仅允许首条 semantics
    - Mapping-ified news.yml (all "- " prefixes stripped) produces exit 1 with the 顶层结构应为列表 message — the exact silent-failure shape jekyll build passes on
    - All four probes restored; git status --porcelain over _data/ and papers/ empty; final baseline run exits 0
  </acceptance_criteria>
  <done>结构层落地：jekyll 静默吞掉的结构错误（role/date/Latest 位置/映射化）全部被中文报出并 exit 1，基线保持 0 新失败</done>
</task>

<task type="auto">
  <name>Task 3: BibTeX 第 2/3 层 — 逐条必填 title/author/year + 原始正则键唯一（D-03）</name>
  <read_first>03-RESEARCH.md（Pattern 1 三层校验与 7 行破坏形态实测矩阵、Pitfall 1 两类静默失败、Pitfall 2 重复键静默改名、Pitfall 4 API 形状、Don't Hand-Roll 表「引用键唯一性」唯一允许手写正则场景、Open Question 1/2 推荐）、papers/ref.bib（wang2026maize 无 doi 为 DOI 非必填实证）、03-CONTEXT.md（D-03 决策原文）</read_first>
  <files>scripts/validate.rb</files>
  <precondition>Task 1 完成（BibTeX 解析层与 errors 数组已就位；解析失败时第 2/3 层跳过——与 YAML 单文件短路同型）</precondition>
  <action>叠加 BibTeX 第 2、3 层（per D-03：解析成功 + 每条必填 title/author/year + 引用键唯一）。① 必填层：解析成功后 bib.to_a.each 遍历条目（Pitfall 4：bib.entries 是 Hash 不可当数组迭代；条目是 Hash 子类，只有 e[:field] 下标访问），对每条检查 e[:title]、e[:author]、e[:year] 三字段 to_s.strip.empty?（字段值带大括号原样——判空勿比对裸字符串），缺失则 errors 追加「papers/ref.bib <e[:bibtex_key]>：<字段> 字段缺失」。DOI 不检查（D-03 原文：现状 wang2026maize 等条目本就无 doi，规则须宽容到不误报）；journal 也不加必填（RESEARCH Open Question 1 推荐：按 D-03 字面执行 title/author/year，锁定决策不扩写——现状 12/12 虽全有 journal，但对在线预印本类新条目会误报）。② 键唯一层：对 File.read("papers/ref.bib") 的原始文本 scan(/@\w+\{([^,\s]+)\s*,/) 提取全部引用键后 tally，count > 1 则 errors 追加「papers/ref.bib：引用键 <k> 重复出现 <N> 次」。此层必须在原始文本上做、禁止用解析器 API 查重（Pitfall 2：bibtex-ruby 对重复键静默改名 k,k,k→k,l,m 且零告警，解析结果里不存在重复——正则提键是唯一真相源，Don't Hand-Roll 表中唯一允许的手写正则场景）；% 注释行（第 1 行「% Zhang Tao Lab Publications」）天然不匹配该正则，无需特判（Pitfall 7）。③ 不做 @type 白名单（RESEARCH Open Question 2 推荐：按现状只收 @article 的白名单会阻断 talks 页未来合法的 @incollection 数据；D-03 未锁此检查）。④ PASS 汇总行在 bib 计数后追加「, 键唯一」标记（无重复键时；RESEARCH Code Examples §4 风格）。解析层抛错时本任务两层跳过（exit 已由第 1 层置 1）。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && scripts/validate.sh > /tmp/v3-t3-base.txt 2>&1; RC=$?; [ "$RC" = "0" ] && grep -q '键唯一' /tmp/v3-t3-base.txt && cp papers/ref.bib /tmp/v3-bib3.bak && printf '@article{probeyear, title={T}, author={A}, journal={J}}\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3-t3-year.txt 2>&1; R1=$?; cp /tmp/v3-bib3.bak papers/ref.bib; [ "$R1" = "1" ] && grep -q 'probeyear' /tmp/v3-t3-year.txt && grep -q 'year 字段缺失' /tmp/v3-t3-year.txt && printf '@article{dupkey, title={T}, author={A}, year={2026}}\n@article{dupkey, title={T2}, author={A2}, year={2027}}\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3-t3-dup.txt 2>&1; R2=$?; cp /tmp/v3-bib3.bak papers/ref.bib; [ "$R2" = "1" ] && grep -q '引用键 dupkey 重复出现 2 次' /tmp/v3-t3-dup.txt && printf '@article{probetrunc, title={截断\n' >> papers/ref.bib && scripts/validate.sh > /tmp/v3-t3-trunc.txt 2>&1; R3=$?; cp /tmp/v3-bib3.bak papers/ref.bib; [ "$R3" = "1" ] && grep -q 'papers/ref.bib' /tmp/v3-t3-trunc.txt && scripts/validate.sh >/dev/null 2>&1 && [ -z "$(git status --porcelain _data papers)" ] && echo TASK3-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Baseline run exits 0 with PASS line now containing 键唯一; wang2026maize (no doi in ref.bib) still passes — DOI-optional tolerance proven on the live baseline
    - Appended entry @article{probeyear,...} without year produces exit 1 with output matching probeyear + "year 字段缺失"
    - Appended duplicate-key pair (dupkey twice, both otherwise complete) produces exit 1 with output exactly containing 引用键 dupkey 重复出现 2 次 — the raw-text tally catching what bibtex-ruby silently renames
    - Appended truncated entry (@article{probetrunc, title={截断 with no closing braces at EOF) produces exit 1 with a Chinese error naming papers/ref.bib — regardless of whether the parser throws (layer 1) or silently degrades the entry to empty fields (layer 2)
    - All probes restored; git status --porcelain over _data/ and papers/ empty; final baseline run exits 0
  </acceptance_criteria>
  <done>BibTeX 三层齐备：缺逗号/截断/缺必填/重复键四类破坏形态全部 exit 1 且中文报出文件+键/字段名；基线（含无 doi 条目）保持全绿</done>
</task>

</tasks>

## Artifacts this phase produces（Plan 01 部分）

- `scripts/validate.rb`（新文件）——独立 Ruby 脚本，无导出符号；CLI 契约 = 输出（PASS 单行汇总 / FAIL + 逐行中文错误 / D-08 提醒行〔Plan 02 加入〕）+ 退出码（0 全绿 / 1 有错）；内部符号约定：errors 数组、逐文件 schema 规则表（news: date+headline；team_members: name+role+position 且 role 枚举 pi/member/student；pi: education 非空数组；alumni: name+period+degree+position；grants: name）、BibTeX 必填字段集 title/author/year、原始正则提键 /@\w+\{([^,\s]+)\s*,/
- `scripts/validate.sh`（新文件）——exec 包装，符号：无（透传 argv）
- 输出格式契约：`PASS: news=6, team=4, pi=1, alumni=2, grants=2, bib=12, 键唯一`（计数动态计算）与 `FAIL: N 个问题`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 维护者手写内容（_data/*.yml、papers/ref.bib）→ 验证脚本 | 被校验内容不可信：畸形输入不得使脚本崩溃或掩盖其余错误（DoS 面）；news headline 现状含手写 HTML——脚本只读不执行内容 |
| 验证脚本 → 仓库内容文件 | 脚本对被校验文件只有读权限语义（CONTEXT：不做任何数据文件改动）——写入即违规 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / Elevation | validate.rb YAML 解析入口 | high | mitigate | 解析一律走 Psych.parse_file(...).to_ruby（parser API，与 Jekyll 数据读取接受面一致）；Task 1 automated 断言脚本含 Psych.parse_file 且无反序列化式加载入口（正/负双 grep）。注：YAML 反序列化属 canon 安全项——breadcrumb 至 /gsd-secure-phase 与本威胁项，不另铸 prohibition |
| T-03-03 | DoS | 畸形 yml/bib 触发脚本崩溃掩盖真错误 | medium | mitigate | errors 数组收集全部错误 + 逐文件 rescue 兜底（语法坏仅短路该文件），任何单文件异常不中断其余校验；截断条目 probe 证明坏输入仍得到明确中文判定而非崩溃 |
| T-03-05 | Tampering | 验证过程改写被校验内容文件 | medium | mitigate | 脚本严格只读（File.read / Psych.parse_file，无写路径）；prohibition P-03-1；每个 probe 的 automated 命令以 git status --porcelain 空 + 恢复后基线重跑绿收尾，写入行为会被当场证伪 |
| T-03-SC | Tampering | 包安装（npm/pip/cargo 等） | high | mitigate | 本阶段零包安装（D-06；RESEARCH Package Legitimacy Audit：无新增包，bibtex-ruby/psych 均为既有）——无可审对象，无 [ASSUMED]/[SUS] 包，故无需人工闸门检查点 |

</threat_model>

<verification>
- Task 1/2/3 的 automated 验证全绿（基线 PASS + probe 矩阵 CONTENT-01-a/b/c/d/e + 恢复证洁）
- 全套件（RESEARCH Validation Architecture phase gate 前半）：`scripts/validate.sh && bundle exec jekyll build --destination /tmp/_site_v3 && scripts/ci-smoke.sh /tmp/_site_v3` 三连绿——验证脚本不破坏正常构建链
- probe 纪律：每条破坏性 probe 后恢复现场并 git status --porcelain 证洁（零副作用纪律，PATTERNS Shared Patterns）
</verification>

<success_criteria>
- ROADMAP sc1（YAML 构建前报出）与 sc2（BibTeX 构建前报出）的本地半边成立：一条命令、中文、文件+行号/条目/键定位、exit 1
- ROADMAP sc3（错误信息清晰指出文件与位置）：YAML 行列号 + 条目序号 + 引用键/字段名三类定位全部有 probe 实证
- D-01~D-03、D-05~D-07 全部落地且被 probe 矩阵覆盖；基线 0 新失败（Pitfall 6 守住）
- CONTENT-01-a/b/c/d/e 五项 test map 全过（f/g 由 Plan 02 补齐）
</success_criteria>

<output>
Create `.planning/phases/03-content-validation/03-01-SUMMARY.md` when done
</output>
