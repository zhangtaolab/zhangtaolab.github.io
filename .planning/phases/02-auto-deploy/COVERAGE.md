# API Coverage — Phase 02 (auto-deploy)

No external API integration: one-time `gh` admin calls on the project's own GitHub repo (Pages/build_type/default_branch/variables); site external surface unchanged from Phase 1.
