---
phase: 02-auto-deploy
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified: []
autonomous: false
requirements: [DEPLOY-01]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "DEPLOY_ENABLED is still absent before the launch checkpoint and is set to true only after user approval (D-16 首次设闸放行的时序约束)"
    - "The first deploy run (workflow_dispatch with deploy=true) completes with the deploy job green and the github-pages environment deployment successful (D-15⑤/D-16)"
    - "https://zhangtaolab.org/publications/ returns 200 and the legacy path /Publication returns 404 after deployment (cache-buster + bounded retry — 新旧站点判别式)"
    - "A subsequent push to main auto-triggers a full pipeline run that completes green INCLUDING the deploy job — DEPLOY-01 稳态自动部署证明"
  artifacts:
    - path: "GitHub 仓库变量 DEPLOY_ENABLED（非文件）"
      provides: "常驻部署闸门/kill-switch：true=push 即部署；删除变量即停（D-16）"
  key_links:
    - from: "launch checkpoint（用户批准）"
      to: "gh variable set DEPLOY_ENABLED"
      via: "检查点通过是设变量的前置条件（D-18 核对通过 + D-09 产物确认 → D-16 放行）"
      pattern: "DEPLOY_ENABLED"
    - from: "deploy job（if 闸门放行）"
      to: "GitHub Pages 服务"
      via: "deploy-pages@v5 OIDC 发布 github-pages 工件 → cname=zhangtaolab.org 继承 → Cloudflare ~10 分钟全球生效"
      pattern: "deploy-pages"
  prohibitions: []
---

<objective>
## Phase Goal

**As a** 维护者（lab maintainer）, **I want to** 推送代码到 GitHub 后 Actions 自动完成完整构建（含 jekyll-scholar）并发布上线，本地与 CI 构建一致, **so that** 内容更新推送即自动发布，无需任何手工发布步骤。

## Plan 05 — 首次上线：人工闸门 → D-16 放行 → 热切换 → 稳态自动部署证明

Purpose: 执行 D-15⑤ 正式部署与 D-16 闸门机制，并证明 DEPLOY-01 的稳态（push 即自动发布）。顺序是决策锁定的：先由用户在阻断检查点确认验证模式产物（D-09）与出版物审计结论（D-18 通过是设闸前置），然后才设 DEPLOY_ENABLED=true 并 dispatch deploy=true 完成热切换，最后用一个普通 push 证明此后推送即自动上线。本 Plan 包含线上站替换（热切换）——回滚路径：远端 backup 分支（b09c9b31）或用户本地克隆重新推回。

Output: 新站上线 zhangtaolab.org（Cloudflare 缓存 ~10 分钟内全球切换）、DEPLOY_ENABLED 常驻闸门就位、push→自动部署闭环证明。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-auto-deploy/02-CONTEXT.md
@.planning/phases/02-auto-deploy/02-RESEARCH.md
@.planning/phases/02-auto-deploy/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 上线放行检查点（D-09 产物确认 + D-18 通过 + D-01/D-03 覆盖授权复核）</name>
  <what-built>首次上线前的全部机器证据：Plan 04 的两次绿色构建（push 首跑 + dispatch 验证模式 run，含 Smoke assertions 与 Ruby 4.0.6 日志证据）、Plan 03 的出版物逐条核对报告（.planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md，已在 Plan 03 检查点确认）、Plan 01 的五项修复已随构建生效。批准本检查点 = 授权完成 D-15 序列剩余步骤：热切换（新站替换旧站）**以及其后的 master 分支删除**（Plan 06 执行；D-01"完全覆盖"/D-03 已授权的 one-way 动作——本检查点是对该授权的执行前复核，删除的实际前置断言在 Plan 06 再次机器把关）。</what-built>
  <how-to-verify>
    1. 核对 Plan 04 SUMMARY 中的验证模式 run URL：build job（含 Smoke assertions）绿、deploy job skipped
    2. 确认 .planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md 末尾含 `Verdict: APPROVED` 标记行（Plan 03 检查点批准时由 executor 落盘——D-18 通过是本检查点前置；标记缺失即审计未通过，本检查点不得批准）
    3. 本地最后一眼：`bundle exec jekyll serve` 浏览首页/出版物/新闻页（可选，构建产物与 CI 同源同命令）
    4. 知悉批准后果一（热切换）：设置 DEPLOY_ENABLED=true → dispatch deploy=true → 线上立即替换（新站 /publications/ 上线、旧站 /Publication 消失；Cloudflare 缓存 max-age=600，全球生效约 10 分钟）
    5. 知悉批准后果二（one-way 收尾）：新站验证上线后，Plan 06 将删除远端 master 与 3 个 dependabot 分支——旧 git 历史仅存 backup 分支与本地克隆双保险（D-01/D-02）
    6. 知悉回滚路径：远端 backup 分支（b09c9b31）与本地克隆（~/GitHub/zhangtaolab.github.io-legacy-backup）双保险仍在
  </how-to-verify>
  <resume-signal>Type "approved — deploy"（授权热切换及后续清理）或描述问题（验证模式产物存疑 / 审计需重看 / 暂缓上线）</resume-signal>
</task>

<task type="auto">
  <name>Task 2: D-16 设闸 + D-15⑤ 首次正式部署 + 线上切换验证</name>
  <read_first>02-RESEARCH.md（Pattern 1 行为矩阵 dispatch+deploy=true 行；Runtime State Inventory 的 variable set 命令；Pitfall 6：部署后 Cloudflare 缓存 ~10 分钟勿误判失败）、02-CONTEXT.md（D-16 闸门机制全文、D-15⑤ 定位、D-01 回滚授权）、02-04-SUMMARY.md（验证模式证据与 pages API 回读值）</read_first>
  <files></files>
  <precondition>Task 1 检查点已 approved；gh variable list 中无 DEPLOY_ENABLED（闸门初始态，设置前断言）；gh api repos/zhangtaolab/zhangtaolab.github.io/pages 的 build_type 仍为 workflow</precondition>
  <reversibility rating="costly">热切换替换线上站；回滚 = 将 backup 分支内容经流水线重新部署（分钟级、无数据丢失），但期间访客已看到新站——属高影响可回退操作</reversibility>
  <action>per D-16/D-15⑤，顺序执行：① `gh variable set DEPLOY_ENABLED --body true -R zhangtaolab/zhangtaolab.github.io`（设闸——此后 push main 自动部署）；② `gh workflow run deploy.yml -f deploy=true -R zhangtaolab/zhangtaolab.github.io`（单次强制放行，deploy job 的 if 双条件第二个分支命中）；③ dispatch 后先等 ~15 秒再取 run id（`gh run list --workflow=deploy.yml --limit 1` 选最新 workflow_dispatch run——Actions 注册新 run 有延迟，过早查询会选中 Plan 04 的验证模式 dispatch run 导致误判失败，与 Task 3 同纪律），`gh run watch <run-id> --exit-status` 等待 deploy job 绿（environment github-pages 部署成功）。④ 线上切换验证（Pitfall 6 纪律：容忍 Cloudflare ~10 分钟）：循环最多 15 分钟、每 30 秒——`curl -fsS "https://zhangtaolab.org/publications/?cb=$RANDOM"` 返回 200 且 `curl -s -o /dev/null -w '%{http_code}' "https://zhangtaolab.org/Publication?cb=$RANDOM"` 返回 404（新站用 /publications/ 小写路径、旧站用 /Publication——这是新旧站的判别式）；Pages 直连域也应就绪：`curl -fsSI https://zhangtaolab.github.io/` 跟随重定向到 zhangtaolab.org 且 200。⑤ 回读 `gh api repos/zhangtaolab/zhangtaolab.github.io/pages` 记录 status（应 built）与 cname。失败处理：deploy job 红或 15 分钟后线上未切换——停，导出 run 日志与 pages API 状态回报；**不得**删闸门以外任何远端对象（master 删除属 Plan 06，且其前置是本任务绿）。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && gh variable list -R zhangtaolab/zhangtaolab.github.io | grep -q 'DEPLOY_ENABLED' && sleep 15 && RUN_ID=$(gh run list --workflow=deploy.yml --limit 1 --json databaseId,event --jq '.[] | select(.event=="workflow_dispatch") | .databaseId' | head -1) && gh run view "$RUN_ID" --json jobs,conclusion --jq '.conclusion, (.jobs[] | "\(.name): \(.conclusion)")' | tee /tmp/02-05-deploy.txt && grep -q 'success' /tmp/02-05-deploy.txt && grep -q 'deploy: success' /tmp/02-05-deploy.txt && N=000; O=000; for i in $(seq 1 30); do N=$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 "https://zhangtaolab.org/publications/?cb=$RANDOM$i"); O=$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 "https://zhangtaolab.org/Publication?cb=$RANDOM$i"); if [ "$N" = "200" ] && [ "$O" = "404" ]; then echo "SWITCHED after $i checks"; break; fi; sleep 30; done && [ "$N" = "200" ] && [ "$O" = "404" ] && echo TASK2-PASS</automated>
  </verify>
  <acceptance_criteria>
    - gh variable list shows DEPLOY_ENABLED = true (set only after checkpoint approval)
    - The deploy=true dispatch run completes with overall conclusion success AND deploy job conclusion success (not skipped)
    - https://zhangtaolab.org/publications/ returns 200 with a cache-buster query and https://zhangtaolab.org/Publication returns 404 (new site live, old path gone)
    - gh api .../pages shows status built with cname zhangtaolab.org
  </acceptance_criteria>
  <done>新站已在 zhangtaolab.org 上线（双路径判别式成立）、首次部署 run 全绿、DEPLOY_ENABLED 常驻闸门就位</done>
</task>

<task type="auto">
  <name>Task 3: 稳态证明——普通 push 触发完整自动部署（DEPLOY-01 验收）</name>
  <read_first>02-RESEARCH.md（Pattern 1 行为矩阵 push+DEPLOY_ENABLED='true'=完整部署行）、02-CONTEXT.md（D-06 push 触发、D-16 常驻态）</read_first>
  <files></files>
  <precondition>Task 2 全绿（DEPLOY_ENABLED=true 已生效且新站在线）</precondition>
  <action>per D-06/D-16 稳态验收：制造一次无内容影响的推送——`git commit --allow-empty -m "chore(02): verify push-triggered auto-deploy"` 后 `git push origin main`（若本地尚有未推送的真实提交，直接 push 它们即可，无需空提交）。观察新 run：事件列应为 Push、自动触发（无人工 dispatch）；`gh run watch --exit-status` 至完成——预期 build 绿 **且 deploy job 也绿**（DEPLOY_ENABLED=true 放行，与 Plan 04 的 skipped 形成对照，证明闸门常驻逻辑正确）。终验线上仍健康：`curl -fsS "https://zhangtaolab.org/publications/?cb=$RANDOM"` 200。此 run 即 DEPLOY-01"推送后自动构建并发布"的验收证据，run id 与结论记入 SUMMARY。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && git commit --allow-empty -m "chore(02): verify push-triggered auto-deploy" && git push origin main && sleep 15 && RUN_ID=$(gh run list --workflow=deploy.yml --limit 1 --json databaseId,event --jq '.[] | select(.event=="push") | .databaseId' | head -1) && [ -n "$RUN_ID" ] && gh run watch "$RUN_ID" --exit-status && gh run view "$RUN_ID" --json jobs --jq '.jobs[] | "\(.name): \(.conclusion)"' | tee /tmp/02-05-steady.txt && grep -q 'build: success' /tmp/02-05-steady.txt && grep -q 'deploy: success' /tmp/02-05-steady.txt && [ "$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 "https://zhangtaolab.org/publications/?cb=$RANDOM")" = "200" ] && echo TASK3-PASS</automated>
  </verify>
  <acceptance_criteria>
    - A push (not dispatch) to main auto-triggers a new deploy.yml run
    - That run completes with BOTH build: success and deploy: success (steady-state auto-deploy, DEPLOY-01)
    - Live site still returns 200 on /publications/ after the redeploy
  </acceptance_criteria>
  <done>推送即自动构建+发布闭环证明完成——DEPLOY-01 验收证据（push 事件 run 全绿）入库 SUMMARY</done>
</task>

</tasks>

## Artifacts this phase produces

- GitHub 仓库变量 `DEPLOY_ENABLED=true`（常驻自动部署闸门；删除即 kill-switch，per D-16）
- 首次 deployment（github-pages environment）与其 run 记录
- 一次空提交（chore(02): verify push-triggered auto-deploy）作为稳态触发器——不改动任何文件内容
- 本 Plan 不新增仓库文件

<flagged_assumptions>
Edge probe（spec-less fallback）DEPLOY-01 unresolved 的发布面在此落证：
- DEPLOY-01（可访问性假设）：假设"发布后站点经 Pages URL 与自定义域可访问"由 Task 2 的双路径判别式（/publications/ 200 且 /Publication 404，cache-buster + 15 分钟重试）与 pages API status=built 显式证实；Cloudflare ~10 分钟传播窗口已计入验证设计（Pitfall 6），不把窗口期误判为失败。
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 人工闸门 → 部署放行 | 唯一能开闸的路径：用户在检查点批准（D-09/D-18）→ 设变量 + dispatch |
| GitHub Pages → 公网访客（经 Cloudflare） | 首次部署即替换线上旧站；缓存窗口内新旧混杂属预期 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | 未授权/误触发部署（首闸开启） | high | mitigate | 开闸前置三条件机器断言（checkpoint approved / 变量原缺失 / build_type=workflow）；变量单次设置后 D-16 kill-switch 语义向用户明示 |
| T-02-12 | DoS | 热切换后新站异常（断言漏网问题上线） | high | mitigate | D-08 四断言在 deploy 前把门 + 双路径判别式在线上复核 + 回滚路径（backup 分支重推）已在检查点向用户明示 |
| T-02-13 | Tampering | 部署验证被缓存伪影误导（旧内容 200 误判为新站） | medium | mitigate | 判别式用新旧路径互补（新 200 且旧 404），不只看单一 URL；cache-buster 参数强制绕过 Cloudflare 缓存 |

</threat_model>

<verification>
- Task 2/3 automated 全绿（首部署 run + 稳态 push run 双证据 + 线上判别式）
- D-18 审计报告仍含 Verdict: APPROVED 标记行（追溯链完整）
- 本 Plan 结束时：master 仍在远端（删除属 Plan 06）——清理前的安全余量
</verification>

<success_criteria>
- 用户批准 → 设闸 → 热切换 → 线上验证 → 稳态自动部署，五环按 D-15⑤/D-16 锁定顺序完成
- DEPLOY-01 端到端证据：push 事件 run 全绿（build+deploy）+ 线上 200/404 判别式成立
- 闸门语义就位：此后 push 即发布、删变量即停
</success_criteria>

<output>
Create `.planning/phases/02-auto-deploy/02-05-SUMMARY.md` when done
</output>
