---
phase: 02-auto-deploy
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/phases/02-auto-deploy/old-site-publication.html
  - .planning/phases/02-auto-deploy/pub-extract-new.txt
  - .planning/phases/02-auto-deploy/pub-extract-old.txt
  - .planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md
autonomous: false
requirements: [DEPLOY-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A byte-archived copy of https://zhangtaolab.org/Publication is committed to the phase directory BEFORE any remote mutation happens (参照物抢收, per D-18)"
    - "Both sides are extracted to per-entry text files: new built site (expected 83 entries) and archived old site (expected ~91; exact count established by machine extraction, not the research-time fetch summary)"
    - "Every single old-site entry is classified one by one (matched / missing) and every unmatched new entry classified as extra — a per-entry table, not aggregate counts (per D-18)"
    - "02-PUBLICATION-AUDIT.md records totals for both sides, per-year breakdowns, the missing list (deploy-blocking) and the extra list (user-confirmation), and a verdict"
    - "The audit checkpoint is resolved (user decision recorded) before Plan 05 may set DEPLOY_ENABLED — 核对通过是 D-16 首次放行的前置条件"
  artifacts:
    - path: ".planning/phases/02-auto-deploy/old-site-publication.html"
      provides: "旧站 /Publication 页原文存档（热切换后参照物消失，此文件是唯一本地参照）"
    - path: ".planning/phases/02-auto-deploy/pub-extract-new.txt"
      provides: "新站构建产物出版物条目规范化清单（YEAR | normalized title | first author | raw title）"
    - path: ".planning/phases/02-auto-deploy/pub-extract-old.txt"
      provides: "旧站条目规范化清单（同格式）"
    - path: ".planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md"
      provides: "逐条核对报告：方法、双侧总数、逐年对照表、missing/extra 分类、结论"
      contains: "Missing"
  key_links:
    - from: "_site/publications/index.html（构建产物）"
      to: "pub-extract-new.txt"
      via: "提取脚本按年份标题分桶解析条目三元组（标题/首作者/年份）"
      pattern: "pub-extract-new"
    - from: "old-site-publication.html（存档）"
      to: "pub-extract-old.txt"
      via: "同一脚本/同一规范化规则解析旧站 HTML，保证两侧可比"
      pattern: "pub-extract-old"
    - from: "02-PUBLICATION-AUDIT.md"
      to: "Plan 05 launch checkpoint"
      via: "审计结论 = D-16 首次设 DEPLOY_ENABLED 的前置条件（D-18 锁定）"
      pattern: "DEPLOY_ENABLED"
  prohibitions:
    - statement: "MUST NOT drop, replace, or silently lose any publication entry during migration or deployment work — the deployed site must contain every old-site publication entry unless the user explicitly waives a specific entry at the audit checkpoint (缺失阻断上线, per D-18)"
      status: resolved
      verification: judgment
---

<objective>
## Phase Goal

**As a** 维护者（lab maintainer）, **I want to** 推送代码到 GitHub 后 Actions 自动完成完整构建（含 jekyll-scholar）并发布上线，本地与 CI 构建一致, **so that** 内容更新推送即自动发布，无需任何手工发布步骤。

## Plan 03 — D-18 出版物逐条核对（首次上线阻断闸门）

Purpose: 用户在 plan-phase 期间补充的决策 D-18（2026-08-19）：首次上线前必须把新站出版物页与现线上旧站 https://zhangtaolab.org/Publication **一条一条逐一比对**（每条标题/作者/年份 + 条目总数）。语料事实（研究证实）：新站出版物页是手写 markdown `_pages/publications.md`（实测 83 条编号条目、19 个年份标题），**不是** papers/ref.bib 渲染（ref.bib 仅 12 条且只喂 talks.md，与本页无关）；旧站实测约 91 条——83 vs 91 的 8 条差额正是本核对要定性的对象。规则（D-18 锁定）：新站**缺失**任何旧站条目 → 阻断上线并回报；新站**多出**条目 → 列出供用户确认后放行。核对必须发生在 D-16 设 DEPLOY_ENABLED 之前、D-15⑥ 删 master 之前，且必须**先抓取存档旧站页面原文**（旧站被替换后参照物消失）。

Output: 旧站页面存档 + 双侧规范化条目清单 + 逐条核对报告 + 用户确认（阻断性检查点）。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-auto-deploy/02-CONTEXT.md
@.planning/phases/02-auto-deploy/02-RESEARCH.md
@.planning/phases/02-auto-deploy/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 抢收旧站参照物 + 构建新站产物 + 双侧条目提取</name>
  <read_first>02-RESEARCH.md（Pattern 4：D-18 语料纠偏表——83 条手写 markdown vs 旧站约 91 条、ref.bib 不参与比对；流程设计四步）、_pages/publications.md（条目书写格式与年份标题结构）、.planning/phases/02-auto-deploy/02-PATTERNS.md</read_first>
  <files>.planning/phases/02-auto-deploy/old-site-publication.html, .planning/phases/02-auto-deploy/pub-extract-new.txt, .planning/phases/02-auto-deploy/pub-extract-old.txt</files>
  <precondition>https://zhangtaolab.org/Publication 返回 200（旧站仍在线服务——Plan 04 之前必然成立，执行时先确认；若非 200 立即停下回报，参照物丢失是阻断性事故）</precondition>
  <action>per D-18：第一步抢收参照物——`curl -fsSL --compressed -A "Mozilla/5.0" https://zhangtaolab.org/Publication -o .planning/phases/02-auto-deploy/old-site-publication.html`（若 Cloudflare 拒绝默认 UA 则带浏览器 UA 重试；存档必须完整落盘并 git add，热切换后此文件是唯一参照）。第二步本地构建：`JEKYLL_ENV=production bundle exec jekyll build`。第三步写一次性提取脚本（ruby 或 python3，executor 自选；脚本可留 .planning 目录或用后即弃，产物必须入库）：解析**两侧 HTML**——新侧 `_site/publications/index.html`（手写列表的构建产物，按年份标题 h2 分桶、桶内逐条目取三元组）；旧侧存档 HTML（先检视其真实 DOM 结构——年份标题与条目容器的标签形态以文件实际内容为准，再写对应解析）。每条输出一行，格式 `YEAR | normalized-title | first-author | raw-title`，其中 normalized-title = 去标签、HTML 实体解码（&amp; &rarr; 等）、转小写、去全部非字母数字字符、压空白；first-author 取条目作者序列第一位（两侧行文格式可能不同——新站手写格式以 _pages/publications.md 实际书写为准，先读文件确认作者与标题的先后顺序再写解析）。产出 pub-extract-new.txt 与 pub-extract-old.txt 两个清单文件并提交。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && test -s .planning/phases/02-auto-deploy/old-site-publication.html && grep -q '20[0-2][0-9]' .planning/phases/02-auto-deploy/old-site-publication.html && NEW=$(wc -l < .planning/phases/02-auto-deploy/pub-extract-new.txt | tr -d ' ') && OLD=$(wc -l < .planning/phases/02-auto-deploy/pub-extract-old.txt | tr -d ' ') && echo "new=$NEW old=$OLD" && [ "$NEW" -ge 80 ] && [ "$OLD" -ge 80 ] && NEWYEARS=$(cut -d'|' -f1 .planning/phases/02-auto-deploy/pub-extract-new.txt | sort -u | wc -l | tr -d ' ') && OLDYEARS=$(cut -d'|' -f1 .planning/phases/02-auto-deploy/pub-extract-old.txt | sort -u | wc -l | tr -d ' ') && echo "year-buckets new=$NEWYEARS old=$OLDYEARS" && [ "$NEWYEARS" = "$OLDYEARS" ] && echo TASK1-PASS</automated>
  </verify>
  <acceptance_criteria>
    - old-site-publication.html exists, is non-empty, and contains year headings (archived before any remote mutation)
    - pub-extract-new.txt line count equals the machine-extracted new-site entry count (expected 83; record exact — if != 83, investigate the extractor before proceeding, do not adjust the threshold)
    - pub-extract-old.txt line count is the exact machine-extracted old-site count (research-time estimate was ~91 — the exact number is established here, record it)
    - Both files have identical year-bucket sets (research found 19/19 identical year headings 2006-2026; equality of sets is the assertion, not the literal 19)
    - Both extracts committed to git
  </acceptance_criteria>
  <done>参照物已存档入库、双侧清单已生成入库、年份桶对齐、精确条目数已确立（新侧与旧侧各多少条写入 SUMMARY）</done>
</task>

<task type="auto">
  <name>Task 2: 逐条比对 + 分类 + 审计报告</name>
  <read_first>.planning/phases/02-auto-deploy/pub-extract-new.txt（全读）、.planning/phases/02-auto-deploy/pub-extract-old.txt（全读）、02-RESEARCH.md（Pattern 4 流程第 2-3 步与 D-18 规则原文）、02-CONTEXT.md（D-18 决策全文）</read_first>
  <files>.planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md</files>
  <action>per D-18：在年份桶内做**逐条**比对——旧侧每一条按 normalized-title 精确匹配新侧同桶条目；未命名的残留对（旧剩 × 新剩）由 executor 逐对人工阅读 raw-title 做模糊判定（标点/斜体/连字符/子标题差异；首作者与年份作辅助裁决），模糊判定必须显式列出判定理由。输出 02-PUBLICATION-AUDIT.md，内容五节：① 方法（语料纠偏声明：新侧为手写 markdown 构建产物，ref.bib 不参与；规范化规则；模糊判定清单）；② 总数（新侧 N 条 / 旧侧 M 条 / 差额）；③ 逐年对照表——**旧侧每条一行**（年份、raw-title、首作者、状态 MATCHED/MISSING、匹配到的新侧条目或模糊判定理由），这是"一条一条"要求的落地形态；④ 新侧多出条目表（EXTRA，含年份与 raw-title——迁移后新增论文属此类）；⑤ 结论——missing 条数（>0 即 verdict: BLOCK）、extra 条数、放行条件声明（missing=0 且 extra 经用户确认，或用户明确豁免特定条目并记录）。报告与两个清单一并提交。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && test -s .planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md && OLD=$(wc -l < .planning/phases/02-auto-deploy/pub-extract-old.txt | tr -d ' ') && ROWS=$(grep -cE '^\| *(19|20)[0-9]{2} *\|.*\| *(MATCHED|MISSING) *\|' .planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md) && echo "old=$OLD rows=$ROWS" && [ "$ROWS" -ge "$OLD" ] && grep -q 'EXTRA' .planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md && grep -q 'verdict' .planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md && echo TASK2-PASS</automated>
  </verify>
  <acceptance_criteria>
    - 02-PUBLICATION-AUDIT.md contains one MATCHED/MISSING status row per old-site entry (row count >= pub-extract-old.txt line count — no old entry may be skipped)
    - Report contains an EXTRA section listing every unmatched new entry, a totals section with both exact counts, and a verdict line (BLOCK if missing > 0, else conditional-PASS pending user confirmation)
    - Every fuzzy match decision is listed with its rationale
    - Report committed to git
  </acceptance_criteria>
  <done>逐条核对完成：旧侧每条有状态行、新侧差额全部定性（missing/extra）、结论与放行条件成文入库</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-18 审计结论人工裁决（阻断闸门）</name>
  <what-built>D-18 出版物逐条核对报告：新侧精确条数 vs 旧侧精确条数、逐年对照表（旧侧每条的 MATCHED/MISSING 状态）、新侧多出条目表、模糊判定清单。报告路径 .planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md；原始清单 pub-extract-new.txt / pub-extract-old.txt；旧站原文存档 old-site-publication.html。</what-built>
  <how-to-verify>
    1. 打开 .planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md，读总数与结论行（missing 几条 / extra 几条）
    2. 若 missing > 0（D-18 判定 BLOCK）：逐条审阅 MISSING 行——选择 (a) 暂停本阶段、先补录这些条目到 _pages/publications.md 后重跑本 Plan；(b) 明确豁免特定条目（逐条列出并在报告中记录豁免理由）；不做选择则部署保持阻断
    3. 若 extra > 0：逐条审阅 EXTRA 表，确认为迁移后新增论文（应与 2026 年新发表一致）而非错录
    4. 抽查 3-5 条 MATCHED 行与双侧清单原文，确认匹配判定可信
    5. 本检查点通过 = D-16 首次设 DEPLOY_ENABLED 的前置条件满足（per D-18）
  </how-to-verify>
  <resume-signal>Type "approved"（审计接受，放行后续部署计划）或描述问题（missing 需补录 / extra 存疑 / 匹配判定有误）</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- 新文件 `.planning/phases/02-auto-deploy/old-site-publication.html`（旧站 /Publication 原文存档——热切换后的唯一本地参照）
- 新文件 `.planning/phases/02-auto-deploy/pub-extract-new.txt`、`pub-extract-old.txt`（双侧规范化条目清单，行格式 `YEAR | normalized-title | first-author | raw-title`）
- 新文件 `.planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md`（逐条核对报告：MATCHED/MISSING 状态行、EXTRA 表、verdict）
- 一次性提取/比对脚本（ruby 或 python3，executor 自选——可入库 .planning 或用后即弃，不入仓库 scripts/）

<flagged_assumptions>
Edge probe（spec-less fallback）将 DEPLOY-01 标记为 unclassified/unresolved——本 Plan 承接其对"产物完整性"一面的显式落证：
- DEPLOY-01（内容完整性假设）：假设"构建并部署的出版物页不丢条目"可由本 Plan 的逐条审计 + Plan 05/06 的线上锚点检查显式证实，而非默认成立。审计报告即证据；若旧侧计数（研究摘要值 91）与机器提取值不符，以机器提取为准并在此报告差异。
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 公网旧站 → 本地存档 | curl 拉取的是将被替换的线上内容；存档必须先行且入库（完整性 = 回滚参照） |
| 存档/产物 → 审计结论 | 提取脚本的解析正确性直接决定阻断闸门的有效性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering/DoS | 参照物丢失（旧站被提前替换或存档不完整） | high | mitigate | Task 1 precondition 强制先验 200；存档文件 test -s + 年份标题 grep 断言；入库提交后才开始任何远端操作（Plan 04 在后续 wave） |
| T-02-09 | Tampering | 提取脚本漏提/错提导致假阴性（漏报 missing） | high | mitigate | 行数断言（new>=80 / old>=80）+ 年份桶集合相等断言 + 报告行数 == 旧侧清单行数（逐条不可跳过）+ 模糊判定必须列理由供检查点人工抽查 |
</threat_model>

<verification>
- Task 1/2 automated 命令全绿（含"报告行数 >= 旧侧条数"这一条-one-by-one 的机器化证明）
- 检查点已解决（approved / 豁免记录在案）
- 本 Plan 完成后 DEPLOY_ENABLED 仍未设置（闸门保持关闭直到 Plan 05）
</verification>

<success_criteria>
- D-18 全流程落地：存档 → 双侧提取 → 逐条分类 → 报告 → 用户裁决
- 8 条差额（83 vs ~91）已完全定性（每条要么 MATCHED 要么 MISSING/EXTRA，无"未解释"条目）
- 审计产物全部提交入库
</success_criteria>

<output>
Create `.planning/phases/02-auto-deploy/02-03-SUMMARY.md` when done
</output>
