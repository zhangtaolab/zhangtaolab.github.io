---
phase: 02-auto-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _config.yml
  - .gitignore
  - _includes/head.html
  - feed.xml
  - _pages/home.md
  - _pages/about.md
  - _pages/publications.md
  - _pages/news.md
  - _pages/contact.md
  - _pages/blogs.md
  - _pages/teaching.md
  - _pages/talks.md
  - _pages/allnews.md
autonomous: true
requirements: [DEPLOY-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "JEKYLL_ENV=production bundle exec jekyll build exits 0 with all five review fixes applied (D-10~D-14)"
    - "_site/sitemap.xml contains at least 10 <loc> URLs after D-11 (expected 11: 8 flipped + 3 pre-existing)"
    - "_site/allnews.html no longer exists and _site/news/index.html does (D-13)"
    - "Every feed.xml news item carries a unique guid and links to /news/ (D-13/D-14)"
    - "xmllint --noout _site/feed.xml passes after the guid/link changes"
    - "head.html renders dark_mode: false as false (allow_false fix, D-12)"
  artifacts:
    - path: "_config.yml"
      provides: "exclude list gains - vendor entry (D-10 defensive layer)"
      contains: "- vendor"
    - path: ".gitignore"
      provides: "vendor/ ignored directory-wide (D-10)"
      contains: "vendor/"
    - path: "_includes/head.html"
      provides: "dark_mode default filter honors explicit false (D-12)"
      contains: "allow_false: true"
    - path: "feed.xml"
      provides: "news items with unique guid + /news/ link (D-13/D-14)"
      contains: "isPermaLink=\"false\""
    - path: "_pages/allnews.md"
      provides: "DELETED duplicate news page (D-13)"
  key_links:
    - from: "feed.xml news loop"
      to: "_data/news.yml"
      via: "site.data.news iteration — guid built from forloop.index + article_date"
      pattern: "forloop\\.index"
    - from: "8 flipped _pages/*.md frontmatter"
      to: "jekyll-sitemap generation"
      via: "removal of the sitemap exclusion key lets the plugin emit the URLs"
      pattern: "<loc>"
  prohibitions: []
---

<objective>
## Phase Goal

**As a** 维护者（lab maintainer）, **I want to** 推送代码到 GitHub 后 Actions 自动完成完整构建（含 jekyll-scholar）并发布上线，本地与 CI 构建一致, **so that** 内容更新推送即自动发布，无需任何手工发布步骤。

## Plan 01 — D-10~D-14 评审遗留随车修复

Purpose: 用户决策 5 项全修（D-10 vendor exclude、D-11 sitemap 全量化、D-12 dark_mode false 语义、D-13 删重复新闻页、D-14 RSS guid）。这五项是 Phase 1 代码评审的遗留问题，必须在首次推送 main 之前落入历史——它们直接决定 Plan 02 冒烟断言（D-08）的断言面（sitemap ≥10 URL、feed 合法性）与发布产物的正确性。

Output: 修复后的站点源文件 + 本地 production 构建全绿 + sitemap 3→11 URL。不触碰任何远端。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-auto-deploy/02-CONTEXT.md
@.planning/phases/02-auto-deploy/02-RESEARCH.md
@.planning/phases/02-auto-deploy/02-PATTERNS.md
</context>

<tasks>

<!-- planner-discipline-allow: sitemap: false -->
<!-- planner-discipline-allow: allnews -->

<task type="auto">
  <name>Task 1: D-10 vendor 排除防御层 + D-12 dark_mode 显式 false 修复</name>
  <read_first>02-RESEARCH.md（Pattern 3 的 D-10/D-12 小节：含 Jekyll 4.4.1 DEFAULT_EXCLUDES 源码取证与 Liquid default 过滤器语义）、02-PATTERNS.md（_config.yml 与 head.html 段落）、_config.yml（exclude 列表，约 93-103 行）、.gitignore、_includes/head.html（44 行附近）</read_first>
  <files>_config.yml, .gitignore, _includes/head.html</files>
  <action>三处原位小改，均不改其他键。① D-10（per D-10）：_config.yml 的 exclude 列表在 `- docs` 之后追加一行 `- vendor`；同一决策下把 .gitignore 第 8 行的 `vendor/bundle` 改为 `vendor/`（本地 bundle path 同类操作有实义）。定性说明：Jekyll 4.4.1 的 DEFAULT_EXCLUDES 已追加式排除 vendor/bundle/（RESEARCH 已源码取证+本地实验双重证实），本项是零成本防御层而非"不修就泄漏"的阻断项——真正的兜底是 Plan 02 ci-smoke.sh 的 vendor tripwire 断言，不要在任务里夸大本项的前提。② D-12（per D-12）：_includes/head.html 第 44 行 `var darkMode = {{ site.dark_mode | default: true }};` 在 default 过滤器内追加 `allow_false: true` 参数（Liquid 官方修复参数，RESEARCH 已取证：default 对 empty/false/nil 均回退，导致 `dark_mode: false` 永远失效）。改后构建一次确认无 Liquid 报错。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && grep -c '^- vendor$' _config.yml | grep -qx 1 && grep -c '^vendor/$' .gitignore | grep -qx 1 && grep -c 'allow_false: true' _includes/head.html | grep -qx 1 && JEKYLL_ENV=production bundle exec jekyll build && echo TASK1-PASS</automated>
  </verify>
  <acceptance_criteria>
    - _config.yml contains exactly one `- vendor` exclude entry
    - .gitignore contains the line `vendor/`
    - _includes/head.html contains `default: true, allow_false: true`
    - JEKYLL_ENV=production bundle exec jekyll build exits 0（已知无害告警：assets/main.css 与 main.scss 输出目标冲突——静态快照胜出，不当作失败）
  </acceptance_criteria>
  <done>D-10 与 D-12 落盘且 production 构建退出码 0，已提交</done>
</task>

<task type="auto">
  <name>Task 2: D-11 sitemap 全量化——8 页翻出黑名单</name>
  <read_first>02-RESEARCH.md（Pattern 3 的 D-11 小节：jekyll-sitemap 源码机制 + 黑名单 10 页清单 + 11 URL 预期推导）、02-PATTERNS.md（8 页 frontmatter 段落）、_pages/research.md（ Analog：已在 sitemap 内的页面的 frontmatter 形态）、_pages/home.md（待改页面代表）</read_first>
  <files>_pages/home.md, _pages/about.md, _pages/publications.md, _pages/news.md, _pages/contact.md, _pages/blogs.md, _pages/teaching.md, _pages/talks.md</files>
  <action>per D-11：对上述 8 个页面各删除 frontmatter 中 `sitemap: false` 那一行（各文件约第 4 行；其余字段 title/layout/permalink 一律不动）。可用一次循环 sed 批量删除（macOS 需 `sed -i ''`），或逐文件 Edit——8 处改动完全同构。两个例外不得动：_pages/404.md 保留该行（404 不该进 sitemap）；_pages/allnews.md 本任务不动（Task 3 整文件删除）。删除后跑 production 构建并数 sitemap URL：预期 11（8 翻转 + research/software/team 3 个原有）；断言用阈值 ≥10（精确数记录进 SUMMARY，不写成硬断言——内容页数将来会变，精确断言会脆断，per RESEARCH Pitfall 5）。robots.txt 的 Sitemap 广播行保留不动（per D-11）。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && [ "$(grep -l '^sitemap: false' _pages/home.md _pages/about.md _pages/publications.md _pages/news.md _pages/contact.md _pages/blogs.md _pages/teaching.md _pages/talks.md 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && JEKYLL_ENV=production bundle exec jekyll build && [ "$(grep -c '<loc>' _site/sitemap.xml)" -ge 10 ] && echo TASK2-PASS</automated>
  </verify>
  <acceptance_criteria>
    - None of the 8 named _pages files contains a `sitemap: false` line anymore
    - _pages/404.md still contains its `sitemap: false` line (unchanged)
    - JEKYLL_ENV=production bundle exec jekyll build exits 0
    - _site/sitemap.xml contains >= 10 `<loc>` entries (record the exact count — expected 11 — in the SUMMARY)
  </acceptance_criteria>
  <done>8 页翻出黑名单，sitemap 从 3 URL 升至 ≥10（预期 11），已提交</done>
</task>

<task type="auto">
  <name>Task 3: D-13 删除 allnews 重复页 + D-14 feed 条目 guid/link 修复</name>
  <read_first>02-RESEARCH.md（Pattern 3 的 D-13/D-14 小节：news 循环现状 21-30 行、posts 循环 guid 先例 17-18 行、RSS 2.0 无 permalink 条目惯例）、02-PATTERNS.md（feed.xml 段落：两处编辑的行号与目标 liquid）、feed.xml、_data/news.yml、_pages/news.md（保留的双胞胎页）</read_first>
  <files>feed.xml, _pages/allnews.md</files>
  <action>per D-13：删除 _pages/allnews.md 整个文件（与 news.md 正文 100% 同为 site.data.news 全量循环，双页并存且链接各指一处是评审④的根因）。per D-13+D-14：feed.xml 的 news 循环（`{% for article in site.data.news limit:20 %}` 块内）两处修改——① 条目 `<link>` 由指向 allnews.html 的旧值改为 `{{ site.url }}{{ site.baseurl }}/news/`；② 在 link 之后新增一行 guid 元素，形如 `<guid isPermaLink="false">news-{{ forloop.index }}-{{ article_date | date_to_xmlschema | default: 'latest' }}</guid>`（news 数据无独立 URL 字段，forloop.index + 日期组合保证条目唯一；对齐同文件 posts 循环 17-18 行已有的 guid 写法风格）。既有的 `strip_html | xml_escape` 过滤器与 `article_date == "Latest"` 回退逻辑（约 25-26 行）保持不动。sidebar 已指 /news/ 无需改动。改后全仓扫一遍确认旧页引用清零。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && [ ! -e _pages/allnews.md ] && [ ! -e _site/allnews.html ] && [ -e _site/news/index.html ] && grep -c 'guid isPermaLink="false">news-' feed.xml | grep -qx 1 && [ "$(grep -rIl 'allnews' _pages _includes _layouts _data feed.xml _config.yml assets robots.txt 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && JEKYLL_ENV=production bundle exec jekyll build && xmllint --noout _site/feed.xml && echo TASK3-PASS</automated>
  </verify>
  <acceptance_criteria>
    - _pages/allnews.md is deleted and _site/allnews.html is absent after build
    - feed.xml news loop contains the non-permalink guid pattern (isPermaLink="false", news- prefix, forloop.index + date)
    - No file under _pages, _includes, _layouts, _data, feed.xml, _config.yml, assets, robots.txt references allnews anymore
    - xmllint --noout _site/feed.xml passes (feed still valid XML after guid insertion)
  </acceptance_criteria>
  <done>双新闻页收敛为 /news/ 单页，feed 条目 guid 唯一化，构建+XML 校验全绿，已提交</done>
</task>

</tasks>

## Artifacts this phase produces

本 Plan 不新增文件符号；产物为修改/删除：
- 修改：`_config.yml`（exclude 增 `- vendor` 条目）、`.gitignore`（`vendor/` 行）、`_includes/head.html`（allow_false 参数）、`feed.xml`（news 条目 guid 元素 + link 改 `/news/`）、8 个 `_pages/*.md`（各删一行 frontmatter 键）
- 删除：`_pages/allnews.md`
- 新构建基线：`_site/sitemap.xml` 3→11 URL（本 Plan 的可观测输出，供 Plan 02 断言依赖）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 维护者本地 → 构建产物 | 本 Plan 全部改动在本地源文件与 _site 产物，无网络面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | Gemfile/Jekyll 供应链 | medium | accept | 本 Plan 不改任何 gem 依赖；Gemfile.lock 锁定组合（Jekyll 4.4.1）不变，风险面与 Phase 1 相同 |
| T-02-07 | Tampering | feed.xml RSS 注入 | low | mitigate | guid/link 只使用模板内已有变量与字面量前缀，不引入 _data 之外的原始字符串，沿用既有 strip_html/xml_escape 过滤器 |
</threat_model>

<verification>
- 三任务各自 automated 命令全绿
- 收尾总检：`JEKYLL_ENV=production bundle exec jekyll build && scripts 之外的检查`——sitemap ≥10 URL（SUMMARY 记录精确值）、feed xmllint 通过、allnews 产物不存在、vendor 防御层配置在位
- 全部改动以原子提交入库（本 Plan 结束后工作树 clean，是 Plan 04 push 的前置）
</verification>

<success_criteria>
- D-10~D-14 五项决策全部落地且可被 grep/xmllint 断言
- production 构建退出码 0，无新增告警类别
- 工作树 clean、全部提交完成（准备好被 Plan 04 推送）
</success_criteria>

<output>
Create `.planning/phases/02-auto-deploy/02-01-SUMMARY.md` when done
</output>
