---
phase: 02-auto-deploy
plan: 06
type: execute
wave: 5
depends_on: ["02-05"]
files_modified: []
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Remote branches are exactly main and backup after cleanup (master deleted per D-03/D-15⑥; 3 dependabot refs deleted per D-17; backup untouched per D-02)"
    - "The live site is unaffected by branch deletion: key pages still 200 and feed/sitemap still valid (workflow-mode Pages depends on no branch)"
    - "The full phase acceptance battery passes post-cleanup: home/publications/news 200, publications anchor present online, feed.xml valid XML, sitemap >= 10 URLs online, pages API status built with cname zhangtaolab.org"
  artifacts:
    - path: "远端分支布局（非文件）"
      provides: "终态：main（默认）+ backup（回滚锚点）——旧历史仅存于 backup 与用户本地克隆"
  key_links:
    - from: "Plan 05 稳态验证（新站已在线）"
      to: "git push origin --delete master"
      via: "D-15⑥ 的唯一前置：新站线上已验证 + D-18 已通过——本 Plan 任务前置条件把它变成机器可检断言"
      pattern: "--delete master"
  prohibitions:
    - statement: "MUST NOT delete the remote master branch (or otherwise destroy old-site git history) before the new site is verified live AND the D-18 publication audit has passed — the deletion task's preconditions encode both, and the backup branch must never be touched (per D-01/D-02/D-15⑥)"
      status: resolved
      verification: judgment
---

<objective>
## Phase Goal

**As a** 维护者（lab maintainer）, **I want to** 推送代码到 GitHub 后 Actions 自动完成完整构建（含 jekyll-scholar）并发布上线，本地与 CI 构建一致, **so that** 内容更新推送即自动发布，无需任何手工发布步骤。

## Plan 06 — D-15⑥ 收尾：删除 master + D-17 分支清理 + 终局验收

Purpose: 执行接管序列的唯一破坏性动作（D-15⑥ 删 master，时点锁定在"最后"，前置 = 新站已验证上线 + D-18 已通过），顺手清理 3 个 dependabot 旧分支（D-17），然后跑终局验收把 Phase 2 四条成功标准逐条落证。删 master 属 one-way 操作，用户已在 D-01（"旧版本全部抛弃，可以进行完全覆盖"）与 D-03 中明确授权——本 Plan 记录评级与前置断言，不重复设问。backup 分支是回滚锚点，永不触碰（D-02）。

Output: 远端分支终态（main + backup）、终局验收全绿（线上四类检查 + Pages API 状态）。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-auto-deploy/02-CONTEXT.md
@.planning/phases/02-auto-deploy/02-RESEARCH.md
@.planning/phases/02-auto-deploy/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-15⑥ 删除远端 master + D-17 清理 dependabot 分支</name>
  <read_first>02-CONTEXT.md（D-01 覆盖授权、D-02 backup 双保险、D-03 分支布局、D-15⑥ 时点锁定、D-17 清单）、02-RESEARCH.md（Runtime State Inventory：3 个 dependabot 分支全名与"98 个旧 PR ref 由 GitHub 托管不可删也无需删"）、02-05-SUMMARY.md（新站在线证据）</read_first>
  <files></files>
  <precondition>四项全真才可执行（D-15⑥ 前置的机器化）：① https://zhangtaolab.org/publications/ 返回 200（新站已验证上线）；② 02-PUBLICATION-AUDIT.md 存在且其检查点结论为 approved（D-18 已通过）；③ git ls-remote origin backup 非空且回滚锚点在位；④ gh api repos/zhangtaolab/zhangtaolab.github.io/pages 的 build_type 为 workflow（Pages 不依赖任何分支）</precondition>
  <reversibility rating="one-way">删除远端 master = 旧站 git 历史在远端的主副本消失（backup 分支与用户本地克隆为剩余双保险）；用户已在 D-01/D-03 明确授权完全覆盖，且 Plan 05 Task 1 上线检查点的第 5 步对该 one-way 授权做了执行前人工复核（按 planner-reversibility"已决策不重问"原则不再单独设 checkpoint:decision），本任务前置四断言是最终机器把关</reversibility>
  <action>per D-15⑥/D-03：`git push origin --delete master`（唯一破坏性动作，时点在新站验证上线之后——前置断言已证）。per D-17：依次 `git push origin --delete dependabot/bundler/addressable-2.8.1`、`dependabot/bundler/kramdown-2.3.1`、`dependabot/bundler/rexml-3.3.3`。**backup 分支不碰**（D-02 回滚锚点）；98 个旧 PR ref 属 GitHub 托管对象，不可删也无需删（D-17 原文）。删后回读分支终态并记录。若任一前置断言不满足：停止并回报，绝不带病执行 one-way 操作。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && [ "$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 "https://zhangtaolab.org/publications/?cb=$RANDOM")" = "200" ] && git ls-remote --heads origin backup | grep -q 'refs/heads/backup' && test -s .planning/phases/02-auto-deploy/02-PUBLICATION-AUDIT.md && git push origin --delete master && git push origin --delete dependabot/bundler/addressable-2.8.1 && git push origin --delete dependabot/bundler/kramdown-2.3.1 && git push origin --delete dependabot/bundler/rexml-3.3.3 && BRANCHES=$(gh api 'repos/zhangtaolab/zhangtaolab.github.io/branches?per_page=100' --jq '[.[].name] | sort | join(",")') && echo "branches=$BRANCHES" && [ "$BRANCHES" = "backup,main" ] && echo TASK1-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Pre-delete assertions held: live 200 on /publications/, audit report present, backup branch exists at origin
    - git push origin --delete master succeeded
    - All three dependabot branches deleted (addressable-2.8.1, kramdown-2.3.1, rexml-3.3.3)
    - Final remote branch list via gh api is exactly: backup, main
  </acceptance_criteria>
  <done>远端分支终态 backup+main，旧历史仅存双保险副本，backup 未受任何影响</done>
</task>

<task type="auto">
  <name>Task 2: 终局验收扫描——Phase 2 成功标准逐条落证</name>
  <read_first>02-RESEARCH.md（Validation Architecture：Phase gate 定义；Pitfall 6 缓存窗口）、.planning/ROADMAP.md（Phase 2 四条成功标准）、scripts/ci-smoke.sh（本地对照的同源断言）</read_first>
  <files></files>
  <precondition>Task 1 完成（分支终态 backup+main）</precondition>
  <action>分支删除后在线上跑完整验收电池并逐条记录进 SUMMARY（对应 ROADMAP 四条成功标准）：① 触发与构建——Plan 05 已证 push 自动触发完整 run（引用其 run id）；② 发布可访问——`curl -fsS https://zhangtaolab.org/` 200、`/publications/` 200 且页面源码含锚点 s41467-026-73769-8、`/news/` 200；③ CI 一致性——引用 Plan 04 的 Linux 绿跑与 Ruby 4.0.6 日志证据 + Gemfile.lock 平台条目；④ 线上显示与本地一致——`/feed.xml` 200 且下载后 `xmllint --noout` 通过、`/sitemap.xml` 200 且 `<loc>` 计数 ≥10 且等于本地值、Pages API `status: built` 且 `cname: zhangtaolab.org`。所有 URL 检查带 cache-buster 参数规避 Cloudflare 缓存伪影；任何一项不过即回报（分支已删但 backup 在——回滚路径仍有效）。结果表（检查项/命令/实测值/判定）写入 SUMMARY。</action>
  <verify>
    <automated>cd /Users/forrest/GitHub/zhangtaolab.github.io && CB="cb=$RANDOM" && [ "$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 "https://zhangtaolab.org/?$CB")" = "200" ] && curl -fsS --max-time 15 "https://zhangtaolab.org/publications/?$CB" | grep -q 's41467-026-73769-8' && [ "$(curl -s -o /dev/null -w '%{http_code}' --max-time 15 "https://zhangtaolab.org/news/?$CB")" = "200" ] && curl -fsS --max-time 15 "https://zhangtaolab.org/feed.xml?$CB" -o /tmp/02-06-feed.xml && xmllint --noout /tmp/02-06-feed.xml && curl -fsS --max-time 15 "https://zhangtaolab.org/sitemap.xml?$CB" -o /tmp/02-06-sitemap.xml && [ "$(grep -c '<loc>' /tmp/02-06-sitemap.xml)" -ge 10 ] && gh api repos/zhangtaolab/zhangtaolab.github.io/pages | grep -q '"status": "built"' && gh api repos/zhangtaolab/zhangtaolab.github.io/pages | grep -q '"cname": "zhangtaolab.org"' && echo TASK2-PASS</automated>
  </verify>
  <acceptance_criteria>
    - Home, /publications/ (with DOI anchor in served HTML), and /news/ all return 200 on the live domain
    - Live feed.xml downloads and passes xmllint --noout; live sitemap.xml contains >= 10 loc entries (record exact, expected to equal the local count)
    - gh api pages shows status built and cname zhangtaolab.org after branch deletion
    - SUMMARY contains the acceptance table mapping each ROADMAP success criterion to its evidence (run ids, HTTP codes, counts)
  </acceptance_criteria>
  <done>Phase 2 四条成功标准全部有线上实测证据，终局验收表入 SUMMARY</done>
</task>

</tasks>

## Artifacts this phase produces

- 远端状态变更（非文件）：删除分支 master、dependabot/bundler/addressable-2.8.1、dependabot/bundler/kramdown-2.3.1、dependabot/bundler/rexml-3.3.3（保留 main、backup）
- 终局验收表（记录于 02-06-SUMMARY.md：检查项/命令/实测值/判定，映射 ROADMAP 四条成功标准）
- 本 Plan 不新增仓库文件

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| one-way 删除操作 → 回滚能力 | master 删除后回滚能力仅剩 backup 分支 + 本地克隆；前置断言是最后防线 |
| 分支布局变更 → Pages 服务 | workflow 模式 Pages 不依赖任何分支；删后线上复查确认无影响 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | DoS（回滚能力） | 过早/误删 master 丢失旧站历史 | high | mitigate | 删除前置四断言（线上 200 / 审计 approved / backup 在位 / build_type=workflow）；one-way 评级记录；backup 分支列为永不触碰对象 |
| T-02-14 | DoS | 分支删除意外影响 Pages 服务 | medium | mitigate | Task 2 删后全面线上复查（页面/锚点/feed/sitemap/pages API）；异常时回滚路径（backup 重推）仍开放并明示 |

</threat_model>

<verification>
- Task 1/2 automated 全绿（前置断言 → 删除 → 终态复核 → 线上验收电池）
- 02-PUBLICATION-AUDIT.md 与旧站存档仍在库中（追溯链完整保留）
- Phase 2 至此满足 /gsd-verify-work 前置：全链机器真值 + 人工检查点已决
</verification>

<success_criteria>
- D-15 六步序列完整走完（⑥ 如期最后执行），D-17 完成，backup 完好
- ROADMAP Phase 2 四条成功标准逐条有线上证据
- 删除操作全程留痕（前置断言输出 + 分支终态记录于 SUMMARY）
</success_criteria>

<output>
Create `.planning/phases/02-auto-deploy/02-06-SUMMARY.md` when done
</output>
